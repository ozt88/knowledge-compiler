---
phase: 10-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-verify/10-VERIFICATION.md
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-02
tags:
  - verification
  - hooks
  - rtk
  - gsd
must_haves:
  truths:
    - "~/.claude/settings.json은 유효한 JSON이며 PreToolUse hook 5개가 존재한다"
    - "PreToolUse 배열에 'rtk hook claude' command를 가진 hook이 존재한다"
    - "PreToolUse 배열에 'gsd-validate-commit.sh'를 참조하는 Bash hook이 존재한다"
    - "`rtk git status` 실행 시 '~ Modified:' 또는 '? Untracked:' 형식의 RTK 압축 출력이 나타난다"
    - "`gsd-validate-commit.sh`에 git commit JSON을 stdin으로 파이프하면 exit code 0을 반환한다"
    - "검증 결과가 10-VERIFICATION.md에 기록된다"
  artifacts:
    - path: "/home/ozt88/knowledge-compiler/.planning/phases/10-verify/10-VERIFICATION.md"
      provides: "세 검증 명령의 실행 출력 및 판정 기록"
      contains: "VERIFY-01, VERIFY-02"
  key_links:
    - from: "~/.claude/settings.json PreToolUse[4]"
      to: "rtk hook claude 바이너리"
      via: "Claude Code PreToolUse hook 실행"
      pattern: "rtk hook claude"
    - from: "~/.claude/settings.json PreToolUse[3]"
      to: "gsd-validate-commit.sh"
      via: "bash 실행"
      pattern: "gsd-validate-commit.sh"
---

<objective>
Phase 9에서 등록된 RTK PreToolUse hook이 기존 GSD hook과 충돌 없이 공존하는지,
실제 CLI 출력에서 RTK 압축이 동작하는지, GSD commit hook이 여전히 정상 exit code 0을
반환하는지를 세 검증 명령으로 확인하고 결과를 VERIFICATION.md에 기록한다.

Purpose: v1.2 Token Optimization 마일스톤 완료 선언 전, RTK 설치 후 기존 GSD
워크플로가 깨지지 않았음을 객관적으로 입증.
Output: `.planning/phases/10-verify/10-VERIFICATION.md` — 세 검증 명령의 실제 출력과
기대 결과 비교표를 포함한 판정 보고서.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-verify/10-RESEARCH.md
@.planning/phases/10-verify/10-PATTERNS.md
@.planning/phases/10-verify/10-VALIDATION.md

<interfaces>
<!-- 검증 대상 파일의 관련 구조 — executor는 읽기 전용으로 참조만 한다 -->

~/.claude/settings.json PreToolUse 배열 (5개 hook, index 순):
  0. [Write|Edit] node .../gsd-prompt-guard.js
  1. [Write|Edit] node .../gsd-read-guard.js
  2. [Write|Edit] node .../gsd-workflow-guard.js
  3. [Bash]       bash /home/ozt88/.claude/hooks/gsd-validate-commit.sh
  4. [Bash]       rtk hook claude

gsd-validate-commit.sh opt-in 계약:
  - .planning/config.json의 hooks.community !== true 면 exit 0
  - 현재 프로젝트 config.json에 hooks.community 키 없음 → 항상 exit 0

RTK hook stdin/stdout 계약:
  input : {"tool_name":"Bash","tool_input":{"command":"git status"}}
  output: {"hookSpecificOutput":{"updatedInput":{"command":"rtk git status"},
          "permissionDecision":"allow",...}}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: settings.json JSON 유효성 + RTK/GSD hook 공존 검증 (VERIFY-01)</name>
  <files>.planning/phases/10-verify/10-VERIFICATION.md</files>
  <read_first>
    - /home/ozt88/.claude/settings.json (현재 PreToolUse 배열 실제 상태 확인)
    - .planning/phases/10-verify/10-RESEARCH.md (SC-1 기대 출력 참조)
    - .planning/phases/10-verify/10-PATTERNS.md (PreToolUse 배열 구조 참조)
  </read_first>
  <action>
    아래 bash 명령을 정확히 실행하고 stdout/stderr/exit code를 모두 캡처한다.
    결과 판정 후 `.planning/phases/10-verify/10-VERIFICATION.md` 파일을 생성하여
    이 task의 결과 섹션을 기록한다 (다음 task들이 이어서 append).

    실행 명령 (그대로 복사하여 실행):
    ```bash
    python3 -c "
    import json
    with open('/home/ozt88/.claude/settings.json') as f:
        s = json.load(f)
    pre = s['hooks']['PreToolUse']
    rtk_found = any('rtk hook claude' in hh['command'] for h in pre for hh in h.get('hooks', []))
    gsd_commit_found = any('gsd-validate-commit.sh' in hh['command'] for h in pre for hh in h.get('hooks', []))
    print('JSON valid: OK')
    print(f'RTK hook: {\"FOUND\" if rtk_found else \"MISSING\"}')
    print(f'GSD commit hook: {\"FOUND\" if gsd_commit_found else \"MISSING\"}')
    print(f'Total PreToolUse hooks: {len(pre)}')
    "
    ```

    기대 출력 (정확히 일치해야 PASS):
    ```
    JSON valid: OK
    RTK hook: FOUND
    GSD commit hook: FOUND
    Total PreToolUse hooks: 5
    ```

    VERIFICATION.md에 다음 프론트매터 및 SC-1 섹션을 기록:
    - frontmatter: phase: 10, plan: 01, status: in-progress, verified_at: <UTC ISO8601>, requirements: [VERIFY-01, VERIFY-02]
    - `# Phase 10 — Verification Report` 제목
    - `## SC-1: settings.json 구조 검증 (VERIFY-01)` 섹션에
      실행 명령(fenced bash), 실제 출력(fenced), 기대 출력(fenced), 판정(PASS/FAIL),
      요구사항 ID(VERIFY-01)를 표로 기록.

    어떤 라인이라도 기대값과 불일치(MISSING, Total != 5, JSON parse error)면
    판정을 FAIL로 기록하고 차이점을 명시한다. task는 FAIL이어도 파일 기록 후 완료 처리
    (후속 task에서 갭 클로저 판단).
  </action>
  <acceptance_criteria>
    - 파일 `.planning/phases/10-verify/10-VERIFICATION.md`가 존재한다
    - 파일에 literal string `## SC-1:`이 포함된다
    - 파일에 literal string `JSON valid: OK`가 포함된다 (실제 실행 출력)
    - 파일에 literal string `RTK hook: FOUND`가 포함된다
    - 파일에 literal string `GSD commit hook: FOUND`가 포함된다
    - 파일에 literal string `Total PreToolUse hooks: 5`가 포함된다
    - 파일에 literal string `VERIFY-01`이 포함된다
    - 파일에 literal string `판정: PASS` 또는 `판정: FAIL`이 포함된다
    - frontmatter에 `phase: 10`, `plan: 01`이 포함된다
    - grep 검증: `grep -q 'JSON valid: OK' .planning/phases/10-verify/10-VERIFICATION.md && grep -q 'Total PreToolUse hooks: 5' .planning/phases/10-verify/10-VERIFICATION.md`
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json; json.load(open('/home/ozt88/.claude/settings.json'))" && grep -q 'JSON valid: OK' .planning/phases/10-verify/10-VERIFICATION.md && grep -q 'Total PreToolUse hooks: 5' .planning/phases/10-verify/10-VERIFICATION.md && grep -q 'VERIFY-01' .planning/phases/10-verify/10-VERIFICATION.md</automated>
  </verify>
  <done>
    VERIFICATION.md SC-1 섹션에 실행 명령·실제 출력·기대 출력·판정이 기록되고,
    grep 검증 4개(JSON valid, Total 5, VERIFY-01, 파일 존재)가 모두 통과한다.
  </done>
</task>

<task type="auto">
  <name>Task 2: RTK 압축 출력 실제 확인 (VERIFY-02 / SC-2)</name>
  <files>.planning/phases/10-verify/10-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/10-verify/10-VERIFICATION.md (Task 1에서 생성된 파일에 append)
    - .planning/phases/10-verify/10-RESEARCH.md (RTK 압축 출력 포맷 참조)
  </read_first>
  <action>
    `rtk git status`를 현재 working directory(`/home/ozt88/knowledge-compiler`)에서
    실행하고 stdout/stderr/exit code를 캡처한다.

    실행 명령:
    ```bash
    rtk git status
    echo "exit code: $?"
    ```

    기대 출력 포맷 (정확한 문자열보다 패턴 매칭):
    ```
    * <branch>...<remote> [ahead N] 또는 동등한 브랜치 상태 라인
    ~ Modified: <N> files
       <파일 경로>
    ? Untracked: <N> files
       <파일 경로>
    exit code: 0
    ```

    판정 기준 (모두 충족 시 PASS):
    1. exit code == 0
    2. 출력에 `~ Modified:` 또는 `? Untracked:` 중 최소 하나 포함 (RTK 압축 포맷 시그니처)
    3. 출력에 원본 git status의 장황한 안내 문구 `(use "git add` 또는 `(use "git restore` 이 포함되지 않는다
       (RTK가 안내 문구를 제거했음을 입증)

    VERIFICATION.md에 `## SC-2: RTK 압축 출력 확인 (VERIFY-02)` 섹션을 append하여
    실행 명령(fenced bash), 실제 출력(fenced, 전체 캡처), 판정 기준 3개 체크리스트,
    최종 판정(PASS/FAIL), 요구사항 ID(VERIFY-02)를 표로 기록.

    빈 출력 또는 exit code != 0 이면 FAIL로 기록하되 파일 append는 수행.
  </action>
  <acceptance_criteria>
    - 파일에 literal string `## SC-2:`가 포함된다
    - 파일에 literal string `rtk git status`가 포함된다 (실행한 명령 기록)
    - 파일에 `~ Modified:` 또는 `? Untracked:` 중 최소 하나 포함된다 (실제 RTK 출력 캡처)
    - 파일에 literal string `exit code: 0`이 포함된다
    - 파일에 literal string `VERIFY-02`가 포함된다
    - 파일에 `판정: PASS` 또는 `판정: FAIL`이 SC-2 섹션 내에 포함된다
    - grep 검증: `grep -q '## SC-2' .planning/phases/10-verify/10-VERIFICATION.md && grep -qE '(~ Modified|\? Untracked)' .planning/phases/10-verify/10-VERIFICATION.md`
  </acceptance_criteria>
  <verify>
    <automated>rtk git status > /dev/null 2>&1 && grep -q '## SC-2' .planning/phases/10-verify/10-VERIFICATION.md && grep -qE '(~ Modified|\? Untracked)' .planning/phases/10-verify/10-VERIFICATION.md && grep -q 'VERIFY-02' .planning/phases/10-verify/10-VERIFICATION.md</automated>
  </verify>
  <done>
    VERIFICATION.md에 SC-2 섹션이 append되고 실제 `rtk git status` 출력이 캡처되며
    RTK 압축 시그니처(`~ Modified:` 또는 `? Untracked:`)가 파일에 기록된다.
  </done>
</task>

<task type="auto">
  <name>Task 3: gsd-validate-commit.sh 충돌 없음 검증 + 최종 판정 기록 (VERIFY-02 / SC-3)</name>
  <files>.planning/phases/10-verify/10-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/10-verify/10-VERIFICATION.md (Task 1, 2에서 누적된 파일에 최종 append)
    - /home/ozt88/.claude/hooks/gsd-validate-commit.sh (opt-in 구조 확인)
    - .planning/config.json (hooks.community 키 부재 확인 — 없어야 정상)
    - .planning/phases/10-verify/10-RESEARCH.md (SC-3 기대 결과 참조)
  </read_first>
  <action>
    Step 1 — gsd-validate-commit.sh dry-run 실행:
    ```bash
    echo '{"tool_name":"Bash","tool_input":{"command":"git commit -m \"docs(10): verify phase complete\""}}' \
      | bash /home/ozt88/.claude/hooks/gsd-validate-commit.sh
    echo "exit code: $?"
    ```

    기대 결과:
    - exit code: 0 (이유: .planning/config.json에 hooks.community: true 없음 → opt-in 비활성 → 즉시 exit 0)
    - stdout에 block 메시지 없음 (정상 통과)

    판정 기준:
    1. exit code == 0 → PASS
    2. exit code == 2 또는 stdout에 `"decision": "block"` 포함 → FAIL (충돌 발생)

    Step 2 — VERIFICATION.md에 `## SC-3: GSD commit hook 충돌 없음 검증 (VERIFY-02)` 섹션
    append:
    - 실행 명령(fenced bash)
    - 실제 출력(fenced, 전체 캡처 — stdout + "exit code: N")
    - 판정(PASS/FAIL)
    - 요구사항 ID(VERIFY-02)

    Step 3 — 파일 끝에 `## 최종 판정` 섹션을 append:
    - 요구사항별 판정 표:
      | Requirement | Success Criterion | 판정 |
      |-------------|-------------------|------|
      | VERIFY-01 | SC-1 settings.json 구조 | PASS/FAIL |
      | VERIFY-02 | SC-2 RTK 압축 출력    | PASS/FAIL |
      | VERIFY-02 | SC-3 GSD hook 공존    | PASS/FAIL |
    - 전체 판정: 3/3 PASS 시 `**Phase 10 검증 완료: PASS**` 기록, 아니면
      `**Phase 10 검증 실패: <실패한 SC 나열>**` 기록
    - frontmatter의 `status:`를 `in-progress` → `complete`(PASS 시) 또는
      `gaps-found`(FAIL 시)로 변경

    Step 4 — 커밋 준비 안내는 생략 (gsd workflow가 자동 처리).
  </action>
  <acceptance_criteria>
    - 파일에 literal string `## SC-3:`가 포함된다
    - 파일에 literal string `gsd-validate-commit.sh`가 SC-3 섹션 내에 포함된다
    - 파일에 SC-3 섹션 내 `exit code: 0` 기록이 포함된다
    - 파일에 literal string `## 최종 판정`이 포함된다
    - 파일에 literal string `**Phase 10 검증 완료: PASS**` 또는 `**Phase 10 검증 실패:` 중 하나가 포함된다
    - frontmatter의 `status:` 값이 `complete` 또는 `gaps-found`이다 (더 이상 `in-progress` 아님)
    - 최종 판정 표에 VERIFY-01과 VERIFY-02가 모두 등장한다
    - grep 검증: `grep -q '## SC-3' .planning/phases/10-verify/10-VERIFICATION.md && grep -q '## 최종 판정' .planning/phases/10-verify/10-VERIFICATION.md && grep -qE '(complete|gaps-found)' .planning/phases/10-verify/10-VERIFICATION.md`
  </acceptance_criteria>
  <verify>
    <automated>echo '{"tool_name":"Bash","tool_input":{"command":"git commit -m test"}}' | bash /home/ozt88/.claude/hooks/gsd-validate-commit.sh; test $? -eq 0 && grep -q '## SC-3' .planning/phases/10-verify/10-VERIFICATION.md && grep -q '## 최종 판정' .planning/phases/10-verify/10-VERIFICATION.md && grep -qE 'status: (complete|gaps-found)' .planning/phases/10-verify/10-VERIFICATION.md</automated>
  </verify>
  <done>
    VERIFICATION.md에 SC-3 섹션과 최종 판정 표가 append되고, frontmatter status가
    complete 또는 gaps-found로 업데이트되며, 세 SC 모두 실행 출력과 판정이 기록된다.
  </done>
</task>

</tasks>

<verification>
Phase-level 검증 (executor가 plan 완료 직전 실행):

```bash
# 1. VERIFICATION.md 존재 및 세 SC 섹션 모두 포함
test -f .planning/phases/10-verify/10-VERIFICATION.md && \
  grep -q '## SC-1' .planning/phases/10-verify/10-VERIFICATION.md && \
  grep -q '## SC-2' .planning/phases/10-verify/10-VERIFICATION.md && \
  grep -q '## SC-3' .planning/phases/10-verify/10-VERIFICATION.md && \
  grep -q '## 최종 판정' .planning/phases/10-verify/10-VERIFICATION.md

# 2. 요구사항 ID 모두 언급됨
grep -q 'VERIFY-01' .planning/phases/10-verify/10-VERIFICATION.md && \
  grep -q 'VERIFY-02' .planning/phases/10-verify/10-VERIFICATION.md

# 3. settings.json 실제 상태 재확인 (참조 무결성)
python3 -c "import json; s=json.load(open('/home/ozt88/.claude/settings.json')); \
  pre=s['hooks']['PreToolUse']; \
  assert any('rtk hook claude' in hh['command'] for h in pre for hh in h.get('hooks',[])), 'RTK hook missing'; \
  assert any('gsd-validate-commit.sh' in hh['command'] for h in pre for hh in h.get('hooks',[])), 'GSD hook missing'; \
  assert len(pre) == 5, f'Expected 5 hooks, got {len(pre)}'; \
  print('OK')"
```
</verification>

<success_criteria>
- [ ] `.planning/phases/10-verify/10-VERIFICATION.md` 파일 생성됨
- [ ] SC-1 섹션: settings.json JSON 유효 + RTK/GSD hook FOUND + Total 5 기록
- [ ] SC-2 섹션: `rtk git status` 실제 출력 캡처 + `~ Modified:` 또는 `? Untracked:` 시그니처 포함
- [ ] SC-3 섹션: gsd-validate-commit.sh dry-run exit code 0 기록
- [ ] 최종 판정 표: VERIFY-01, VERIFY-02 각각에 대한 PASS/FAIL 기록
- [ ] frontmatter status: complete(전체 PASS) 또는 gaps-found(1개 이상 FAIL)
- [ ] `~/.claude/settings.json`은 수정되지 않음 (읽기 전용 검증)
- [ ] `~/.claude/hooks/gsd-validate-commit.sh`는 수정되지 않음 (읽기 전용 검증)
</success_criteria>

<output>
완료 후 `.planning/phases/10-verify/10-01-SUMMARY.md` 생성:
- 세 SC의 실제 실행 결과 요약
- 최종 판정 (PASS 시 Phase 10 종료, FAIL 시 gap closure 필요 항목 명시)
- 다음 단계 안내: 전체 PASS 시 `/gsd-verify-work 10` 실행 준비, FAIL 시
  `/gsd-plan-phase 10 --gaps`로 갭 클로저 플랜 생성
</output>
