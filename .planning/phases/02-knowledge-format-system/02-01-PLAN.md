---
phase: 02-knowledge-format-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - patches/gsd-phase-researcher.patch.md
  - patches/gsd-verifier.patch.md
autonomous: true
requirements:
  - COMPILE-03
  - COMPILE-04
  - COMPILE-05

must_haves:
  truths:
    - "컴파일러가 guardrails.md를 신규 생성하는 지시가 두 패치 파일에 존재한다"
    - "guardrails.md 형식 지시가 긍정형 액션으로 기술되고 인라인 예시 2개를 포함한다"
    - "anti-patterns.md 형식 지시가 관찰-이유-대신 구조로 기술되고 인라인 예시 2개를 포함한다"
    - "기존 anti-patterns.md가 있을 때 마이그레이션 절차가 명시되어 있다"
    - "두 패치 파일의 형식 지시 블록이 동일하다"
    - "두 파일(guardrails.md, anti-patterns.md)의 역할 경계가 분류 기준과 함께 명시되어 있다"
  artifacts:
    - path: "patches/gsd-phase-researcher.patch.md"
      provides: "Step 0 incremental 컴파일 지시 — guardrails.md + anti-patterns.md 이중 구조"
      contains: "guardrails.md"
    - path: "patches/gsd-verifier.patch.md"
      provides: "Step 10b full reconcile 지시 — guardrails.md + anti-patterns.md 이중 구조"
      contains: "guardrails.md"
  key_links:
    - from: "patches/gsd-phase-researcher.patch.md"
      to: "patches/gsd-verifier.patch.md"
      via: "동일한 형식 지시 블록 (D-08)"
      pattern: "guardrails\\.md.*긍정형"
---

<objective>
researcher와 verifier 패치 파일에 guardrails.md 신규 생성 지시를 추가하고, anti-patterns.md 형식을 관찰-이유-대신 구조로 재정의하며, 기존 anti-patterns.md 마이그레이션 절차를 명시한다.

Purpose: 컴파일러가 knowledge 파일을 생성할 때 절대적 케이스(guardrails)와 맥락 의존적 케이스(anti-patterns)를 구분하여 긍정형으로 기술하도록 지시를 체계화한다.
Output: 수정된 두 패치 파일 (동일한 형식 지시 블록 포함)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-format-system/02-CONTEXT.md
@.planning/phases/02-knowledge-format-system/02-RESEARCH.md

<interfaces>
<!-- 현재 패치 파일의 수정 대상 섹션 구조 -->

From patches/gsd-phase-researcher.patch.md (Line 17-22, Step 0 4번 항목):
```markdown
4. Compile new raw entries into knowledge/ structure:
   - `decisions.md` — 시도 → 결과 → 결정. 상태 태그: `[active]`, `[rejected]`, `[superseded]`, `[uncertain]`
   - `anti-patterns.md` — 피해야 할 패턴과 그 이유 목록
   - `troubleshooting.md` — 에러 메시지 ↔ 해결책 매핑
   - `index.md` — 전체 요약 + 키워드 인덱스
5. 기존 knowledge 파일을 읽은 뒤 새 항목을 추가하거나 기존 항목을 업데이트하여 병합하라
```

From patches/gsd-verifier.patch.md (Line 16-20, Step 10b 3번 항목):
```markdown
3. Full reconcile — reprocess ALL raw entries and rebuild knowledge/:
   - `decisions.md` — 모든 시도/결정 통합, 상태 태그 정합성 확인
   - `anti-patterns.md` — 새로 발견된 anti-pattern 추가
   - `troubleshooting.md` — 에러/해결 매핑 갱신
   - `index.md` — 전체 요약 재생성
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: researcher 패치 파일에 guardrails.md + anti-patterns.md 형식 지시 확장</name>
  <files>patches/gsd-phase-researcher.patch.md</files>
  <read_first>
    - patches/gsd-phase-researcher.patch.md (수정 대상 — 현재 상태 확인)
    - .planning/phases/02-knowledge-format-system/02-CONTEXT.md (D-01~D-08 잠긴 결정)
    - .planning/phases/02-knowledge-format-system/02-RESEARCH.md (Code Examples 섹션 — 목표 패치 구조)
  </read_first>
  <action>
`patches/gsd-phase-researcher.patch.md`의 Step 0, 4번 항목(Line 17~21)을 아래 내용으로 교체한다.

**교체 전 (Line 17~21):**
```
4. Compile new raw entries into knowledge/ structure:
   - `decisions.md` — 시도 → 결과 → 결정. 상태 태그: `[active]`, `[rejected]`, `[superseded]`, `[uncertain]`
   - `anti-patterns.md` — 피해야 할 패턴과 그 이유 목록
   - `troubleshooting.md` — 에러 메시지 ↔ 해결책 매핑
   - `index.md` — 전체 요약 + 키워드 인덱스
```

**교체 후:**
```
4. Compile new raw entries into knowledge/ structure:
   - `decisions.md` — 시도 → 결과 → 결정. 상태 태그: `[active]`, `[rejected]`, `[superseded]`, `[uncertain]`
   - `guardrails.md` — 대안이 하나로 확정되는 케이스를 긍정형 액션으로 기술한다
     - 형식: "## [주제]\n[긍정형 액션]" (예: "~경유 필수", "~방식 사용")
     - 예시 (절대적 케이스): "## raw/ 읽기\nraw/ 읽기는 knowledge/index.md 경유 필수"
     - 예시 (대안 확정 케이스): "## decisions.md 병합\ndecisions.md는 decisions/ 하위 파일 병합 방식 사용"
   - `anti-patterns.md` — 상황에 따라 달라질 수 있는 맥락 의존적 케이스를 관찰-이유-대신 구조로 기술한다
     - 형식: "## [주제]\n**관찰:** [현상]\n**이유:** [문제 원인]\n**대신:** [권장 접근]"
     - 예시 1: "## 컴파일러 부정형 지시\n**관찰:** knowledge 파일 생성 지시에 '하지 마라' 형식 사용\n**이유:** LLM은 부정형 지시 준수율이 낮아 원치 않는 패턴이 잔존\n**대신:** 긍정형 액션으로 재기술 (Phase 1 전환 원칙 적용)"
     - 예시 2: "## raw/ 파일 직접 쿼리\n**관찰:** index.md를 거치지 않고 raw/*.md를 직접 읽어 분석\n**이유:** 컨텍스트 비용이 높고 관련 없는 정보까지 로드됨\n**대신:** index.md에서 관련 날짜 파악 후 해당 raw 파일만 선택적으로 읽기"
   - `troubleshooting.md` — 에러 메시지 ↔ 해결책 매핑
   - `index.md` — 전체 요약 + 키워드 인덱스
   
   분류 기준: 대안이 하나로 확정되는가?
   - YES → `guardrails.md`에 긍정형 액션으로 기술
   - NO (상황에 따라 달라짐) → `anti-patterns.md`에 관찰-이유-대신 구조로 기술
   
   기존 `anti-patterns.md`가 존재하는 경우:
   - 모든 항목을 읽어 위 분류 기준 적용
     - 대안이 하나로 확정 → `guardrails.md`에 긍정형 액션으로 재기술
     - 상황에 따라 달라짐 → `anti-patterns.md`에 관찰-이유-대신 구조로 재형식화
   - 마이그레이션 완료 후 `anti-patterns.md`를 새 형식으로 덮어쓴다
```

주의사항:
- Line 22 이후 (`5. 기존 knowledge 파일을 읽은 뒤...`)와 그 아래 `**During research**` 섹션은 변경하지 않는다
- `guardrails.md`가 `anti-patterns.md`보다 위에 위치해야 한다 (신규 파일이므로 먼저 소개)
- 인라인 예시는 knowledge-compiler 도메인 소재를 사용한다 (per D-02)
- 형식 지시 블록의 정확한 텍스트를 보존하라 — Task 2에서 동일한 블록을 verifier 패치에 적용한다 (per D-08)
  </action>
  <verify>
    <automated>grep -c "guardrails.md" patches/gsd-phase-researcher.patch.md && grep -c "관찰.*이유.*대신\|관찰-이유-대신" patches/gsd-phase-researcher.patch.md && grep -c "대안이 하나로 확정" patches/gsd-phase-researcher.patch.md && grep -c "마이그레이션 완료 후" patches/gsd-phase-researcher.patch.md && grep -c "경유 필수\|방식 사용" patches/gsd-phase-researcher.patch.md</automated>
  </verify>
  <acceptance_criteria>
    - patches/gsd-phase-researcher.patch.md에 "guardrails.md" 문자열이 4회 이상 존재 (파일 정의 1 + 예시 2 + 마이그레이션 1)
    - "관찰" 문자열이 4회 이상 존재 (형식 정의 1 + 예시1 관찰 1 + 예시2 관찰 1 + 관찰-이유-대신 구조 언급)
    - "대안이 하나로 확정" 문자열이 최소 1회 존재 (분류 기준)
    - "마이그레이션 완료 후" 문자열이 1회 존재 (마이그레이션 절차)
    - "경유 필수" 문자열이 1회 이상 존재 (guardrails 예시)
    - "방식 사용" 문자열이 1회 이상 존재 (guardrails 예시)
    - "decisions.md" 줄은 변경 없이 기존 그대로 유지
    - "troubleshooting.md" 줄은 변경 없이 기존 그대로 유지
    - "index.md" 줄은 변경 없이 기존 그대로 유지
    - Line 22 이후의 5번 항목과 During research 섹션은 변경 없음
  </acceptance_criteria>
  <done>researcher 패치 Step 0의 4번 항목이 guardrails.md 생성 지시 + anti-patterns.md 형식 재정의 + 분류 기준 + 마이그레이션 절차를 포함하며, 인라인 예시가 knowledge-compiler 도메인 소재로 작성되어 있다</done>
</task>

<task type="auto">
  <name>Task 2: verifier 패치 파일에 동일한 형식 지시 블록 적용</name>
  <files>patches/gsd-verifier.patch.md</files>
  <read_first>
    - patches/gsd-verifier.patch.md (수정 대상 — 현재 상태 확인)
    - patches/gsd-phase-researcher.patch.md (Task 1 결과 — 형식 지시 블록 원본, D-08에 따라 동일하게 복사)
  </read_first>
  <action>
`patches/gsd-verifier.patch.md`의 Step 10b, 3번 항목(Line 16~20)을 아래 내용으로 교체한다.

**교체 전 (Line 16~20):**
```
3. Full reconcile — reprocess ALL raw entries and rebuild knowledge/:
   - `decisions.md` — 모든 시도/결정 통합, 상태 태그 정합성 확인
   - `anti-patterns.md` — 새로 발견된 anti-pattern 추가
   - `troubleshooting.md` — 에러/해결 매핑 갱신
   - `index.md` — 전체 요약 재생성
```

**교체 후:**
```
3. Full reconcile — reprocess ALL raw entries and rebuild knowledge/:
   - `decisions.md` — 모든 시도/결정 통합, 상태 태그 정합성 확인
   - `guardrails.md` — 대안이 하나로 확정되는 케이스를 긍정형 액션으로 기술한다
     - 형식: "## [주제]\n[긍정형 액션]" (예: "~경유 필수", "~방식 사용")
     - 예시 (절대적 케이스): "## raw/ 읽기\nraw/ 읽기는 knowledge/index.md 경유 필수"
     - 예시 (대안 확정 케이스): "## decisions.md 병합\ndecisions.md는 decisions/ 하위 파일 병합 방식 사용"
   - `anti-patterns.md` — 상황에 따라 달라질 수 있는 맥락 의존적 케이스를 관찰-이유-대신 구조로 기술한다
     - 형식: "## [주제]\n**관찰:** [현상]\n**이유:** [문제 원인]\n**대신:** [권장 접근]"
     - 예시 1: "## 컴파일러 부정형 지시\n**관찰:** knowledge 파일 생성 지시에 '하지 마라' 형식 사용\n**이유:** LLM은 부정형 지시 준수율이 낮아 원치 않는 패턴이 잔존\n**대신:** 긍정형 액션으로 재기술 (Phase 1 전환 원칙 적용)"
     - 예시 2: "## raw/ 파일 직접 쿼리\n**관찰:** index.md를 거치지 않고 raw/*.md를 직접 읽어 분석\n**이유:** 컨텍스트 비용이 높고 관련 없는 정보까지 로드됨\n**대신:** index.md에서 관련 날짜 파악 후 해당 raw 파일만 선택적으로 읽기"
   - `troubleshooting.md` — 에러/해결 매핑 갱신
   - `index.md` — 전체 요약 재생성
   
   분류 기준: 대안이 하나로 확정되는가?
   - YES → `guardrails.md`에 긍정형 액션으로 기술
   - NO (상황에 따라 달라짐) → `anti-patterns.md`에 관찰-이유-대신 구조로 기술
   
   기존 `anti-patterns.md`가 존재하는 경우:
   - 모든 항목을 읽어 위 분류 기준 적용
     - 대안이 하나로 확정 → `guardrails.md`에 긍정형 액션으로 재기술
     - 상황에 따라 달라짐 → `anti-patterns.md`에 관찰-이유-대신 구조로 재형식화
   - 마이그레이션 완료 후 `anti-patterns.md`를 새 형식으로 덮어쓴다
```

핵심 (per D-08): guardrails.md 정의, anti-patterns.md 정의, 분류 기준, 마이그레이션 절차의 텍스트가 Task 1에서 작성한 researcher 패치의 형식 지시 블록과 **글자 그대로 동일**해야 한다. 다른 부분은 3번 항목의 첫째 줄(`Full reconcile...`)과 decisions.md/troubleshooting.md/index.md의 설명문뿐이다 (이들은 verifier 고유 표현 유지).

verifier 패치의 Line 21 이후 (`4. 충돌하는 [active] decision이...`)는 변경하지 않는다.

검증: Task 1 완료 후 두 패치 파일의 guardrails.md~마이그레이션 블록을 diff로 비교하여 동일성을 확인한다.
  </action>
  <verify>
    <automated>grep -c "guardrails.md" patches/gsd-verifier.patch.md && grep -c "관찰.*이유.*대신\|관찰-이유-대신" patches/gsd-verifier.patch.md && grep -c "대안이 하나로 확정" patches/gsd-verifier.patch.md && grep -c "마이그레이션 완료 후" patches/gsd-verifier.patch.md && diff <(sed -n '/guardrails\.md/,/마이그레이션 완료 후.*덮어쓴다/p' patches/gsd-phase-researcher.patch.md) <(sed -n '/guardrails\.md/,/마이그레이션 완료 후.*덮어쓴다/p' patches/gsd-verifier.patch.md)</automated>
  </verify>
  <acceptance_criteria>
    - patches/gsd-verifier.patch.md에 "guardrails.md" 문자열이 4회 이상 존재
    - "관찰" 문자열이 4회 이상 존재
    - "대안이 하나로 확정" 문자열이 최소 1회 존재
    - "마이그레이션 완료 후" 문자열이 1회 존재
    - "경유 필수" 문자열이 1회 이상 존재
    - "방식 사용" 문자열이 1회 이상 존재
    - diff 명령어의 출력이 빈 문자열 (두 패치의 guardrails.md~마이그레이션 블록이 동일)
    - "decisions.md" 줄은 verifier 고유 표현("모든 시도/결정 통합, 상태 태그 정합성 확인") 유지
    - Line 21 이후의 4번 항목("충돌하는 [active] decision")은 변경 없음
  </acceptance_criteria>
  <done>verifier 패치 Step 10b의 3번 항목이 researcher 패치와 동일한 guardrails.md/anti-patterns.md 형식 지시 블록을 포함하며, verifier 고유 표현(decisions/troubleshooting/index 설명)은 유지되어 있다</done>
</task>

</tasks>

<verification>
Phase 2 전체 검증:

1. **COMPILE-03 (guardrails.md 도입 + 마이그레이션):**
   ```bash
   grep -c "guardrails.md" patches/gsd-phase-researcher.patch.md patches/gsd-verifier.patch.md
   grep "마이그레이션 완료 후" patches/gsd-phase-researcher.patch.md patches/gsd-verifier.patch.md
   ```
   두 패치 모두 guardrails.md 언급 4회+, 마이그레이션 절차 포함

2. **COMPILE-04 (guardrails.md 긍정형 형식):**
   ```bash
   grep "경유 필수\|방식 사용" patches/gsd-phase-researcher.patch.md patches/gsd-verifier.patch.md
   ```
   두 패치 모두 긍정형 액션 예시 포함

3. **COMPILE-05 (anti-patterns.md 관찰-이유-대신 형식):**
   ```bash
   grep "관찰.*이유.*대신\|관찰-이유-대신" patches/gsd-phase-researcher.patch.md patches/gsd-verifier.patch.md
   ```
   두 패치 모두 관찰-이유-대신 구조 언급

4. **D-08 동일성 (두 패치의 형식 지시 블록 일치):**
   ```bash
   diff <(sed -n '/guardrails\.md/,/마이그레이션 완료 후.*덮어쓴다/p' patches/gsd-phase-researcher.patch.md) <(sed -n '/guardrails\.md/,/마이그레이션 완료 후.*덮어쓴다/p' patches/gsd-verifier.patch.md)
   ```
   출력 없음 = 동일

5. **역할 경계 (분류 기준 명시):**
   ```bash
   grep "대안이 하나로 확정" patches/gsd-phase-researcher.patch.md patches/gsd-verifier.patch.md
   ```
   두 패치 모두 분류 기준 포함
</verification>

<success_criteria>
- 두 패치 파일에 guardrails.md 생성 지시가 존재한다
- guardrails.md 형식이 긍정형 액션 + 인라인 예시 2개로 규정되어 있다
- anti-patterns.md 형식이 관찰-이유-대신 구조 + 인라인 예시 2개로 규정되어 있다
- 분류 기준("대안이 하나로 확정되는가?")이 두 패치에 명시되어 있다
- 기존 anti-patterns.md 마이그레이션 절차가 두 패치에 명시되어 있다
- 두 패치의 형식 지시 블록이 동일하다 (D-08)
- decisions.md, troubleshooting.md, index.md 지시는 기존 그대로 유지된다
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-format-system/02-01-SUMMARY.md`
</output>
