---
phase: 01-compiler-prompt-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - patches/gsd-phase-researcher.patch.md
  - patches/gsd-verifier.patch.md
autonomous: true
requirements:
  - COMPILE-01
  - COMPILE-02

must_haves:
  truths:
    - "researcher 패치 Step 0의 컴파일러 프롬프트가 긍정형 지시만 포함한다"
    - "verifier 패치 Step 10b의 컴파일러 프롬프트가 긍정형 지시만 포함한다"
    - "knowledge/ 파일 4개(index.md, decisions.md, anti-patterns.md, troubleshooting.md) 구조가 유지된다"
  artifacts:
    - path: "patches/gsd-phase-researcher.patch.md"
      provides: "Step 0 Knowledge Compile — 긍정형 지시"
      contains: "확인하고"
    - path: "patches/gsd-verifier.patch.md"
      provides: "Step 10b Knowledge Reconcile"
      contains: "Step 10b"
  key_links:
    - from: "patches/gsd-phase-researcher.patch.md"
      to: ".knowledge/knowledge/"
      via: "knowledge 파일 4개 참조"
      pattern: "decisions\\.md|anti-patterns\\.md|troubleshooting\\.md|index\\.md"
    - from: "patches/gsd-verifier.patch.md"
      to: ".knowledge/knowledge/"
      via: "knowledge 파일 4개 참조"
      pattern: "decisions\\.md|anti-patterns\\.md|troubleshooting\\.md|index\\.md"
---

<objective>
researcher와 verifier 패치 파일의 컴파일러 프롬프트를 부정형에서 긍정형 지시로 전환한다.

Purpose: LLM은 부정형 지시("하지 마라")보다 긍정형 지시("~하라")의 준수율이 높다 (Pink Elephant Problem). 컴파일러 프롬프트를 긍정형으로 전환해 knowledge 파일 생성 품질을 높인다.
Output: 두 패치 파일이 긍정형 지시만 포함하며, knowledge 파일 4개 구조는 그대로 유지된다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-compiler-prompt-refactor/01-RESEARCH.md
@.planning/phases/01-compiler-prompt-refactor/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: researcher 패치 Step 0 긍정형 전환 (COMPILE-01)</name>
  <files>patches/gsd-phase-researcher.patch.md</files>
  <read_first>
    - patches/gsd-phase-researcher.patch.md (수정 대상 — 현재 상태 확인 필수)
    - .planning/phases/01-compiler-prompt-refactor/01-RESEARCH.md (전환 예시 및 부정형 위치 참조)
  </read_first>
  <action>
patches/gsd-phase-researcher.patch.md의 Step 0에서 다음 4개 라인을 긍정형으로 교체한다.

**라인 19 (anti-patterns.md 설명):**
현재: `- \`anti-patterns.md\` — "이것은 하지 마라" 목록과 이유`
변경: `- \`anti-patterns.md\` — 피해야 할 패턴과 그 이유 목록`

**라인 22 (merge 지시):**
현재: `5. Merge with existing knowledge (don't overwrite, append/update)`
변경: `5. 기존 knowledge 파일을 읽은 뒤 새 항목을 추가하거나 기존 항목을 업데이트하여 병합하라`

**라인 25 (decisions.md 지시):**
현재: `- \`decisions.md\` — \`[rejected]\` 항목은 같은 접근 시도 금지`
변경: `- \`decisions.md\` — \`[rejected]\` 항목을 확인하고 대안 접근법을 선택하라`

**라인 26 (anti-patterns.md 지시):**
현재: `- \`anti-patterns.md\` — 연구 추천에서 제외할 패턴`
변경: `- \`anti-patterns.md\` — 목록을 확인하고 연구 추천에서 검증된 패턴을 우선 적용하라`

**라인 27 (troubleshooting.md 지시):**
현재: `- \`troubleshooting.md\` — 이미 해결된 문제는 재조사 불필요`
변경: `- \`troubleshooting.md\` — 이미 해결된 문제를 확인하고 새로운 미해결 문제에 집중하라`

knowledge 파일 4개(decisions.md, anti-patterns.md, troubleshooting.md, index.md)의 이름과 역할은 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -c "하지 마라\|금지\|불필요\|제외할\|don't overwrite" patches/gsd-phase-researcher.patch.md | grep -q "^0$" && echo "PASS: no negative patterns" || echo "FAIL: negative patterns remain"</automated>
  </verify>
  <acceptance_criteria>
    - patches/gsd-phase-researcher.patch.md에 "하지 마라" 문자열이 0회 등장
    - patches/gsd-phase-researcher.patch.md에 "금지" 문자열이 0회 등장
    - patches/gsd-phase-researcher.patch.md에 "불필요" 문자열이 0회 등장
    - patches/gsd-phase-researcher.patch.md에 "제외할" 문자열이 0회 등장
    - patches/gsd-phase-researcher.patch.md에 "don't overwrite" 문자열이 0회 등장
    - patches/gsd-phase-researcher.patch.md에 "확인하고" 문자열이 최소 2회 등장 (긍정형 전환 확인)
    - patches/gsd-phase-researcher.patch.md에 "decisions.md", "anti-patterns.md", "troubleshooting.md", "index.md" 4개 파일명 모두 존재
  </acceptance_criteria>
  <done>researcher 패치 Step 0의 모든 부정형 지시가 긍정형으로 전환되고, knowledge 파일 4개 구조 참조가 유지된다</done>
</task>

<task type="auto">
  <name>Task 2: verifier 패치 Step 10b 검증 및 최소 보정 (COMPILE-02)</name>
  <files>patches/gsd-verifier.patch.md</files>
  <read_first>
    - patches/gsd-verifier.patch.md (수정 대상 — 현재 상태 확인 필수)
    - .planning/phases/01-compiler-prompt-refactor/01-RESEARCH.md (verifier 분석 결과 참조)
  </read_first>
  <action>
patches/gsd-verifier.patch.md의 Step 10b를 읽고 부정형 패턴 존재 여부를 확인한다.

RESEARCH.md 분석 결과: Step 10b에는 명시적 부정형("하지 마라", "금지", "불필요", "제외할")이 없다. "충돌하는 [active] decision이 있으면 [uncertain]으로 표시"는 긍정형 행동 지시이므로 변경 불필요.

**수행할 작업:**
1. `grep -c "하지 마라\|금지\|불필요\|제외할" patches/gsd-verifier.patch.md`를 실행하여 부정형 패턴이 0개임을 확인한다
2. 만약 grep 결과가 0이면 — 파일 변경 없이 검증만 완료한다 (변경 없음이 올바른 결정)
3. 만약 grep 결과가 1 이상이면 — 해당 라인을 researcher 패치와 동일한 긍정형 전환 원칙(Pink Elephant: "금지" 대신 "대안 행동" 기술)으로 전환한다
4. knowledge 파일 4개(decisions.md, anti-patterns.md, troubleshooting.md, index.md) 참조가 모두 존재하는지 확인한다
  </action>
  <verify>
    <automated>grep -c "하지 마라\|금지\|불필요\|제외할" patches/gsd-verifier.patch.md | grep -q "^0$" && echo "PASS: no negative patterns" || echo "FAIL: negative patterns remain"</automated>
  </verify>
  <acceptance_criteria>
    - patches/gsd-verifier.patch.md에 "하지 마라" 문자열이 0회 등장
    - patches/gsd-verifier.patch.md에 "금지" 문자열이 0회 등장
    - patches/gsd-verifier.patch.md에 "불필요" 문자열이 0회 등장
    - patches/gsd-verifier.patch.md에 "제외할" 문자열이 0회 등장
    - patches/gsd-verifier.patch.md에 "decisions.md", "anti-patterns.md", "troubleshooting.md", "index.md" 4개 파일명 모두 존재
  </acceptance_criteria>
  <done>verifier 패치 Step 10b가 부정형 패턴 없이 긍정형 지시만 포함하고, knowledge 파일 4개 구조 참조가 유지된다</done>
</task>

</tasks>

<verification>
Phase 1 전체 검증:

1. 부정형 패턴 부재 확인:
```bash
grep -n "하지 마라\|금지\|불필요\|제외할\|don't overwrite" patches/gsd-phase-researcher.patch.md patches/gsd-verifier.patch.md
# 기대: 매칭 없음 (exit code 1)
```

2. knowledge 파일 구조 유지 확인:
```bash
grep -c "decisions\.md\|anti-patterns\.md\|troubleshooting\.md\|index\.md" patches/gsd-phase-researcher.patch.md
# 기대: 4 이상

grep -c "decisions\.md\|anti-patterns\.md\|troubleshooting\.md\|index\.md" patches/gsd-verifier.patch.md
# 기대: 4 이상
```

3. 긍정형 전환 확인 (researcher):
```bash
grep -c "확인하고\|집중하라\|우선 적용하라\|선택하라" patches/gsd-phase-researcher.patch.md
# 기대: 3 이상
```
</verification>

<success_criteria>
- 두 패치 파일 모두에서 "하지 마라", "금지", "불필요", "제외할", "don't overwrite" 패턴이 0회 등장
- researcher 패치에 긍정형 지시 표현("확인하고", "집중하라", "선택하라" 등)이 포함됨
- 두 패치 파일 모두 knowledge 파일 4개(index.md, decisions.md, anti-patterns.md, troubleshooting.md)를 참조
</success_criteria>

<output>
After completion, create `.planning/phases/01-compiler-prompt-refactor/01-01-SUMMARY.md`
</output>
