---
phase: 05-gsd-workflow-stages
plan: 01
type: execute
wave_count: 2
status: planned
depends_on: []
files_modified:
  - patches/gsd-phase-researcher.patch.md
  - patches/gsd-planner.patch.md
  - patches/gsd-discuss-phase.patch.md
  - skills/gsd-clear/skill.md
  - skills/gsd-knowledge-compile/skill.md
  - install.sh
autonomous: true
requirements:
  - WORKFLOW-01
  - WORKFLOW-02
  - WORKFLOW-03
  - WORKFLOW-04
  - WORKFLOW-05
must_haves:
  truths:
    - "researcher Step 0 compile 블록이 패치 파일에서 제거되고 Step 3 lookup만 남는다"
    - "planner 패치에 manifest 조건부 fallback compile 블록이 추가된다"
    - "discuss-phase에 knowledge lookup이 추가된다 (install.sh 통해 배포)"
    - "/gsd-clear 스킬이 생성된다 (compile + /clear)"
    - "/gsd-knowledge-compile 스킬이 생성된다 (compile만, 컨텍스트 유지)"
    - "install.sh가 모든 변경사항을 배포한다"
  artifacts:
    - path: "patches/gsd-phase-researcher.patch.md"
      provides: "Step 0 제거된 researcher 패치 (Step 3 lookup만)"
    - path: "patches/gsd-planner.patch.md"
      provides: "fallback compile + knowledge lookup 패치"
    - path: "patches/gsd-discuss-phase.patch.md"
      provides: "discuss-phase knowledge lookup 패치"
    - path: "skills/gsd-clear/skill.md"
      provides: "/gsd-clear 스킬"
    - path: "skills/gsd-knowledge-compile/skill.md"
      provides: "/gsd-knowledge-compile 스킬"
    - path: "install.sh"
      provides: "모든 패치 및 스킬 배포 로직"
---

<objective>
GSD 워크플로 단계별 knowledge 활동(수집/컴파일/조회)을 구현한다.

- researcher Step 0 compile 제거 (D-01)
- planner fallback compile 추가 (D-03)
- discuss-phase knowledge lookup 추가 (D-08)
- /gsd-clear 스킬 생성 (D-02)
- /gsd-knowledge-compile 스킬 생성 (D-14)
- install.sh 업데이트로 전부 배포 (D-15)

spec 문서는 생성하지 않는다 (D-13 수정).
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/05-gsd-workflow-stages/05-CONTEXT.md
@.planning/phases/05-gsd-workflow-stages/05-RESEARCH.md
</context>

<tasks>

<!-- ═══════════════════════════════════════════════ WAVE 1 (parallel) -->

<task id="05-01-01" wave="1" type="auto">
  <name>researcher 패치: Step 0 compile 제거, Step 3 lookup 유지</name>
  <files>patches/gsd-phase-researcher.patch.md</files>
  <action>
`patches/gsd-phase-researcher.patch.md`를 수정한다.

**제거할 것:** `## Step 0: Knowledge Compile (before research)` 블록 전체
- 파일의 1~91번째 줄 (Step 0 전체 — "Step 0 completion gate" 마지막 문장까지)
- D-01: researcher는 compile 없이 리서치만 수행

**유지할 것:** Step 3 lookup 지시 (현재 파일 92~98번째 줄)
- `**During research (Step 3):** knowledge/ lookup order:` 단락
- `decisions.md`, `anti-patterns.md`, `troubleshooting.md` 조회 순서
- `**Lookup order:** Read index.md first ...` 문장

**수정 후 파일 구조:**
```
<!-- PATCH:knowledge-compiler for gsd-phase-researcher.md -->
<!-- Insert BEFORE: "## Step 1: Receive Scope and Load Context" -->

<!-- PATCH:knowledge-compiler — reapply after GSD updates -->

**During research (Step 3):** knowledge/ lookup order:
- `decisions.md` — Check [rejected] entries and choose an alternative approach
- `anti-patterns.md` — Review the list and prioritize proven patterns in research recommendations
- `troubleshooting.md` — Check already-solved problems and focus on new unresolved issues

**Lookup order:** Read index.md first to identify files relevant to the current phase, then selectively Read only those files.
```

헤더 주석 2줄(Insert BEFORE 지시)은 반드시 유지한다 — install.sh가 anchor로 사용한다.
  </action>
  <verify>
    <automated>bash -c "! grep -q 'Step 0: Knowledge Compile' patches/gsd-phase-researcher.patch.md && grep -q 'During research (Step 3)' patches/gsd-phase-researcher.patch.md && echo OK"</automated>
  </verify>
  <done>
- patches/gsd-phase-researcher.patch.md에 Step 0 블록 없음
- Step 3 lookup 지시 존재
- 헤더 주석 2줄 유지
  </done>
</task>

<task id="05-01-02" wave="1" type="auto">
  <name>planner 패치: fallback compile 블록 추가</name>
  <files>patches/gsd-planner.patch.md</files>
  <action>
`patches/gsd-planner.patch.md`에 기존 knowledge lookup 블록 **앞**에 fallback compile 블록을 추가한다.

**추가할 내용** (기존 `**Project knowledge:**` 줄 바로 앞에 삽입):

```markdown
**Knowledge compile (fallback):** `/gsd-clear`를 실행하지 않고 세션을 종료한 경우를 대비해 compile을 수행한다.

<!-- PATCH:knowledge-compiler — reapply after GSD updates -->

**Skip conditions (모두 해당하면 건너뜀):**
- `.knowledge/raw/` 디렉토리가 없거나 비어 있음
- `.planning/compile-manifest.json`이 존재하고 변경된 파일이 없음

**Fallback compile 절차:**

1. `.knowledge/raw/`가 없으면 compile 건너뜀 → 기존 knowledge lookup으로 진행.
2. `.planning/compile-manifest.json` 로드.
   - 없으면: full compile 모드 (모든 raw/ + .planning/** 처리).
3. 변경된 소스 파일 스캔:
   - `raw/*.md` — 파일의 mtime이 manifest의 `last_compiled`보다 최신인 것
   - `.planning/**/*.md` — manifest에 기록된 mtime과 현재 mtime이 다른 것
   - **제외:** `.planning/compile-manifest.json` 자체 (자기 참조 방지)
4. 변경된 파일이 없으면 compile 건너뜀 → 기존 knowledge lookup으로 진행.
5. 변경된 파일에서 knowledge 추출 → `.knowledge/knowledge/`에 병합 (B+C fusion 정책 적용).
   - 포함: 미래 세션의 실수를 방지하는 결정/발견, 시스템 동작 구조 지식, 반복 오류와 해법, 중요 완료 이벤트
   - 건너뜀: 일회성 검증 작업, 결과 없는 탐색, 이미 존재하는 중복
6. `.planning/compile-manifest.json` 업데이트 (각 파일의 현재 mtime 기록, `last_compiled` 갱신).
7. `.knowledge/knowledge/index.md`의 `Last compiled` 날짜 업데이트.
```

그 다음에 기존 `**Project knowledge:**` 블록이 이어진다 (수정하지 않음).

**수정 후 전체 파일 구조:**
1. 헤더 주석 3줄 (PATCH marker + Insert INSIDE 지시 + 빈 줄)
2. `**Knowledge compile (fallback):**` 블록 (신규)
3. 빈 줄
4. `**Project knowledge:**` 블록 (기존 — 그대로 유지)
  </action>
  <verify>
    <automated>bash -c "grep -q 'Knowledge compile (fallback)' patches/gsd-planner.patch.md && grep -q 'compile-manifest.json' patches/gsd-planner.patch.md && grep -q 'Project knowledge' patches/gsd-planner.patch.md && echo OK"</automated>
  </verify>
  <done>
- patches/gsd-planner.patch.md에 fallback compile 블록 존재
- compile-manifest.json 참조 있음
- 기존 Project knowledge lookup 블록 유지
  </done>
</task>

<task id="05-01-03" wave="1" type="auto">
  <name>/gsd-clear 스킬 생성</name>
  <files>skills/gsd-clear/skill.md</files>
  <action>
`skills/gsd-clear/` 디렉토리를 생성하고 `skill.md`를 작성한다.

기존 스킬 파일 형식을 참고하기 위해 `skills/` 디렉토리의 다른 스킬 파일을 먼저 확인한다.
확인 후, 동일한 형식으로 아래 내용의 스킬을 작성한다.

**스킬 목적:** 세션 종료 시 knowledge compile을 실행한 뒤 Claude `/clear`로 컨텍스트를 초기화한다.

**스킬 동작 (순서대로):**

1. `.knowledge/raw/`가 없으면 "No .knowledge/raw/ found — skipping compile" 출력 후 7번으로 이동.
2. `.planning/compile-manifest.json` 로드. 없으면 full compile 모드.
3. 변경된 소스 파일 스캔:
   - `raw/*.md` — mtime이 `last_compiled`보다 최신인 파일
   - `.planning/**/*.md` — manifest mtime과 현재 mtime이 다른 파일
   - **제외:** `.planning/compile-manifest.json` 자체
4. 변경된 파일이 없으면 "Knowledge is up to date — no changes since last compile" 출력 후 7번으로 이동.
5. 변경된 파일에서 knowledge 추출 → `.knowledge/knowledge/`에 병합.
   - decisions.md, guardrails.md, anti-patterns.md, troubleshooting.md, index.md 업데이트
   - B+C fusion: 동일 결론 → Observed 카운트 증가, 반대 결론 → [conflict] 태그 + 새 내용 blockquote
6. `.planning/compile-manifest.json`과 `index.md`의 `Last compiled` 업데이트.
7. `/clear` 실행.

**트리거:** 사용자가 세션 종료 전 `/gsd-clear` 실행.
  </action>
  <verify>
    <automated>bash -c "test -f skills/gsd-clear/skill.md && grep -qi 'compile' skills/gsd-clear/skill.md && grep -qi 'clear' skills/gsd-clear/skill.md && echo OK"</automated>
  </verify>
  <done>
- skills/gsd-clear/skill.md 존재
- compile 로직 포함
- /clear 실행 포함
  </done>
</task>

<task id="05-01-04" wave="1" type="auto">
  <name>/gsd-knowledge-compile 스킬 생성</name>
  <files>skills/gsd-knowledge-compile/skill.md</files>
  <action>
`skills/gsd-knowledge-compile/` 디렉토리를 생성하고 `skill.md`를 작성한다.

기존 스킬 파일 형식을 참고하기 위해 `skills/` 디렉토리의 다른 스킬 파일을 먼저 확인한다.
확인 후, 동일한 형식으로 아래 내용의 스킬을 작성한다.

**스킬 목적:** 세션 중 언제든 on-demand로 knowledge compile을 실행한다. 컨텍스트는 유지된다.

**스킬 동작 (/gsd-clear와 동일하되 마지막 단계만 다름):**

1. `.knowledge/raw/`가 없으면 "No .knowledge/raw/ found — nothing to compile" 출력 후 종료.
2. `.planning/compile-manifest.json` 로드. 없으면 full compile 모드.
3. 변경된 소스 파일 스캔:
   - `raw/*.md` — mtime이 `last_compiled`보다 최신인 파일
   - `.planning/**/*.md` — manifest mtime과 현재 mtime이 다른 파일
   - **제외:** `.planning/compile-manifest.json` 자체
4. 변경된 파일이 없으면 "Knowledge is up to date — no changes since last compile" 출력 후 종료.
5. 변경된 파일에서 knowledge 추출 → `.knowledge/knowledge/`에 병합.
   - B+C fusion 동일하게 적용
6. `.planning/compile-manifest.json`과 `index.md`의 `Last compiled` 업데이트.
7. **/clear 실행하지 않음.** 대신 compile 결과 요약 출력:
   - 처리된 파일 수
   - 추가/업데이트된 knowledge 항목 수
   - 수정된 knowledge 파일 목록

**트리거:** 사용자가 세션 중 `/gsd-knowledge-compile` 실행.
  </action>
  <verify>
    <automated>bash -c "test -f skills/gsd-knowledge-compile/skill.md && grep -qi 'compile' skills/gsd-knowledge-compile/skill.md && ! grep -qi '^/clear' skills/gsd-knowledge-compile/skill.md && echo OK"</automated>
  </verify>
  <done>
- skills/gsd-knowledge-compile/skill.md 존재
- compile 로직 포함
- /clear 없음 (컨텍스트 유지)
- 결과 요약 출력 포함
  </done>
</task>

<!-- ═══════════════════════════════════════════════ WAVE 2 -->

<task id="05-01-05" wave="2" type="auto">
  <name>discuss-phase 패치 생성 + install.sh 업데이트</name>
  <files>patches/gsd-discuss-phase.patch.md, install.sh</files>
  <action>
**Step A: patches/gsd-discuss-phase.patch.md 생성**

discuss-phase workflow 파일에 knowledge lookup을 추가하는 패치 파일을 작성한다.

먼저 discuss-phase.md 워크플로를 확인한다:
```bash
cat "$HOME/.claude/get-shit-done/workflows/discuss-phase.md" | head -100
```

`load_prior_context` 스텝 또는 PROJECT.md/STATE.md를 로드하는 위치의 anchor 문자열을 찾는다.

그 위치를 anchor로 사용하는 패치 파일을 작성한다:

```markdown
<!-- PATCH:knowledge-compiler for discuss-phase.md -->
<!-- Insert BEFORE: "{anchor 문자열}" -->

<!-- PATCH:knowledge-compiler — reapply after GSD updates -->

**Project knowledge:** discuss 시작 전 이전 세션의 결정사항을 확인한다.

**Skip condition:** `.knowledge/knowledge/`가 없거나 `index.md`가 없으면 건너뜀.

**If knowledge/ exists:**

1. `.knowledge/knowledge/index.md` 읽기 — Quick Reference 테이블과 키워드 인덱스 확인.
2. 현재 Phase 주제와 관련 있는 knowledge 파일 식별.
3. 관련 파일만 선택적으로 읽기:
   - `decisions.md` — `[rejected]` 항목 확인: 이미 거부된 방향을 재질문하거나 재제안하지 않음
   - `guardrails.md` — 하드 제약 확인: discuss 중 이 제약을 위반하는 선택지를 제시하지 않음
4. compile은 수행하지 않음 — discuss는 조회만.

**Lookup order:** index.md를 먼저 읽고, Quick Reference에서 관련 파일만 선택적으로 로드한다.
```

**Step B: install.sh 업데이트**

`install.sh`에 다음 3가지를 추가한다:

**1. patch_workflow 함수 추가** (patch_agent 함수 아래에):
```bash
# --- Workflow patching ---
patch_workflow() {
    local workflow_file="$1"
    local patch_file="$2"
    local anchor="$3"
    local workflow_name
    workflow_name="$(basename "$workflow_file")"

    if [ ! -f "$workflow_file" ]; then
        warn "$workflow_name not found at $workflow_file — skipping"
        return 0
    fi

    if grep -q "$PATCH_MARKER" "$workflow_file" 2>/dev/null; then
        if [ "$FORCE" = true ]; then
            # Remove existing patch block
            local tmp_file
            tmp_file="$(mktemp)"
            awk -v marker="$PATCH_MARKER" '
                $0 ~ marker { found=1; next }
                found && /^## / { found=0 }
                !found { print }
            ' "$workflow_file" > "$tmp_file"
            mv "$tmp_file" "$workflow_file"
            warn "$workflow_name existing patch removed (--force)"
        else
            warn "$workflow_name already patched — skipping (use --force to re-apply)"
            return 0
        fi
    fi

    local patch_content
    patch_content="$(tail -n +3 "$patch_file")"

    local tmp_file
    tmp_file="$(mktemp)"
    awk -v anchor="$anchor" -v patch="$patch_content" '
        $0 ~ anchor { print patch; print ""; }
        { print }
    ' "$workflow_file" > "$tmp_file"
    mv "$tmp_file" "$workflow_file"
    info "$workflow_name patched"
}
```

**2. discuss-phase 패치 호출 추가** (section 2 워크플로 패치 블록 안에):
```bash
# discuss-phase.md: knowledge lookup before load_prior_context
patch_workflow \
    "$WORKFLOWS_DIR/discuss-phase.md" \
    "$SCRIPT_DIR/patches/gsd-discuss-phase.patch.md" \
    "{Step A에서 찾은 anchor 문자열}"
```

**3. skills 설치 로직 추가** (section 3 CLAUDE.md 설치 이후에 새 section으로):
```bash
# --- 4. Install GSD skills ---
echo "--- Installing GSD skills ---"

SKILLS_DIR="$HOME/.claude/skills"
mkdir -p "$SKILLS_DIR"

install_skill() {
    local skill_name="$1"
    local skill_src="$SCRIPT_DIR/skills/$skill_name/skill.md"
    local skill_dst="$SKILLS_DIR/$skill_name/skill.md"

    if [ ! -f "$skill_src" ]; then
        warn "Skill $skill_name not found at $skill_src — skipping"
        return 0
    fi

    mkdir -p "$SKILLS_DIR/$skill_name"
    if [ -f "$skill_dst" ] && [ "$FORCE" = false ]; then
        warn "$skill_name already installed — skipping (use --force to re-install)"
    else
        cp "$skill_src" "$skill_dst"
        info "$skill_name installed"
    fi
}

install_skill "gsd-clear"
install_skill "gsd-knowledge-compile"
echo ""
```

**4. Done 메시지 업데이트** — 설치 완료 출력에 새 항목 추가:
```
  [global] GSD skills (gsd-clear, gsd-knowledge-compile)
  [global] discuss-phase knowledge lookup
```

기존 project setup section 번호를 4 → 5로 변경한다.
  </action>
  <verify>
    <automated>bash -c "test -f patches/gsd-discuss-phase.patch.md && grep -q 'PATCH:knowledge-compiler' patches/gsd-discuss-phase.patch.md && grep -q 'patch_workflow' install.sh && grep -q 'install_skill' install.sh && grep -q 'gsd-clear' install.sh && grep -q 'gsd-knowledge-compile' install.sh && echo OK"</automated>
  </verify>
  <done>
- patches/gsd-discuss-phase.patch.md 존재, PATCH marker 포함
- install.sh에 patch_workflow 함수 존재
- install.sh에 discuss-phase 패치 호출 존재
- install.sh에 install_skill 함수 및 gsd-clear, gsd-knowledge-compile 설치 로직 존재
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 패치 파일 → 설치된 에이전트 | install.sh가 패치를 적용; 패치 내용이 잘못되면 에이전트 전체가 손상될 수 있음 |
| compile-manifest.json → compile 로직 | manifest가 없거나 손상되면 full compile 실행 (graceful degradation) |
| .knowledge/ → 에이전트 | knowledge 파일에 민감한 세션 데이터가 포함될 수 있음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-01 | Tampering | patches/*.patch.md | mitigate | PATCH_MARKER로 중복 적용 방지; --force 명시적 요구 |
| T-05-02 | Tampering | compile-manifest.json | accept | 손상 시 full compile로 fallback — 데이터 손실 없음 |
| T-05-03 | Information Disclosure | .knowledge/knowledge/*.md | accept | 로컬 파일, 사용자가 git 추적 여부 결정 (D-12) |
| T-05-04 | Tampering | discuss-phase.md | mitigate | patch_workflow가 PATCH_MARKER로 중복 적용 방지 |
</threat_model>
