---
phase: 09-install-secure
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - "~/.bashrc"
  - "~/.claude/settings.json"
autonomous: true
requirements: [INSTALL-02, SEC-01, SEC-02]
tags: [rtk, telemetry, bashrc, claude-settings, security, hook]
user_setup: []

must_haves:
  truths:
    - "새 셸 세션에서 `RTK_TELEMETRY_DISABLED=1`이 자동 설정된다"
    - "`~/.claude/settings.json`의 PreToolUse 배열에 RTK Bash matcher 훅이 추가되어 있다"
    - "기존 GSD 훅들(Write/Edit matcher, Bash matcher의 gsd-validate-commit.sh)은 그대로 유지된다"
    - "settings.json이 패치 후에도 유효한 JSON이다"
    - "`RTK_TELEMETRY_DISABLED=1 rtk telemetry status` 출력이 'disabled'이다"
  artifacts:
    - path: "~/.bashrc"
      provides: "영구 RTK_TELEMETRY_DISABLED=1 export"
      contains: "export RTK_TELEMETRY_DISABLED=1"
    - path: "~/.claude/settings.json"
      provides: "RTK PreToolUse Bash matcher 훅 등록"
      contains: "rtk-rewrite.sh"
    - path: "~/.claude/settings.json.bak"
      provides: "rtk init -g가 생성한 자동 백업"
      check: "파일 존재"
  key_links:
    - from: "새 bash 프로세스"
      to: "RTK_TELEMETRY_DISABLED env var"
      via: "~/.bashrc source"
      pattern: "export RTK_TELEMETRY_DISABLED=1"
    - from: "Claude Code Bash tool call"
      to: "rtk-rewrite.sh"
      via: "settings.json PreToolUse hook"
      pattern: "rtk-rewrite.sh"
    - from: "rtk 바이너리 실행"
      to: "텔레메트리 차단"
      via: "RTK_TELEMETRY_DISABLED=1 env var"
      pattern: "telemetry status → disabled"
---

<objective>
D-06 실행 순서대로 `RTK_TELEMETRY_DISABLED=1`을 `~/.bashrc`에 영구 등록한 후 `rtk init -g`를 실행하여 Claude Code settings.json에 PreToolUse Bash 훅을 등록한다. 최종적으로 `rtk telemetry status`가 'disabled'를 반환하는지 확인한다.

Purpose: SEC-01(영구 텔레메트리 차단) + INSTALL-02(글로벌 훅 등록) + SEC-02(disabled 상태 확인)을 단일 플랜에서 원자적으로 처리. env var 선설정이 init 대화형 프롬프트의 텔레메트리 동의를 자동 거부하도록 하기 위해 순서 엄수 필수.
Output: 패치된 ~/.bashrc, 패치된 ~/.claude/settings.json, RTK 자동 생성 백업 파일.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-install-secure/09-CONTEXT.md
@.planning/phases/09-install-secure/09-RESEARCH.md
@.planning/phases/09-install-secure/09-PATTERNS.md
@.planning/phases/09-install-secure/09-01-SUMMARY.md

<interfaces>
<!-- 기존 settings.json PreToolUse 배열 구조 (09-PATTERNS.md lines 28-39, 151-159) -->
<!-- RTK hook은 5번째 항목으로 Bash matcher에 추가됨 -->

기존 Bash matcher 훅 (유지):
```json
{
  "matcher": "Bash",
  "hooks": [
    {
      "type": "command",
      "command": "bash /home/ozt88/.claude/hooks/gsd-validate-commit.sh",
      "timeout": 5
    }
  ]
}
```

추가될 RTK 훅 (rtk init -g가 자동 생성):
```json
{
  "matcher": "Bash",
  "hooks": [
    {
      "type": "command",
      "command": "bash ~/.claude/hooks/rtk-rewrite.sh"
    }
  ]
}
```

~/.bashrc 기존 export 패턴 (09-PATTERNS.md lines 68-73):
```bash
export ANTHROPIC_DEFAULT_SONNET_MODEL='claude-sonnet-4-6'
export ANTHROPIC_MODEL='claude-sonnet-4-6'
export CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RTK_TELEMETRY_DISABLED=1을 ~/.bashrc에 영구 등록 및 현재 셸 export</name>
  <files>~/.bashrc</files>
  <read_first>
    - ~/.bashrc (기존 export 패턴 및 중복 등록 방지 확인)
    - .planning/phases/09-install-secure/09-CONTEXT.md (D-03, D-04, D-06)
    - .planning/phases/09-install-secure/09-PATTERNS.md (~/.bashrc 섹션)
  </read_first>
  <action>
  D-06 실행 순서의 1-2단계를 수행한다: 현재 셸에 env var를 export한 뒤, `~/.bashrc`에 동일 export 라인을 append한다.

  정확한 명령:

  1. 중복 등록 사전 확인:
     ```bash
     grep -n 'RTK_TELEMETRY_DISABLED' ~/.bashrc || echo "not registered yet"
     ```
     이미 등록되어 있으면 append 생략 (idempotent).

  2. 현재 셸에 즉시 적용 (다음 Task의 `rtk init -g` 실행 전 필수):
     ```bash
     export RTK_TELEMETRY_DISABLED=1
     ```

  3. `~/.bashrc`에 영구 등록 (미등록일 때만):
     ```bash
     if ! grep -q 'RTK_TELEMETRY_DISABLED=1' ~/.bashrc; then
       echo 'export RTK_TELEMETRY_DISABLED=1' >> ~/.bashrc
     fi
     ```

     추가되는 정확한 라인: `export RTK_TELEMETRY_DISABLED=1`
     (따옴표 없음 — 기존 ANTHROPIC_* export 중 CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1 패턴과 일치)

  4. 영속성 검증 (새 bash 프로세스):
     ```bash
     bash -c 'source ~/.bashrc && echo "RTK_TELEMETRY_DISABLED=$RTK_TELEMETRY_DISABLED"'
     # Expected: RTK_TELEMETRY_DISABLED=1
     ```

  5. 현재 셸 export 상태 검증:
     ```bash
     echo "current shell: RTK_TELEMETRY_DISABLED=$RTK_TELEMETRY_DISABLED"
     # Expected: current shell: RTK_TELEMETRY_DISABLED=1
     ```

  주의:
  - D-04: `/etc/environment`에는 등록하지 않는다 (~/.bashrc만).
  - D-03: env var 설정이 `rtk init -g`의 텔레메트리 프롬프트를 자동 거부하도록 하기 위해 반드시 Task 2 전에 완료되어야 함.
  - A1(RESEARCH.md): env var suppression이 interactive prompt 자체를 완전히 막는지는 소스 레벨로 미확인 — Task 2에서 프롬프트가 표시되면 'n' 입력 폴백.
  </action>
  <verify>
    <automated>grep -c '^export RTK_TELEMETRY_DISABLED=1$' ~/.bashrc | grep -q '^1$' && bash -c 'source ~/.bashrc && [ "$RTK_TELEMETRY_DISABLED" = "1" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export RTK_TELEMETRY_DISABLED=1$' ~/.bashrc` 출력이 정확히 `1` (정확히 한 번 등록, 중복 없음)
    - `bash -c 'source ~/.bashrc && echo $RTK_TELEMETRY_DISABLED'` 출력이 `1`
    - `/etc/environment`에는 RTK 관련 라인이 추가되지 않음 (`grep RTK /etc/environment` 결과 비어있음 또는 종료 코드 1)
    - 현재 셸에서 `echo $RTK_TELEMETRY_DISABLED` 출력이 `1` (Task 2의 rtk init 전제 조건)
  </acceptance_criteria>
  <done>
    `~/.bashrc`에 `export RTK_TELEMETRY_DISABLED=1` 한 줄이 영구 등록되고, 현재 셸과 새 bash 프로세스 모두에서 env var가 `1`로 평가된다. SEC-01 요구사항 충족. Task 2 `rtk init -g` 진행 가능.
  </done>
</task>

<task type="auto">
  <name>Task 2: `rtk init -g`로 PreToolUse Bash 훅 등록 및 settings.json JSON 유효성 검증</name>
  <files>~/.claude/settings.json, ~/.claude/settings.json.bak</files>
  <read_first>
    - ~/.claude/settings.json (기존 PreToolUse 배열 구조 파악 — Write/Edit 훅 3개 + Bash matcher의 gsd-validate-commit.sh 1개)
    - .planning/phases/09-install-secure/09-CONTEXT.md (D-06, D-08, Claude's Discretion jq 확인)
    - .planning/phases/09-install-secure/09-RESEARCH.md (Architecture Patterns settings.json 훅 등록 구조, Common Pitfalls C3)
    - .planning/phases/09-install-secure/09-PATTERNS.md (Shared Patterns 기존 PreToolUse 훅 구조)
  </read_first>
  <action>
  D-06 실행 순서의 3-4단계를 수행한다: Task 1에서 `RTK_TELEMETRY_DISABLED=1`이 current shell에 export된 상태에서 `rtk init -g`를 실행하고, 패치된 settings.json의 JSON 유효성 및 훅 등록 여부를 검증한다. 수동 JSON 편집 금지 — `rtk init -g`에 위임 (Don't Hand-Roll).

  정확한 실행 순서:

  1. 사전 조건 재확인:
     ```bash
     # Task 1에서 설정된 env var 확인
     [ "$RTK_TELEMETRY_DISABLED" = "1" ] || { echo "ABORT: Task 1 선행 필요"; exit 1; }

     # rtk 바이너리 존재 (Plan 01 결과물)
     which rtk || { echo "ABORT: Plan 01 선행 필요"; exit 1; }

     # jq 의존성 확인 (Claude's Discretion)
     jq --version || { echo "ABORT: jq 미설치"; exit 1; }

     # 기존 settings.json 유효성 확인 (패치 전 기준선)
     jq . ~/.claude/settings.json > /dev/null && echo "pre-patch JSON valid"

     # 기존 훅 개수 기록
     BEFORE_HOOKS=$(jq '.hooks.PreToolUse | length' ~/.claude/settings.json)
     echo "PreToolUse hooks before rtk init: $BEFORE_HOOKS"
     ```

  2. `rtk init -g` 실행 (글로벌 훅 등록):
     ```bash
     rtk init -g
     ```

     - 텔레메트리 대화형 프롬프트가 표시되는 경우 (A1 폴백): `n` 입력하여 거부.
     - `--hook-only` 플래그 미사용 (Open Question #1 Recommendation: 기본 실행 허용 — `~/.claude/RTK.md` 생성은 개인 도구 설정이므로 D-08 범위 외).
     - `--auto-patch` 플래그 사용 금지 (개인 개발 환경 권장 방식 — RESEARCH.md).

  3. 패치 후 JSON 유효성 검증 (Pitfall C3 방어 — 필수):
     ```bash
     jq . ~/.claude/settings.json > /dev/null && echo "post-patch JSON valid" || {
       echo "FATAL: settings.json JSON broken — restoring from .bak"
       cp ~/.claude/settings.json.bak ~/.claude/settings.json
       exit 1
     }
     ```

  4. 자동 백업 존재 확인:
     ```bash
     ls -la ~/.claude/settings.json.bak
     # Expected: 파일 존재, 크기 > 0
     ```

  5. RTK Bash matcher 훅 등록 확인 (INSTALL-02):
     ```bash
     # 등록된 Bash matcher 훅 중 rtk를 포함하는 항목 조회
     jq '.hooks.PreToolUse[] | select(.matcher == "Bash") | .hooks[].command' ~/.claude/settings.json | grep -i rtk
     # Expected: "bash ~/.claude/hooks/rtk-rewrite.sh" 유사 문자열
     ```

  6. 기존 GSD 훅 보존 확인 (Pitfall H2 방어):
     ```bash
     # 기존 4개 훅 유지 여부
     jq '.hooks.PreToolUse[] | .hooks[].command' ~/.claude/settings.json | grep -c 'gsd-'
     # Expected: 4 (gsd-prompt-guard.js, gsd-read-guard.js, gsd-workflow-guard.js, gsd-validate-commit.sh)

     # 훅 총 개수 증가 확인
     AFTER_HOOKS=$(jq '.hooks.PreToolUse | length' ~/.claude/settings.json)
     echo "PreToolUse hooks after rtk init: $AFTER_HOOKS (before: $BEFORE_HOOKS)"
     # Expected: AFTER_HOOKS >= BEFORE_HOOKS + 1
     ```

  7. SEC-02 확인 (`rtk telemetry status` → disabled):
     ```bash
     rtk telemetry status
     # Expected: "disabled"
     # D-05: 별도 `rtk telemetry disable` 실행 안함. env var만으로 충분한지 확인.
     ```

     만약 `enabled` 출력 시: A1 가정(env var가 텔레메트리를 완전 차단)이 틀림. 이 경우 SUMMARY에 명시하고 Phase 10 Verify 단계에 경고 전달 (D-05 재검토 필요).

  8. 훅 스크립트 파일 존재 확인:
     ```bash
     ls -la ~/.claude/hooks/rtk-rewrite.sh 2>/dev/null || echo "rtk-rewrite.sh location differs — check command string in settings.json"
     ```
     (RTK가 스크립트를 다른 경로에 설치할 수 있음 — settings.json의 command 문자열이 실제 위치이므로 그 경로로 존재 확인.)

  주의:
  - D-08: 프로젝트 파일 수정 없음. `~/.claude/`는 개인 도구 설정이므로 D-08 범위 외로 취급 (Open Question #1 Recommendation 준수).
  - Pitfall C3: JSON 깨질 시 .bak에서 즉시 복구.
  - Pitfall H2: 기존 gsd-validate-commit.sh Bash 훅과 RTK Bash 훅 공존 — Phase 10에서 실제 git 명령으로 충돌 검증.
  </action>
  <verify>
    <automated>jq . ~/.claude/settings.json > /dev/null && jq -e '[.hooks.PreToolUse[] | select(.matcher == "Bash") | .hooks[].command] | map(select(test("rtk"; "i"))) | length > 0' ~/.claude/settings.json && [ -f ~/.claude/settings.json.bak ] && [ "$(jq '[.hooks.PreToolUse[] | .hooks[].command | select(test("gsd-"))] | length' ~/.claude/settings.json)" -ge 4 ] && RTK_TELEMETRY_DISABLED=1 rtk telemetry status | grep -qi disabled</automated>
  </verify>
  <acceptance_criteria>
    - `jq . ~/.claude/settings.json > /dev/null` 종료 코드 0 (JSON 유효)
    - `~/.claude/settings.json.bak` 파일 존재, 크기 > 0
    - `jq '.hooks.PreToolUse[] | select(.matcher == "Bash") | .hooks[].command' ~/.claude/settings.json`가 `rtk` 문자열 포함 항목 1개 이상 반환 (INSTALL-02)
    - 기존 GSD 훅 명령 문자열 4개 모두 보존: `gsd-prompt-guard`, `gsd-read-guard`, `gsd-workflow-guard`, `gsd-validate-commit` 각각 grep으로 발견 가능
    - `rtk telemetry status` 출력이 `disabled` 문자열 포함 (대소문자 무시) — SEC-02
    - 프로젝트 git 저장소 상태에서 `git status` 출력에 `~/.claude/` 또는 `~/.bashrc` 관련 변경 없음 (D-08: 프로젝트 파일 수정 없음)
    - `rtk telemetry status` 출력이 `enabled` 문자열을 포함하지 않음
  </acceptance_criteria>
  <done>
    `rtk init -g`가 settings.json에 PreToolUse Bash matcher RTK 훅을 추가하고 `.bak` 백업을 생성했다. JSON 유효성 확인됨. 기존 GSD 훅 4개 모두 보존됨. `rtk telemetry status`가 'disabled' 반환. INSTALL-02, SEC-02 충족.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `rtk init -g` 프로세스 → `~/.claude/settings.json` | 제3자 도구가 Claude Code 설정 파일을 쓰기 권한으로 수정 |
| `rtk init -g` 대화형 프롬프트 → 텔레메트리 동의 상태 | 사용자 입력(또는 env var)이 보안 경계를 결정 |
| Claude Code Bash tool call → rtk-rewrite.sh | 훅 스크립트가 모든 Bash 명령 입출력을 관찰/변형 |
| `~/.bashrc` → 모든 신규 shell 세션 env | 한 번의 append가 전체 shell 생태계에 영속적 영향 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | `~/.claude/settings.json` patch 손상 | mitigate | 패치 직후 `jq . ~/.claude/settings.json > /dev/null` 유효성 검증. 실패 시 `.bak`에서 즉시 복원 (Pitfall C3 대응). |
| T-09-06 | Information Disclosure | RTK 텔레메트리 opt-in 실수 | mitigate | D-06 순서 엄수: `export RTK_TELEMETRY_DISABLED=1` → `~/.bashrc` 등록 → `rtk init -g`. 프롬프트 표시 시 'n' 입력 폴백 (A1). `rtk telemetry status`로 최종 상태 확인. |
| T-09-07 | Denial of Service | 기존 GSD 훅 덮어쓰기 또는 실행 순서 손상 | mitigate | 패치 전후 PreToolUse 배열 길이 기록. 4개 GSD 훅 명령 문자열 존재 여부 grep으로 검증. Pitfall H2는 Phase 10 실제 git 명령 테스트로 추가 확인. |
| T-09-08 | Repudiation | `~/.bashrc` 중복 등록 | mitigate | append 전 `grep -q 'RTK_TELEMETRY_DISABLED=1'` idempotent 체크. 재실행 시 중복 라인 생성 방지. |
| T-09-09 | Elevation of Privilege | `rtk-rewrite.sh`가 임의 Bash 명령 입출력을 가로챔 | accept | RTK는 신뢰된 개인 도구 (Plan 01에서 rtk-ai/rtk 바이너리 검증 완료). 훅 스크립트 코드는 RTK가 생성 — 별도 감사 인프라 없음. |
| T-09-10 | Information Disclosure | `settings.json.bak`에 과거 secret 노출 | accept | 기존 settings.json 내용 자체의 보안 등급에 의존. RTK 행위는 단순 파일 복사로 신규 유출 경로 아님. 권한은 user-only 유지. |
</threat_model>

<verification>
Plan 수준 전체 검증:
```bash
# 1. ~/.bashrc SEC-01
grep -c '^export RTK_TELEMETRY_DISABLED=1$' ~/.bashrc    # → 1
bash -c 'source ~/.bashrc && echo $RTK_TELEMETRY_DISABLED'  # → 1

# 2. settings.json JSON 유효성 (Pitfall C3)
jq . ~/.claude/settings.json > /dev/null && echo valid

# 3. RTK 훅 등록 (INSTALL-02)
jq '.hooks.PreToolUse[] | select(.matcher == "Bash") | .hooks[].command' ~/.claude/settings.json

# 4. 기존 GSD 훅 4개 보존
jq -r '.hooks.PreToolUse[] | .hooks[].command' ~/.claude/settings.json | grep -cE 'gsd-(prompt|read|workflow|validate)'  # → 4

# 5. 백업 존재
ls -la ~/.claude/settings.json.bak

# 6. SEC-02
rtk telemetry status | grep -qi disabled

# 7. D-08: 프로젝트 파일 변경 없음
git status --porcelain
```
</verification>

<success_criteria>
- `~/.bashrc`에 `export RTK_TELEMETRY_DISABLED=1`이 정확히 1회 등록 (SEC-01)
- 새 bash 프로세스에서 env var가 `1`로 평가됨
- `~/.claude/settings.json`이 유효한 JSON이며 PreToolUse Bash matcher에 RTK 훅 포함 (INSTALL-02)
- `~/.claude/settings.json.bak` 백업 파일 존재
- 기존 GSD 훅 4개(prompt-guard, read-guard, workflow-guard, validate-commit) 모두 보존
- `rtk telemetry status` → `disabled` (SEC-02)
- 프로젝트 git 저장소에서 `git status` 결과가 ROADMAP 업데이트 이외의 파일 변경 없음 (D-08)
</success_criteria>

<output>
Create `.planning/phases/09-install-secure/09-02-SUMMARY.md` with:
- `~/.bashrc` append 결과 (등록된 라인, 중복 여부)
- 패치 전후 PreToolUse hook 배열 길이
- 추가된 RTK 훅의 정확한 command 문자열
- `rtk telemetry status` 출력
- JSON 유효성 검증 로그
- `.bak` 파일 존재 확인
- A1(env var가 프롬프트 suppress)이 실제로 관찰되었는지 여부 — Phase 10으로 이관할 경고가 있으면 명시
</output>
