---
phase: 07-knowledge-reinforcement-decay-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .knowledge/raw/2026-04-14.md
  - .knowledge/knowledge/decisions.md
autonomous: false
requirements:
  - D-03
must_haves:
  truths:
    - "Before snapshot: decisions.md 기준 Observed 카운터 보유 항목 수가 기록된다"
    - "테스트 raw 항목 2개가 .knowledge/raw/2026-04-14.md에 추가된다"
    - "/gsd-knowledge-compile 실행 후 decisions.md가 변경된다"
    - "After snapshot: 'index-first 접근 표준화' 항목에 Observed 카운터가 추가 또는 증가한다"
    - "After snapshot: conflict 감지 항목에 [conflict:] 태그 추가 여부가 확인된다"
    - "시뮬레이션 결과(동작 여부 판정)가 기록된다"
  artifacts:
    - path: ".knowledge/raw/2026-04-14.md"
      provides: "테스트 raw 항목 2개 포함"
      contains: "index-first"
    - path: ".knowledge/knowledge/decisions.md"
      provides: "컴파일 후 Before/After 비교 가능한 상태"
  key_links:
    - from: ".knowledge/raw/2026-04-14.md"
      to: ".knowledge/knowledge/decisions.md"
      via: "/gsd-knowledge-compile (B+C fusion)"
      pattern: "Observed"
---

<objective>
D-03: 실제 컴파일 시뮬레이션으로 B+C fusion 메커니즘 동작 여부를 검증한다.

Purpose: decisions.md의 Observed 카운터 증가(reinforcement)와 [conflict:] 태그 감지(conflict detection)가 실제로 동작하는지 before/after 비교로 확인한다. 이 결과는 Plan 02의 감쇄 분석 및 권고안에 직접 반영된다.

Output:
- .knowledge/raw/2026-04-14.md에 테스트 raw 항목 2개 추가
- /gsd-knowledge-compile 실행 후 decisions.md 변경 내용
- 시뮬레이션 판정 결과 (동작 / 미동작 / 부분 동작)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-knowledge-reinforcement-decay-audit/07-CONTEXT.md
@.planning/phases/07-knowledge-reinforcement-decay-audit/07-RESEARCH.md
@skills/gsd-knowledge-compile/SKILL.md
@.knowledge/knowledge/decisions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Before Snapshot — decisions.md 현재 상태 기록</name>
  <files>.planning/phases/07-knowledge-reinforcement-decay-audit/07-01-SIMULATION-LOG.md</files>
  <action>
decisions.md 현재 상태를 기록하여 After snapshot 비교 기준을 확립한다.

실행할 명령:
1. `wc -l .knowledge/knowledge/decisions.md` — 전체 라인 수 기록
2. `grep -c "Observed" .knowledge/knowledge/decisions.md` — Observed 카운터 보유 항목 수 기록
3. `grep -n "Observed" .knowledge/knowledge/decisions.md` — Observed 카운터 위치와 내용 기록
4. `grep "\[conflict:" .knowledge/knowledge/decisions.md` — conflict 태그 현황 기록 (현재 0건 예상)
5. "index-first 접근 표준화" 항목 전체 내용 확인 (grep -A 5 "index-first 접근 표준화")
6. "GSD 프로세스 knowledge 최소 부하 원칙" 항목 전체 내용 확인 (conflict 테스트 대상)

결과를 `.planning/phases/07-knowledge-reinforcement-decay-audit/07-01-SIMULATION-LOG.md`에 기록한다:

```markdown
# Phase 7 Plan 01 — Simulation Log

## Before Snapshot (2026-04-14)

### decisions.md 전체 라인 수
[wc -l 결과]

### Observed 카운터 보유 항목 수
[grep -c 결과]

### Observed 카운터 위치
[grep -n 결과]

### conflict 태그 현황
[grep 결과 — 없으면 "없음"]

### "index-first 접근 표준화" 항목 내용
[항목 전체]

### "GSD 프로세스 knowledge 최소 부하 원칙" 항목 내용 (conflict 테스트 대상)
[항목 전체]
```

이 파일은 After snapshot 비교의 기준선(baseline)이다. 삭제하지 않는다.
  </action>
  <verify>
    <automated>test -f /home/ozt88/knowledge-compiler/.planning/phases/07-knowledge-reinforcement-decay-audit/07-01-SIMULATION-LOG.md && grep "Before Snapshot" /home/ozt88/knowledge-compiler/.planning/phases/07-knowledge-reinforcement-decay-audit/07-01-SIMULATION-LOG.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>07-01-SIMULATION-LOG.md가 존재하고 Before Snapshot 섹션에 decisions.md 기준 상태가 기록된다</done>
</task>

<task type="auto">
  <name>Task 2: 테스트 Raw 항목 추가 — Observed 증가 + Conflict 감지 시나리오</name>
  <files>.knowledge/raw/2026-04-14.md</files>
  <action>
.knowledge/raw/2026-04-14.md에 테스트 raw 항목 2개를 추가한다. 이 파일은 실제 지식 파일이므로 각 항목을 [TEST] 접두어로 표시하여 구별한다. 컴파일 후 이 테스트 항목들을 원상 복구하거나 유지할지 Task 3에서 판단한다.

**항목 1 (Observed 증가 검증용):**
기존 decisions.md의 "index-first 접근 표준화" 항목과 동일한 결론을 담는다.
컴파일러가 이를 기존 항목의 재확인으로 인식해 Observed 카운터를 증가시켜야 한다.
SKILL.md Pitfall 3 주의: "일회성 확인 작업"으로 분류되지 않도록 결정 형식으로 작성한다.

```markdown
### 09:00 — [TEST] index-first 패턴 재확인 (Phase 7 시뮬레이션)
- Phase 7 감사 작업 중 knowledge 조회 방식을 재검토: raw/ 전체를 순차 로드하면 컨텍스트 비용이 높고 무관 정보가 포함되어 에이전트 판단을 방해함
- index.md를 먼저 읽어 관련 파일만 선택적으로 Read하는 index-first 접근이 Phase 4 이후 모든 세션에서 효과적임을 재확인
- 결정: index-first 접근 표준을 Phase 7 이후에도 유지
```

**항목 2 (Conflict 감지 검증용):**
기존 decisions.md의 "GSD 프로세스 knowledge 최소 부하 원칙 (Phase 6)" 항목([active])과 충돌하는 내용을 담는다.
컴파일러가 이를 반대 결론으로 인식해 [conflict:] 태그를 추가해야 한다.

```markdown
### 09:05 — [TEST] planner fallback compile 재검토 (Phase 7 시뮬레이션)
- Phase 7 감사 중 planner에서 fallback compile을 제거한 결정(Phase 6)을 재검토
- /gsd-knowledge-compile 수동 전용 방식은 세션 종료 시 compile을 빠뜨리는 경우가 발생함
- planner가 compile을 수행하는 것이 knowledge 최신성 유지에 더 신뢰성이 있다고 판단됨
```

파일에 두 항목을 이어서 append한다. 기존 내용은 보존한다.

**중요 — compile-manifest.json mtime 처리 (Pitfall 4):**
compile-manifest.json의 last_compiled가 "2026-04-13"이므로 오늘 날짜(2026-04-14)의 raw 파일은 정상 처리된다. 단, .knowledge/raw/2026-04-14.md가 신규 생성된 경우 manifest에 이 파일이 없으므로 full compile 대상에 포함된다. Append 후 파일 mtime이 현재 시각으로 갱신되므로 스킵 위험 없음.
  </action>
  <verify>
    <automated>grep "index-first" /home/ozt88/knowledge-compiler/.knowledge/raw/2026-04-14.md &amp;&amp; grep "planner fallback compile 재검토" /home/ozt88/knowledge-compiler/.knowledge/raw/2026-04-14.md &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>.knowledge/raw/2026-04-14.md에 테스트 항목 2개가 추가된다. 항목 1은 "index-first", 항목 2는 "planner fallback compile 재검토"를 포함한다</done>
</task>

<task type="auto">
  <name>Task 3: /gsd-knowledge-compile 실행 및 After Snapshot 기록</name>
  <files>
    .knowledge/knowledge/decisions.md
    .planning/phases/07-knowledge-reinforcement-decay-audit/07-01-SIMULATION-LOG.md
  </files>
  <action>
/gsd-knowledge-compile을 실행하고 결과를 07-01-SIMULATION-LOG.md에 After Snapshot으로 기록한다.

**실행 전 확인:**
- compile-manifest.json의 last_compiled 날짜 확인: `cat .planning/compile-manifest.json | grep last_compiled`
- .knowledge/raw/2026-04-14.md mtime 확인: `stat -c "%y" .knowledge/raw/2026-04-14.md`

**컴파일 실행:**
`/gsd-knowledge-compile` 호출

**컴파일 완료 후 After Snapshot 수집:**
1. `wc -l .knowledge/knowledge/decisions.md` — 라인 수 변화
2. `grep -c "Observed" .knowledge/knowledge/decisions.md` — Observed 항목 수 변화
3. `grep -n "Observed" .knowledge/knowledge/decisions.md` — 새 Observed 위치
4. `grep "\[conflict:" .knowledge/knowledge/decisions.md` — conflict 태그 추가 여부
5. `grep -A 7 "index-first 접근 표준화" .knowledge/knowledge/decisions.md` — 항목 변화 확인
6. `grep -A 7 "GSD 프로세스 knowledge 최소 부하 원칙" .knowledge/knowledge/decisions.md` — 항목 변화 확인

**판정:**

Before vs After 비교 후 다음 중 하나로 판정한다:

- **B+C fusion 정상 동작**: Observed 카운터가 증가한 경우
- **B+C fusion 미동작**: Observed 변화 없고 테스트 항목이 신규 항목으로 추가된 경우 (중복 감지 실패)
- **B+C fusion 부분 동작**: Observed는 증가했으나 conflict 태그는 없는 경우

07-01-SIMULATION-LOG.md에 After Snapshot 섹션 추가:

```markdown
## After Snapshot (2026-04-14)

### /gsd-knowledge-compile 실행 결과
[컴파일러 출력 요약]

### decisions.md 라인 수 변화
Before: [N줄] → After: [M줄]

### Observed 카운터 보유 항목 수 변화
Before: [N개] → After: [M개]

### Observed 카운터 내용 (After)
[grep -n 결과]

### conflict 태그 추가 여부
[grep 결과 — 없으면 "없음"]

### "index-first 접근 표준화" 항목 After
[항목 전체]

### "GSD 프로세스 knowledge 최소 부하 원칙" 항목 After
[항목 전체]

## 판정

### B+C fusion (Observed 증가) 검증
- [ ] Observed 카운터 증가 → fusion 정상 동작
- [ ] Observed 변화 없음, 신규 항목 추가 → fusion 미동작 (중복 감지 실패)
- [ ] Observed 변화 없음, 항목도 추가 안 됨 → 컴파일러가 건너뜀 (Pitfall 3)

### Conflict 감지 검증
- [ ] [conflict:] 태그 추가됨 → conflict 감지 정상
- [ ] [conflict:] 없음 → 반대 결론으로 인식 못 함

### 최종 판정
[동작 / 미동작 / 부분 동작] — 근거: [설명]
```

**테스트 항목 원상 복구 여부 결정:**
- 시뮬레이션 완료 후, 추가한 테스트 raw 항목 2개는 **파일에 그대로 유지**한다.
- 이유: (1) 실제 Phase 7 감사 작업 기록으로서 자연스럽고 (2) [TEST] 접두어로 구별되며 (3) 컴파일된 decisions.md에 이미 반영됐으므로 삭제해도 decisions.md는 변하지 않음.
- 단, 컴파일 결과 decisions.md에 불필요한 항목이 추가됐다면 (fusion 실패 케이스) 해당 항목을 decisions.md에서 수동 제거한다. 07-01-SIMULATION-LOG.md에 제거 여부와 이유를 기록한다.
  </action>
  <verify>
    <automated>grep -c "Observed" /home/ozt88/knowledge-compiler/.knowledge/knowledge/decisions.md &amp;&amp; grep "After Snapshot" /home/ozt88/knowledge-compiler/.planning/phases/07-knowledge-reinforcement-decay-audit/07-01-SIMULATION-LOG.md &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    07-01-SIMULATION-LOG.md에 After Snapshot과 최종 판정이 기록된다.
    decisions.md가 /gsd-knowledge-compile 실행 후 변경(또는 변경 없음 확인)된 상태다.
    B+C fusion 동작 여부와 conflict 감지 여부가 명확히 판정된다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    B+C fusion 시뮬레이션 완료:
    1. Before snapshot 기록 (07-01-SIMULATION-LOG.md)
    2. 테스트 raw 항목 2개 추가 (.knowledge/raw/2026-04-14.md)
    3. /gsd-knowledge-compile 실행 및 After snapshot 기록
    4. 판정 결과 기록 (동작 / 미동작 / 부분 동작)
  </what-built>
  <how-to-verify>
    1. `.planning/phases/07-knowledge-reinforcement-decay-audit/07-01-SIMULATION-LOG.md`를 읽어 Before/After 비교가 완성됐는지 확인
    2. 판정 섹션에서 최종 판정이 명확히 기록됐는지 확인
    3. decisions.md에서 "index-first 접근 표준화" 항목의 Observed 카운터 변화 확인:
       `grep -A 7 "index-first 접근 표준화" .knowledge/knowledge/decisions.md`
    4. conflict 감지 결과 확인:
       `grep "\[conflict:" .knowledge/knowledge/decisions.md`
    5. 판정이 Plan 02 감쇄 분석에 반영될 준비가 됐는지 확인
  </how-to-verify>
  <resume-signal>시뮬레이션 결과를 확인한 뒤 "approved" 또는 이슈를 설명해 주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → raw file | 에이전트가 .knowledge/raw/2026-04-14.md에 테스트 항목을 직접 append |
| /gsd-knowledge-compile → decisions.md | 컴파일러 에이전트가 decisions.md를 덮어씀 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | .knowledge/raw/2026-04-14.md append | accept | 동일 프로젝트 내 신뢰된 에이전트 작업. 파일 내용이 [TEST] 접두어로 표시됨 |
| T-07-01-02 | Tampering | decisions.md 컴파일 덮어쓰기 | mitigate | Before snapshot을 07-01-SIMULATION-LOG.md에 기록하여 원상 복구 가능. 컴파일 실패 시 git diff로 변경 추적 가능 |
| T-07-01-03 | Denial of Service | compile-manifest.json mtime 스킵 | mitigate | Pitfall 4 대응: last_compiled(2026-04-13) < 오늘(2026-04-14)이므로 정상 처리됨. Task 3에서 실행 전 확인 명시 |
| T-07-01-04 | Information Disclosure | SIMULATION-LOG.md 민감 정보 | accept | 프로젝트 내부 knowledge 구조 메타데이터만 포함. 개인정보 없음 |
</threat_model>

<verification>
시뮬레이션 전체 검증:

```bash
# 1. SIMULATION-LOG 존재 확인
test -f .planning/phases/07-knowledge-reinforcement-decay-audit/07-01-SIMULATION-LOG.md && echo "LOG: OK"

# 2. 테스트 항목 추가 확인
grep "index-first" .knowledge/raw/2026-04-14.md && echo "TEST-ITEM-1: OK"
grep "planner fallback compile 재검토" .knowledge/raw/2026-04-14.md && echo "TEST-ITEM-2: OK"

# 3. 컴파일 결과 확인
grep "Observed" .knowledge/knowledge/decisions.md
grep "\[conflict:" .knowledge/knowledge/decisions.md; echo "conflict check done"

# 4. 로그 완성도 확인
grep "After Snapshot" .planning/phases/07-knowledge-reinforcement-decay-audit/07-01-SIMULATION-LOG.md && echo "AFTER-SNAPSHOT: OK"
grep "최종 판정" .planning/phases/07-knowledge-reinforcement-decay-audit/07-01-SIMULATION-LOG.md && echo "JUDGMENT: OK"
```
</verification>

<success_criteria>
- 07-01-SIMULATION-LOG.md에 Before/After snapshot이 완성됨
- B+C fusion 동작 여부가 "동작 / 미동작 / 부분 동작"으로 판정됨
- conflict 감지 동작 여부가 판정됨
- 테스트 항목이 [TEST] 접두어로 표시되고 원상 복구 여부가 결정됨
- 판정 결과가 Plan 02에서 활용 가능한 형태로 기록됨
</success_criteria>

<output>
완료 후 `.planning/phases/07-knowledge-reinforcement-decay-audit/07-01-SUMMARY.md` 생성
</output>
