---
phase: 04-knowledge-importance-prioritization-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - patches/gsd-phase-researcher.patch.md
  - patches/gsd-verifier.patch.md
autonomous: true
requirements:
  - RELEVANCE-01
  - RELEVANCE-02
  - RELEVANCE-03
  - RELEVANCE-04

must_haves:
  truths:
    - "컴파일러가 raw 항목 중 knowledge 가치 있는 것만 포함하고 일회성 항목을 건너뛴다"
    - "index.md가 주제별 파일 안내(Quick Reference 테이블)를 포함하는 쿼리 안내 문서로 생성된다"
    - "에이전트가 knowledge/ 조회 시 index.md를 먼저 읽고 관련 파일만 선택적으로 Read한다"
    - "researcher 패치와 verifier 패치의 선별 기준 블록이 동일하다"
  artifacts:
    - path: "patches/gsd-phase-researcher.patch.md"
      provides: "컴파일 타임 선별 기준 + index.md 쿼리 안내 형식 + index-first 접근 지시"
      contains: "포함하는 항목"
    - path: "patches/gsd-verifier.patch.md"
      provides: "동일한 선별 기준 + index.md 형식 + index-first 접근 (D-08)"
      contains: "포함하는 항목"
  key_links:
    - from: "patches/gsd-phase-researcher.patch.md"
      to: "patches/gsd-verifier.patch.md"
      via: "D-08 패턴 — 선별 기준 블록 동일"
      pattern: "선별 기준"
---

<objective>
researcher/verifier 두 패치에 컴파일 타임 선별 기준, index.md 쿼리 안내 형식, index-first 접근 지시를 추가한다.

Purpose: raw 항목이 모두 동등하게 knowledge에 포함되는 현재 문제를 해결하고, 에이전트가 index.md를 진입점으로 관련 knowledge만 효율적으로 조회할 수 있게 한다.
Output: 두 패치 파일에 선별 기준 + index.md 형식 지시 + index-first 접근 지시가 추가됨
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-knowledge-importance-prioritization-scoring/04-RESEARCH.md

<interfaces>
<!-- 현재 패치 파일 구조 — executor가 정확한 삽입 위치를 파악하기 위해 필요 -->

From patches/gsd-phase-researcher.patch.md:
```markdown
## Step 0: Knowledge Compile (before research)
...
4. Compile new raw entries into knowledge/ structure:
   - `decisions.md` — 시도 → 결과 → 결정. 상태 태그: [active], [rejected], [superseded], [uncertain]
   - `guardrails.md` — (형식 지시 블록)
   - `anti-patterns.md` — (형식 지시 블록)
   - `troubleshooting.md` — 에러 메시지 ↔ 해결책 매핑
   - `index.md` — 전체 요약 + 키워드 인덱스     ← 이 줄을 형식 지시로 교체
   (분류 기준 블록)
   (기존 anti-patterns.md 마이그레이션 절차)
5. 기존 knowledge 파일을 읽은 뒤 새 항목을 추가하거나 기존 항목을 업데이트하여 병합하라

**During research (Step 3):** Also search ...   ← 이 블록을 index-first 접근 지시로 교체
```

From patches/gsd-verifier.patch.md:
```markdown
## Step 10b: Knowledge Reconcile (after verification)
...
3. Full reconcile — reprocess ALL raw entries and rebuild knowledge/:
   - `decisions.md` — 모든 시도/결정 통합, 상태 태그 정합성 확인
   - `guardrails.md` — (형식 지시 블록)
   - `anti-patterns.md` — (형식 지시 블록)
   - `troubleshooting.md` — 에러/해결 매핑 갱신
   - `index.md` — 전체 요약 재생성              ← 이 줄을 형식 지시로 교체
   (분류 기준 블록)
   (기존 anti-patterns.md 마이그레이션 절차)
4. 충돌하는 [active] decision이 있으면 [uncertain]으로 표시

**This is a FULL reconcile** ...
```

핵심 D-08 패턴 (Phase 02 SUMMARY에서 확립):
- 두 패치의 핵심 형식 지시 블록은 동일하되, troubleshooting/index 설명은 각 파일의 고유 표현 유지
- researcher: incremental compile, verifier: full reconcile — 컴파일 모드만 다르고 선별 기준/형식 지시는 동일
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: researcher 패치에 선별 기준 + index.md 형식 + index-first 접근 지시 추가</name>
  <files>patches/gsd-phase-researcher.patch.md</files>
  <read_first>
    - patches/gsd-phase-researcher.patch.md (현재 전체 내용 — 삽입 위치 파악)
    - .planning/phases/04-knowledge-importance-prioritization-scoring/04-RESEARCH.md (Code Examples 섹션 — 추가할 정확한 텍스트)
  </read_first>
  <action>
patches/gsd-phase-researcher.patch.md Step 0의 4번 항목에 다음 3가지 변경을 적용한다. RESEARCH.md의 Code Examples 섹션에 정확한 텍스트가 있으므로 이를 기반으로 작성한다.

**변경 1: 선별 기준 추가 (4번 항목 시작 직후, 파일별 지시 시작 전)**

4번 항목의 "Compile new raw entries into knowledge/ structure:" 바로 다음에 선별 기준 블록을 삽입한다:

```markdown
   **raw 항목 선별 기준 — knowledge에 포함하는 항목:**
   - 미래 세션에서 동일한 실수를 방지하는 결정 또는 기술적 발견
   - 시스템 동작, 제약, 설계 원칙에 대한 구조적 지식
   - 재발 가능한 에러와 그 해결 방법
   - 프로젝트 상태를 변화시킨 주요 완료 이벤트 (Phase 완료, 설계 결정 등)

   **건너뛰는 항목:**
   - 일회성 확인 작업 또는 상태 점검 (진행 상황 확인, 단순 조회)
   - 결과 없이 종료된 단순 탐색 시도
   - 이미 포함된 항목의 중복 기록
```

**변경 2: index.md 형식 지시 교체**

기존 `index.md` 줄:
```markdown
   - `index.md` — 전체 요약 + 키워드 인덱스
```

이것을 다음으로 교체:
```markdown
   - `index.md` — 다음 형식으로 생성한다:
     - 맨 위에 "Last compiled", "Total entries" 메타 정보
     - "Quick Reference" 테이블: 주제 | 파일 | 핵심 항목 (에이전트가 index.md만 읽어도 어떤 파일로 갈지 알 수 있도록)
     - 전체 요약 단락
     - 키워드 인덱스 (키워드 → 파일#섹션)
```

**변경 3: "During research" 블록을 index-first 접근 지시로 교체**

기존 "During research (Step 3):" 블록 전체(4줄)를 다음으로 교체:
```markdown
**During research (Step 3):** knowledge/ 조회 순서:
- `decisions.md` — `[rejected]` 항목을 확인하고 대안 접근법을 선택하라
- `anti-patterns.md` — 목록을 확인하고 연구 추천에서 검증된 패턴을 우선 적용하라
- `troubleshooting.md` — 이미 해결된 문제를 확인하고 새로운 미해결 문제에 집중하라

**조회 순서:** index.md를 먼저 읽어 현재 Phase와 관련된 파일을 파악한 후, 해당 파일만 선택적으로 Read하라.
```

주의: 기존 "During research" 블록의 decisions.md, anti-patterns.md, troubleshooting.md 안내는 유지하되, "Also search" 서두를 "knowledge/ 조회 순서:"로 변경하고, 마지막에 index-first 접근 지시를 추가한다.
  </action>
  <verify>
    <automated>bash -c 'echo "=== RELEVANCE-01 ===" && grep -c "포함하는 항목" patches/gsd-phase-researcher.patch.md && echo "=== RELEVANCE-02 ===" && grep -c "Quick Reference" patches/gsd-phase-researcher.patch.md && echo "=== RELEVANCE-03 ===" && grep -c "index.md를 먼저" patches/gsd-phase-researcher.patch.md && echo "=== 건너뛰기 ===" && grep -c "건너뛰는 항목" patches/gsd-phase-researcher.patch.md'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "포함하는 항목" patches/gsd-phase-researcher.patch.md` 결과가 1 이상
    - `grep -c "건너뛰는 항목" patches/gsd-phase-researcher.patch.md` 결과가 1 이상
    - `grep -c "Quick Reference" patches/gsd-phase-researcher.patch.md` 결과가 1 이상
    - `grep -c "index.md를 먼저" patches/gsd-phase-researcher.patch.md` 결과가 1 이상
    - `grep "Last compiled" patches/gsd-phase-researcher.patch.md` 출력이 있음
    - `grep "키워드 인덱스" patches/gsd-phase-researcher.patch.md` 출력이 있음
    - 기존 guardrails.md, anti-patterns.md, decisions.md, troubleshooting.md 형식 지시가 그대로 유지됨: `grep -c "guardrails.md" patches/gsd-phase-researcher.patch.md` 결과가 4 이상 (Phase 02에서 확인된 값)
  </acceptance_criteria>
  <done>researcher 패치에 선별 기준(포함/건너뛰기), index.md 쿼리 안내 형식(Quick Reference 테이블), index-first 접근 지시가 추가됨. 기존 형식 지시 블록은 변경 없이 보존됨.</done>
</task>

<task type="auto">
  <name>Task 2: verifier 패치에 동일한 선별 기준 + index.md 형식 + index-first 접근 지시 적용 (D-08)</name>
  <files>patches/gsd-verifier.patch.md</files>
  <read_first>
    - patches/gsd-verifier.patch.md (현재 전체 내용)
    - patches/gsd-phase-researcher.patch.md (Task 1 완료 후 상태 — D-08 동일성 기준)
  </read_first>
  <action>
patches/gsd-verifier.patch.md Step 10b에 Task 1과 동일한 3가지 변경을 적용한다. D-08 패턴에 따라 핵심 블록은 researcher 패치와 동일하게 작성한다.

**변경 1: 선별 기준 추가**

3번 항목의 "Full reconcile — reprocess ALL raw entries and rebuild knowledge/:" 바로 다음에, Task 1에서 researcher 패치에 추가한 것과 동일한 선별 기준 블록을 삽입한다:

```markdown
   **raw 항목 선별 기준 — knowledge에 포함하는 항목:**
   - 미래 세션에서 동일한 실수를 방지하는 결정 또는 기술적 발견
   - 시스템 동작, 제약, 설계 원칙에 대한 구조적 지식
   - 재발 가능한 에러와 그 해결 방법
   - 프로젝트 상태를 변화시킨 주요 완료 이벤트 (Phase 완료, 설계 결정 등)

   **건너뛰는 항목:**
   - 일회성 확인 작업 또는 상태 점검 (진행 상황 확인, 단순 조회)
   - 결과 없이 종료된 단순 탐색 시도
   - 이미 포함된 항목의 중복 기록
```

이 블록은 researcher 패치의 선별 기준 블록과 글자 단위로 동일해야 한다.

**변경 2: index.md 형식 지시 교체**

기존 `index.md` 줄:
```markdown
   - `index.md` — 전체 요약 재생성
```

이것을 다음으로 교체 (researcher 패치의 index.md 형식 지시와 동일):
```markdown
   - `index.md` — 다음 형식으로 생성한다:
     - 맨 위에 "Last compiled", "Total entries" 메타 정보
     - "Quick Reference" 테이블: 주제 | 파일 | 핵심 항목 (에이전트가 index.md만 읽어도 어떤 파일로 갈지 알 수 있도록)
     - 전체 요약 단락
     - 키워드 인덱스 (키워드 → 파일#섹션)
```

**변경 3: verifier 패치 하단에 index-first 접근 지시 추가**

verifier 패치에는 현재 "During research" 블록이 없다 (verifier는 검증 단계이므로). 대신, "**This is a FULL reconcile**" 줄 다음에 접근 안내를 추가한다:

```markdown
**knowledge/ 조회 안내 (Step 10b에서 생성된 index.md의 사용 지시):**
에이전트가 knowledge/를 조회할 때 index.md를 먼저 읽어 관련 파일을 파악한 후, 해당 파일만 선택적으로 Read하라.
```

참고: verifier는 "During research" 단계가 없으므로 researcher와 동일한 조회 순서 블록은 불필요하다. verifier의 역할은 full reconcile 시 index.md를 쿼리 안내 형식으로 생성하는 것이므로, index.md 형식 지시(변경 2)가 핵심이다. 변경 3은 생성된 index.md의 사용 방식을 명시하는 보조 지시다.

**D-08 동일성 검증:** 변경 1(선별 기준)과 변경 2(index.md 형식)는 researcher 패치와 글자 단위로 동일해야 한다. Task 완료 후 diff로 확인한다.
  </action>
  <verify>
    <automated>bash -c 'echo "=== RELEVANCE-01 ===" && grep -c "포함하는 항목" patches/gsd-verifier.patch.md && echo "=== RELEVANCE-02 ===" && grep -c "Quick Reference" patches/gsd-verifier.patch.md && echo "=== RELEVANCE-04 선별 기준 동일성 ===" && diff <(grep -A10 "선별 기준" patches/gsd-phase-researcher.patch.md) <(grep -A10 "선별 기준" patches/gsd-verifier.patch.md) && echo "IDENTICAL" || echo "DIFF FOUND" && echo "=== RELEVANCE-04 index.md 형식 동일성 ===" && diff <(grep -A5 "Quick Reference" patches/gsd-phase-researcher.patch.md) <(grep -A5 "Quick Reference" patches/gsd-verifier.patch.md) && echo "IDENTICAL" || echo "DIFF FOUND"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "포함하는 항목" patches/gsd-verifier.patch.md` 결과가 1 이상
    - `grep -c "건너뛰는 항목" patches/gsd-verifier.patch.md` 결과가 1 이상
    - `grep -c "Quick Reference" patches/gsd-verifier.patch.md` 결과가 1 이상
    - `grep "Last compiled" patches/gsd-verifier.patch.md` 출력이 있음
    - `diff <(grep -A10 "선별 기준" patches/gsd-phase-researcher.patch.md) <(grep -A10 "선별 기준" patches/gsd-verifier.patch.md)` 출력이 비어있음 (동일)
    - `diff <(grep -A5 "Quick Reference" patches/gsd-phase-researcher.patch.md) <(grep -A5 "Quick Reference" patches/gsd-verifier.patch.md)` 출력이 비어있음 (동일)
    - 기존 guardrails.md, anti-patterns.md 형식 지시가 보존됨: `grep -c "guardrails.md" patches/gsd-verifier.patch.md` 결과가 4 이상
  </acceptance_criteria>
  <done>verifier 패치에 researcher 패치와 동일한 선별 기준 및 index.md 형식 지시가 적용됨. D-08 패턴 준수 — 핵심 블록 diff가 비어있음 (동일). 기존 형식 지시 블록은 변경 없이 보존됨.</done>
</task>

</tasks>

<verification>
전체 Phase 검증:

1. **선별 기준 존재 확인:**
   ```bash
   grep -c "포함하는 항목" patches/gsd-phase-researcher.patch.md  # >= 1
   grep -c "포함하는 항목" patches/gsd-verifier.patch.md          # >= 1
   ```

2. **index.md 쿼리 안내 형식 확인:**
   ```bash
   grep -c "Quick Reference" patches/gsd-phase-researcher.patch.md  # >= 1
   grep -c "Quick Reference" patches/gsd-verifier.patch.md          # >= 1
   ```

3. **index-first 접근 지시 확인:**
   ```bash
   grep -c "index.md를 먼저" patches/gsd-phase-researcher.patch.md  # >= 1
   ```

4. **D-08 동일성 확인:**
   ```bash
   diff <(grep -A10 "선별 기준" patches/gsd-phase-researcher.patch.md) <(grep -A10 "선별 기준" patches/gsd-verifier.patch.md)
   # 출력이 비어있으면 동일
   ```

5. **기존 형식 지시 보존 확인:**
   ```bash
   grep -c "guardrails.md" patches/gsd-phase-researcher.patch.md  # >= 4
   grep -c "guardrails.md" patches/gsd-verifier.patch.md          # >= 4
   ```
</verification>

<success_criteria>
- 두 패치 파일 모두 선별 기준(포함하는 항목 + 건너뛰는 항목)을 포함한다
- 두 패치 파일 모두 index.md를 Quick Reference 테이블 포함 쿼리 안내 형식으로 생성하는 지시를 포함한다
- researcher 패치에 index-first 접근 지시("index.md를 먼저 읽어")가 포함된다
- 두 패치의 선별 기준 블록과 index.md 형식 블록이 동일하다 (D-08)
- 기존 guardrails.md, anti-patterns.md, decisions.md, troubleshooting.md 형식 지시가 변경 없이 보존된다
</success_criteria>

<output>
After completion, create `.planning/phases/04-knowledge-importance-prioritization-scoring/04-01-SUMMARY.md`
</output>
