---
phase: 08-knowledge-record-retrieve-design
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - skills/gsd-knowledge-compile/SKILL.md
  - patches/gsd-phase-researcher.patch.md
  - patches/gsd-planner.patch.md
  - patches/gsd-discuss-phase.patch.md
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04
must_haves:
  truths:
    - "SKILL.md Step 5가 반대 결론 시 [uncertain] 상태 전환을 포함한다"
    - "SKILL.md Step 5가 신규 항목 기록 시 context 태그와 Observed를 기본 포함하도록 지시한다"
    - "패치 파일들이 [uncertain] 항목 경고, [superseded] 항목 제외, 조회 우선순위를 지시한다"
    - "install.sh --force 재배포 후 PATCH count가 researcher=1, planner=1, discuss=1이다"
  artifacts:
    - path: "skills/gsd-knowledge-compile/SKILL.md"
      provides: "수정된 B+C fusion 정책 (uncertain 전환 + context 태그 지시)"
      contains: "uncertain"
    - path: "patches/gsd-phase-researcher.patch.md"
      provides: "조회 우선순위 + uncertain/superseded 처리 지침"
      contains: "uncertain"
    - path: "patches/gsd-planner.patch.md"
      provides: "조회 우선순위 + uncertain/superseded 처리 지침"
      contains: "uncertain"
    - path: "patches/gsd-discuss-phase.patch.md"
      provides: "조회 우선순위 + uncertain/superseded 처리 지침"
      contains: "uncertain"
  key_links:
    - from: "skills/gsd-knowledge-compile/SKILL.md"
      to: ".knowledge/knowledge/decisions.md"
      via: "Step 5 B+C fusion이 decisions.md 항목 형식에 맞게 기록"
      pattern: "context:"
    - from: "patches/gsd-phase-researcher.patch.md"
      to: ".knowledge/knowledge/decisions.md"
      via: "조회 지침이 uncertain/superseded 상태를 참조"
      pattern: "uncertain"
---

<objective>
SKILL.md의 B+C fusion 정책을 수정하여 충돌 기반 decay([uncertain] 상태 전환)를 추가하고, 패치 파일들에 조회 우선순위와 상태별 처리 지침을 추가한 후 install.sh로 재배포한다.

Purpose: D-02(증강/감쇄 구현), D-03(신규 증강 방법론), D-04(조회 활용 개선)를 구현한다. Phase 7 RESEARCH.md의 1순위(SKILL.md Step 5 수정) + 2순위(패치 파일 지침 추가) 권고안을 실행한다.
Output: 수정된 SKILL.md + 패치 파일 3개 + install.sh 재배포 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-knowledge-record-retrieve-design/08-CONTEXT.md
@.planning/phases/08-knowledge-record-retrieve-design/08-RESEARCH.md
@.planning/phases/07-knowledge-reinforcement-decay-audit/07-RESEARCH.md
@.planning/phases/08-knowledge-record-retrieve-design/08-01-SUMMARY.md

<interfaces>
<!-- SKILL.md Step 5 현재 B+C fusion 정책 -->
```
**B+C fusion 정책:**
- 동일 결론 재확인 → `**Observed:** N times (date1, date2, ...)` 줄 추가 또는 카운트 증가
- 반대 결론 → `[conflict: YYYY-MM-DD]` 태그 추가 + `> **New (YYYY-MM-DD):**` blockquote로 새 내용 보존
```

<!-- decisions.md 항목 형식 (Plan 01에서 확정, 08-01-SUMMARY.md 참조) -->
```markdown
## 설계 결정 — [제목]
[active] [context: category1, category2]

**시도:** [...]
**결과:** [...]
**결정:** [...]
**Observed:** N times (date1, date2, ...)
```

<!-- 현재 패치 파일 조회 지침 (researcher) -->
```
**During research (Step 3):** knowledge/ lookup order:
- `decisions.md` — Check [rejected] entries and choose an alternative approach
- `anti-patterns.md` — Review the list and prioritize proven patterns
- `troubleshooting.md` — Check already-solved problems
```

<!-- context 태그 카테고리 (6개) -->
file-loading, agent-behavior, knowledge-format, compile-logic, install-deploy, scope-backlog
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md Step 5 B+C fusion 정책 수정 + context 태그 기록 지시 추가</name>
  <files>skills/gsd-knowledge-compile/SKILL.md</files>
  <action>
SKILL.md의 Step 5 "knowledge/ 병합" 섹션을 수정한다.

**1. B+C fusion 정책 수정 (Phase 7 RESEARCH.md 1순위 권고안):**

현재 B+C fusion 정책 블록을 아래로 교체한다:

```
**B+C fusion 정책:**
- 동일 결론 재확인 → `**Observed:** N times (date1, date2, ...)` 줄 추가 또는 카운트 증가. 날짜가 서로 다른 날이면 독립적 재확인으로 더 높은 신뢰도를 의미한다.
- 반대 결론 (동일 주제에서 이전 결정과 **직접적으로 모순되는** 결론) →
  1. `[conflict: YYYY-MM-DD]` 태그 추가
  2. `> **New (YYYY-MM-DD):**` blockquote로 새 내용 보존
  3. 항목 상태를 `[uncertain]`으로 변경
- `[uncertain]` 항목 처리 — 다음 컴파일 시 raw에서 추가 증거를 수집한다:
  - 동일 방향 증거 발견 → `[active]`로 복귀 + conflict blockquote 아래에 판정 결과 기록
  - 반대 방향 증거 확정 → `[superseded]` 또는 `[rejected]`로 확정
  - 증거 없음 → `[uncertain]` 유지
```

"직접적으로 모순되는"이라는 기준을 명시하는 것이 핵심이다. 다른 맥락의 유사 결론을 conflict로 잘못 판정하는 것을 방지한다 (per RESEARCH.md Pitfall 4).

**2. 신규 항목 기록 형식 지시 추가:**

Step 5의 "파일별 업데이트" 하위 항목 `decisions.md` 설명을 수정한다:

현재:
```
- `decisions.md` — Attempt → Result → Decision (status: `[active]`, `[rejected]`, `[superseded]`, `[uncertain]`)
```

변경 후:
```
- `decisions.md` — 신규 항목 형식:
  ```
  ## 설계 결정 — [제목]
  [active] [context: tag1, tag2]

  **시도:** [...]
  **결과:** [...]
  **결정:** [...]
  **Observed:** 1 times (YYYY-MM-DD)
  ```
  - `[context: ...]` 태그는 항목의 적용 영역을 나타낸다. 카테고리: `file-loading`, `agent-behavior`, `knowledge-format`, `compile-logic`, `install-deploy`, `scope-backlog`. 항목당 최대 3개.
  - 상태값: `[active]`, `[rejected]`, `[superseded]`, `[uncertain]`
  - `**Observed:** 1 times (YYYY-MM-DD)`를 기본 포함한다 (최초 기록일 = 오늘 날짜)
```

**3. D-03 신규 증강 방법론 반영 (RESEARCH.md 평가 결과):**

Step 5 B+C fusion 정책 블록 내에 이미 다음이 포함됨:
- "신뢰도 가중치" = Observed 카운터를 신뢰도 신호로 활용 → "날짜가 서로 다른 날이면 독립적 재확인으로 더 높은 신뢰도" 문구로 반영됨
- "Cross-session 합산" = 위 문구에 통합됨
- "사용 컨텍스트 태깅" = context 태그 시스템으로 구현됨

추가로 별도 구현이 필요한 것은 없다. 세 가지 방법론 모두 위 수정 사항에 통합되었다.
  </action>
  <verify>
    <automated>cd /home/ozt88/knowledge-compiler && echo "=== uncertain in SKILL.md ===" && grep -c "uncertain" skills/gsd-knowledge-compile/SKILL.md && echo "=== context tag instruction ===" && grep -c "context:" skills/gsd-knowledge-compile/SKILL.md && echo "=== conflict criteria ===" && grep -c "직접적으로 모순" skills/gsd-knowledge-compile/SKILL.md</automated>
  </verify>
  <done>
- SKILL.md에 "uncertain"이 3회 이상 등장 (상태 전환, uncertain 처리 규칙, 상태값 목록)
- "context:" 관련 지시가 존재
- "직접적으로 모순되는" 판단 기준이 명시됨
  </done>
</task>

<task type="auto">
  <name>Task 2: 패치 파일 3개에 조회 우선순위 + uncertain/superseded 처리 지침 추가 + install.sh 재배포</name>
  <files>patches/gsd-phase-researcher.patch.md, patches/gsd-planner.patch.md, patches/gsd-discuss-phase.patch.md</files>
  <action>
**1. gsd-phase-researcher.patch.md 수정:**

현재 내용을 아래로 교체한다:

```markdown
<!-- PATCH:knowledge-compiler for gsd-phase-researcher.md -->
<!-- Insert BEFORE: "## Step 1: Receive Scope and Load Context" -->

<!-- PATCH:knowledge-compiler — reapply after GSD updates -->

**During research (Step 3):** knowledge/ lookup order:

1. `index.md` — Quick Reference 테이블에서 현재 리서치 주제와 관련된 파일 식별
2. `decisions.md` — 조회 우선순위:
   - Observed 카운터가 높은 항목 우선 (여러 세션에서 재확인된 결정은 더 신뢰성 높음)
   - `[context: ...]` 태그가 현재 리서치 주제와 일치하는 항목 우선
   - `[rejected]` 항목: 이 방향을 피하고 대안을 선택
   - `[uncertain]` 항목: 이 결정은 불확실 상태 — 사용 전 raw/ 최근 항목에서 추가 컨텍스트 확인 필요
   - `[superseded]` 항목: 참고 목적으로만 읽고 리서치 권고안에 반영하지 않음
3. `anti-patterns.md` — Review the list and prioritize proven patterns in research recommendations
4. `troubleshooting.md` — Check already-solved problems and focus on new unresolved issues

**Lookup order:** Read index.md first to identify files relevant to the current phase, then selectively Read only those files.
```

**2. gsd-planner.patch.md 수정:**

현재 내용을 아래로 교체한다:

```markdown
<!-- PATCH:knowledge-compiler for gsd-planner.md -->
<!-- Insert INSIDE: <project_context> block, AFTER the "Project skills" paragraph -->

**Project knowledge:** Check if `.knowledge/knowledge/` exists in the working directory.

**Skip condition:** If `.knowledge/knowledge/` does not exist or `index.md` is missing, skip this step.

**If knowledge/ exists:**

1. Read `.knowledge/knowledge/index.md` to get the "Quick Reference" table and keyword index.
2. Identify which knowledge files are relevant to the current phase (based on phase name, tech stack, and task keywords).
3. Selectively read only the relevant files from:
   - `decisions.md` — 조회 우선순위:
     - Observed 카운터가 높은 항목 우선 (여러 세션에서 재확인된 결정은 더 신뢰성 높음)
     - `[context: ...]` 태그가 현재 Phase와 일치하는 항목 우선
     - `[rejected]` entries: do NOT plan tasks using rejected approaches
     - `[uncertain]` entries: 이 결정은 불확실 상태 — task action에 "uncertain 결정이므로 구현 전 검증 필요" 명시
     - `[superseded]` entries: 참고 목적으로만 읽고 plan에 반영하지 않음
   - `guardrails.md` — Treat entries as hard constraints; plan tasks must comply
   - `anti-patterns.md` — Treat "Instead" recommendations as preferred patterns in task actions
   - `troubleshooting.md` — If a known problem applies to this phase, include the solution in task actions preemptively

4. Embed knowledge findings directly into task `<action>` blocks:
   - Reference the source: `"per knowledge/guardrails.md: [rule]"` or `"avoiding [anti-pattern] per knowledge/anti-patterns.md"`
   - For rejected decisions: note the alternative in the action — `"Use X (Y was rejected: [reason])"`
   - For uncertain decisions: note in the action — `"Decision Z is [uncertain] — verify before relying on it"`

**Lookup order:** Always read `index.md` first. Only load individual files when their Quick Reference entry is relevant. Never load all knowledge files unconditionally.
```

**3. gsd-discuss-phase.patch.md 수정:**

현재 내용을 아래로 교체한다:

```markdown
<!-- PATCH:knowledge-compiler for discuss-phase.md -->
<!-- Insert BEFORE: "<step name=\"check_existing\">" -->

<!-- PATCH:knowledge-compiler — reapply after GSD updates -->

**Project knowledge:** discuss 시작 전 이전 세션의 결정사항을 확인한다.

**Skip condition:** `.knowledge/knowledge/`가 없거나 `index.md`가 없으면 건너뜀.

**If knowledge/ exists:**

1. `.knowledge/knowledge/index.md` 읽기 — Quick Reference 테이블과 키워드 인덱스 확인.
2. 현재 Phase 주제와 관련 있는 knowledge 파일 식별.
3. 관련 파일만 선택적으로 읽기:
   - `decisions.md` — 조회 우선순위:
     - Observed 카운터가 높은 항목 우선 (여러 세션에서 재확인된 결정)
     - `[context: ...]` 태그가 현재 Phase 주제와 일치하는 항목 우선
     - `[rejected]` 항목: 이미 거부된 방향을 재질문하거나 재제안하지 않음
     - `[uncertain]` 항목: 사용자에게 "이 결정은 불확실 상태"임을 알리고 재논의 여부 확인
     - `[superseded]` 항목: 대체된 결정임을 참고로만 언급
   - `guardrails.md` — 하드 제약 확인: discuss 중 이 제약을 위반하는 선택지를 제시하지 않음
4. compile은 수행하지 않음 — discuss는 조회만.

**Lookup order:** index.md를 먼저 읽고, Quick Reference에서 관련 파일만 선택적으로 로드한다.
```

**4. install.sh --force 재배포:**

패치 파일 수정 완료 후 install.sh --force를 실행한다:
```bash
cd /home/ozt88/knowledge-compiler && bash install.sh --force
```

**5. PATCH count 검증 (per guardrails.md: install.sh 실행 후 PATCH count 즉시 확인):**

```bash
echo "researcher=$(grep -c 'PATCH:knowledge-compiler' ~/.claude/agents/gsd-phase-researcher.md)"
echo "planner=$(grep -c 'PATCH:knowledge-compiler' ~/.claude/get-shit-done/patches/gsd-planner.patch.md 2>/dev/null || grep -c 'PATCH:knowledge-compiler' ~/.claude/agents/gsd-planner.md)"
echo "discuss=$(grep -c 'PATCH:knowledge-compiler' ~/.claude/get-shit-done/workflows/discuss-phase.md)"
```

기대값: researcher=1, planner=1, discuss=1. 값이 1이 아니면 install.sh 또는 패치 파일에 버그가 있으므로 즉시 조사한다.
  </action>
  <verify>
    <automated>cd /home/ozt88/knowledge-compiler && bash install.sh --force 2>&1 && echo "=== PATCH counts ===" && echo "researcher=$(grep -c 'PATCH:knowledge-compiler' ~/.claude/agents/gsd-phase-researcher.md)" && echo "planner=$(grep -c 'PATCH:knowledge-compiler' ~/.claude/agents/gsd-planner.md)" && echo "discuss=$(grep -c 'PATCH:knowledge-compiler' ~/.claude/get-shit-done/workflows/discuss-phase.md)" && echo "=== uncertain in patches ===" && grep -c "uncertain" patches/gsd-phase-researcher.patch.md && grep -c "uncertain" patches/gsd-planner.patch.md && grep -c "uncertain" patches/gsd-discuss-phase.patch.md</automated>
  </verify>
  <done>
- PATCH count: researcher=1, planner=1, discuss=1
- 세 패치 파일 모두 "uncertain" 키워드 포함
- 세 패치 파일 모두 "superseded" 키워드 포함
- 세 패치 파일 모두 "Observed" 우선순위 지침 포함
- SKILL.md에 [uncertain] 상태 전환 규칙 + context 태그 지시 포함
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.sh → 에이전트 파일 | 패치 내용이 에이전트 시스템 프롬프트에 주입됨 |
| SKILL.md → 컴파일 에이전트 | fusion 정책이 decisions.md 상태 변경을 제어 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | SKILL.md uncertain 전환 | mitigate | "직접적으로 모순되는" 기준을 명시하여 LLM의 과도한 uncertain 판정 방지 |
| T-08-02-02 | Denial of Service | install.sh 중복 패치 | mitigate | PATCH count 검증을 verify 단계에서 강제. 기대값 1 아니면 fail |
| T-08-02-03 | Tampering | install.sh --force 재배포 | mitigate | 재배포 후 PATCH count 검증 (researcher=1, planner=1, discuss=1) per guardrails.md |
</threat_model>

<verification>
1. SKILL.md Step 5에 충돌 기반 decay + context 태그 기록 지시가 포함됨
2. 패치 파일 3개에 조회 우선순위 + uncertain/superseded 처리 지침이 포함됨
3. install.sh --force 후 PATCH count = researcher:1, planner:1, discuss:1
</verification>

<success_criteria>
- SKILL.md에 "uncertain" 3회 이상, "context:" 2회 이상 등장
- 패치 파일 3개 모두 "uncertain", "superseded", "Observed" 키워드 포함
- install.sh 재배포 후 PATCH count 모두 1
</success_criteria>

<output>
After completion, create `.planning/phases/08-knowledge-record-retrieve-design/08-02-SUMMARY.md`
</output>
