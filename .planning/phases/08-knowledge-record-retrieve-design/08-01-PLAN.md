---
phase: 08-knowledge-record-retrieve-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .knowledge/knowledge/decisions.md
  - .knowledge/knowledge/index.md
autonomous: true
requirements:
  - D-01
  - D-02
must_haves:
  truths:
    - "decisions.md의 모든 15개 항목이 동일한 메타데이터 형식을 갖는다"
    - "모든 항목에 [context: ...] 태그가 존재한다"
    - "모든 항목에 Observed 줄이 존재한다"
    - "index.md 키워드 인덱스가 변경된 제목 형식과 동기화되어 있다"
  artifacts:
    - path: ".knowledge/knowledge/decisions.md"
      provides: "context 태그 + Observed 메타데이터가 추가된 15개 항목"
      contains: "[context:"
    - path: ".knowledge/knowledge/index.md"
      provides: "갱신된 키워드 인덱스"
      contains: "키워드 인덱스"
  key_links:
    - from: ".knowledge/knowledge/decisions.md"
      to: ".knowledge/knowledge/index.md"
      via: "키워드 인덱스의 fragment ID가 decisions.md 제목 형식과 일치"
      pattern: "#설계-결정--"
---

<objective>
context 태그 형식을 확정하고, 기존 decisions.md 15개 항목에 메타데이터를 소급 적용한다.

Purpose: D-01(기록과 조회의 통합 설계)의 기반 작업. 메타데이터 형식이 확정되어야 이후 SKILL.md 수정과 패치 파일 업데이트가 가능하다.
Output: 메타데이터가 추가된 decisions.md + 동기화된 index.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-knowledge-record-retrieve-design/08-CONTEXT.md
@.planning/phases/08-knowledge-record-retrieve-design/08-RESEARCH.md

<interfaces>
<!-- decisions.md 현재 항목 형식 (변환 전) -->
```markdown
## 설계 결정 — [제목]

**시도:** [...]
**결과:** [...]
**결정:** [...]
**상태:** [active|rejected|superseded|uncertain]
```

<!-- decisions.md 목표 항목 형식 (변환 후) -->
```markdown
## 설계 결정 — [제목]
[active] [context: category1, category2]

**시도:** [...]
**결과:** [...]
**결정:** [...]
**Observed:** 1 times (YYYY-MM-DD)
```

<!-- context 태그 카테고리 (6개, RESEARCH.md 기반) -->
file-loading     — 파일 로드 방식, index-first 패턴
agent-behavior   — 에이전트 행동 원칙, GSD 프로세스
knowledge-format — knowledge 파일 형식, 구조 설계
compile-logic    — 컴파일 로직, fusion 정책
install-deploy   — install.sh, 패치 배포
scope-backlog    — 범위 결정, 백로그 항목
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: decisions.md 15개 항목 메타데이터 소급 적용</name>
  <files>.knowledge/knowledge/decisions.md</files>
  <action>
decisions.md의 15개 항목을 아래 규칙에 따라 일괄 변환한다.

**형식 변환 규칙:**
1. "설계 결정 —" 제목 프리픽스는 유지한다 (per CONTEXT.md 제약: 제거 시 index.md 대규모 수정 발생)
2. `**상태:** [xxx]` 줄을 삭제하고, 상태 태그를 제목 바로 다음 줄로 이동: `[active] [context: ...]`
3. `[context: ...]` 태그를 상태 태그와 같은 줄에 추가한다. 항목당 최대 3개 태그, 소문자 kebab-case
4. `**Observed:** 1 times (YYYY-MM-DD)` 줄을 항목 본문 마지막에 추가한다. 이미 Observed가 있는 항목은 유지한다.
5. 날짜는 해당 항목이 처음 기록된 Phase의 완료일을 사용한다:
   - Phase 2 항목 (2026-04-08): guardrails+anti-patterns 이중 구조
   - Phase 4 항목 (2026-04-10): index-first, 컴파일 타임 선별 기준, D-08 패턴, 쿼리 시점 관련성, Phase 4 요구사항, PageIndex backlog
   - Phase 5 항목 (2026-04-12): install.sh --force, spec-only 전환, gsd-clear 삭제, JSONL 측정, researcher compile 제거, knowledge raw 수집 변천
   - Phase 6 항목 (2026-04-13): GSD 최소 부하 원칙, 갭 클로저 플랜 패턴

**context 태그 할당 (RESEARCH.md 분석 기반):**
- guardrails+anti-patterns 이중 구조 → [context: knowledge-format]
- index-first 접근 표준화 → [context: file-loading, agent-behavior]
- 컴파일 타임 선별 기준 도입 → [context: compile-logic]
- D-08 패턴 → [context: knowledge-format, compile-logic]
- 쿼리 시점 관련성 모델 선택 → [context: agent-behavior, file-loading]
- Phase 4 요구사항 → [context: scope-backlog]
- PageIndex 연동 Backlog → [context: scope-backlog]
- install.sh --force → [context: install-deploy]
- Phase 5 spec-only 전환 → [context: agent-behavior, scope-backlog]
- gsd-clear 커스텀 스킬 삭제 → [context: agent-behavior, install-deploy]
- JSONL 기반 참조율 측정 → [context: agent-behavior, compile-logic]
- researcher compile 제거 → [context: agent-behavior, compile-logic]
- GSD 최소 부하 원칙 → [context: agent-behavior]
- 갭 클로저 플랜 패턴 → [context: agent-behavior]
- Knowledge raw 수집 방식 변천 → [context: compile-logic, agent-behavior]

**변환 예시 (Before → After):**

Before:
```
## 설계 결정 — index-first 접근 표준화

**시도:** 에이전트가 knowledge/ 전체를 순차 로드
**결과:** 컨텍스트 비용이 높고 관련 없는 정보까지 로드됨
**결정:** index.md를 먼저 읽어 관련 파일만 선택적으로 Read하는 index-first 접근 표준화 (RELEVANCE-03)
**상태:** [active]
```

After:
```
## 설계 결정 — index-first 접근 표준화
[active] [context: file-loading, agent-behavior]

**시도:** 에이전트가 knowledge/ 전체를 순차 로드
**결과:** 컨텍스트 비용이 높고 관련 없는 정보까지 로드됨
**결정:** index.md를 먼저 읽어 관련 파일만 선택적으로 Read하는 index-first 접근 표준화 (RELEVANCE-03)
**Observed:** 1 times (2026-04-10)
```

**[superseded] 항목 처리:** "researcher compile 제거" 항목은 상태가 `[superseded]`다. 형식은 동일하게 변환하되 상태는 `[superseded]`를 유지한다.

**이미 Observed가 있는 항목 처리:** "Phase 5 spec-only 전환" 항목은 이미 `**Observed:** 2 times (2026-04-12, 2026-04-12)` 줄이 있다. 이 경우 기존 Observed 줄을 유지하고 중복 추가하지 않는다.

모든 항목을 한 번에 변환하여 형식 일관성을 보장한다. 항목별로 개별 수정하지 말고 전체 파일을 Write 도구로 작성한다.
  </action>
  <verify>
    <automated>cd /home/ozt88/knowledge-compiler && echo "=== context tag count ===" && grep -c "\[context:" .knowledge/knowledge/decisions.md && echo "=== status line removed ===" && grep -c "^\*\*상태:\*\*" .knowledge/knowledge/decisions.md && echo "=== Observed count ===" && grep -c "Observed:" .knowledge/knowledge/decisions.md && echo "=== heading count ===" && grep -c "^## 설계 결정" .knowledge/knowledge/decisions.md</automated>
  </verify>
  <done>
- context tag count = 15 (모든 항목에 [context: ...] 존재)
- status line removed count = 0 (**상태:** 줄이 모두 제거됨)
- Observed count = 15 (모든 항목에 Observed 줄 존재)
- heading count = 15 (항목 수 불변)
  </done>
</task>

<task type="auto">
  <name>Task 2: index.md 키워드 인덱스 동기화</name>
  <files>.knowledge/knowledge/index.md</files>
  <action>
decisions.md 항목 형식이 변경됐으므로 index.md를 동기화한다.

**수정 사항:**

1. **키워드 인덱스 fragment ID 확인:** decisions.md 제목 프리픽스("설계 결정 —")를 유지했으므로 기존 fragment ID(#설계-결정--xxx)는 변경 없다. **상태:** 줄 제거와 [context:] 줄 추가는 제목(##)을 바꾸지 않으므로 fragment ID에 영향 없음을 확인한다.

2. **Quick Reference 테이블 업데이트:** 핵심 항목 설명에 변경사항 반영이 필요한 경우만 수정한다. 현재 Quick Reference는 항목 이름 요약이므로 구조 변경 없음.

3. **전체 요약 섹션에 Phase 8 메모 추가:**
```
**Phase 8 (Knowledge Record & Retrieve Design):** decisions.md 항목에 context 태그와 Observed 메타데이터 소급 적용. 상태 태그를 제목 줄 다음으로 이동하여 조회 효율성 개선.
```

4. **Last compiled 날짜 업데이트:** `**Last compiled:** 2026-04-15`로 갱신

5. **Total entries 카운트 확인 및 갱신:** decisions 항목 수는 15로 유지 (신규 추가 없음)

fragment ID가 변경되지 않았으므로 키워드 인덱스 테이블의 링크는 수정 불필요하다.
  </action>
  <verify>
    <automated>cd /home/ozt88/knowledge-compiler && echo "=== Last compiled ===" && grep "Last compiled" .knowledge/knowledge/index.md && echo "=== Phase 8 mention ===" && grep -c "Phase 8" .knowledge/knowledge/index.md && echo "=== fragment IDs intact ===" && grep -c "#설계-결정--" .knowledge/knowledge/index.md</automated>
  </verify>
  <done>
- Last compiled가 2026-04-15로 갱신됨
- Phase 8 관련 요약이 전체 요약 섹션에 추가됨
- 키워드 인덱스의 fragment ID가 모두 유지됨 (변경 전과 동일 개수)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 컴파일러 에이전트 → decisions.md | LLM이 context 태그를 해석하여 항목 필터링에 사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | decisions.md 수동 편집 | accept | 수동 편집은 의도된 동작이며, git으로 변경 추적 가능 |
| T-08-01-02 | Information Disclosure | context 태그에 민감 정보 | accept | 태그는 카테고리명(file-loading 등)만 포함, 실제 데이터 없음 |
</threat_model>

<verification>
1. decisions.md의 모든 15개 항목이 동일한 메타데이터 형식을 따른다
2. [context: ...] 태그가 모든 항목에 존재한다
3. **상태:** 줄이 완전히 제거되었다
4. Observed 줄이 모든 항목에 존재한다
5. index.md 키워드 인덱스의 fragment ID가 정상 작동한다
</verification>

<success_criteria>
- grep -c "\[context:" decisions.md = 15
- grep -c "^\*\*상태:\*\*" decisions.md = 0
- grep -c "Observed:" decisions.md = 15
- index.md Last compiled = 2026-04-15
</success_criteria>

<output>
After completion, create `.planning/phases/08-knowledge-record-retrieve-design/08-01-SUMMARY.md`
</output>
