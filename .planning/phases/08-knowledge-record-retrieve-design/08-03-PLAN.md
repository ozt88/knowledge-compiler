---
phase: 08-knowledge-record-retrieve-design
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - .knowledge/raw/2026-04-15.md
  - .knowledge/knowledge/decisions.md
autonomous: false
requirements:
  - D-02
  - D-03
must_haves:
  truths:
    - "동일 결론 raw 항목 컴파일 후 해당 decisions.md 항목의 Observed 카운터가 증가한다"
    - "반대 결론 raw 항목 컴파일 후 해당 decisions.md 항목이 [uncertain] 상태로 전환된다"
    - "신규 항목이 중복 추가되지 않는다 (fusion 정상 동작)"
    - "패치 파일의 PATCH count가 정상이다 (researcher=1, planner=1, discuss=1)"
  artifacts:
    - path: ".knowledge/knowledge/decisions.md"
      provides: "컴파일 시뮬레이션 결과가 반영된 decisions.md"
      contains: "Observed: 2 times"
    - path: ".knowledge/raw/2026-04-15.md"
      provides: "시뮬레이션용 테스트 raw 항목"
      contains: "index-first"
  key_links:
    - from: ".knowledge/raw/2026-04-15.md"
      to: ".knowledge/knowledge/decisions.md"
      via: "/gsd-knowledge-compile 실행"
      pattern: "Observed"
---

<objective>
Phase 7 D-03에서 이월된 B+C fusion 시뮬레이션을 수행하여 증강/감쇄 메커니즘의 실제 동작을 검증하고, 전체 Phase 8 구현을 최종 점검한다.

Purpose: B+C fusion이 실제로 동작하는지 before/after 비교로 확인한다. 미동작 시 SKILL.md 지시 강화가 필요하다.
Output: 시뮬레이션 결과가 반영된 decisions.md + 검증 완료 확인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-knowledge-record-retrieve-design/08-CONTEXT.md
@.planning/phases/08-knowledge-record-retrieve-design/08-RESEARCH.md
@.planning/phases/07-knowledge-reinforcement-decay-audit/07-RESEARCH.md
@.planning/phases/08-knowledge-record-retrieve-design/08-01-SUMMARY.md
@.planning/phases/08-knowledge-record-retrieve-design/08-02-SUMMARY.md

<interfaces>
<!-- 07-RESEARCH.md D-03 시뮬레이션 방법론 -->

테스트 raw 항목 예시 (Observed 카운터 증가 검증용):
```markdown
### HH:MM — index-first 패턴 재확인
- knowledge 참조 시 index.md를 먼저 읽어 관련 파일만 선택적으로 로드하는 방식이 효율적임을 다시 확인
- 전체 파일을 순차적으로 읽으면 컨텍스트 비용이 높고 무관 정보가 포함됨
- 결정: index-first 접근을 표준으로 유지 (기존 결정 재확인)
```

테스트 raw 항목 예시 (conflict 감지 검증용):
```markdown
### HH:MM — planner fallback compile 재검토
- planner가 fallback compile을 수행하는 것이 knowledge 최신성 유지에 도움이 되는 것으로 재고
- /gsd-knowledge-compile 수동 전용보다 planner 자동 compile이 더 신뢰성 있을 수 있음
```

판정 기준:
- Observed 카운터가 증가함 → fusion 로직 정상 동작
- Observed 카운터 변화 없음 → fusion 로직 미동작
- 신규 항목으로 추가됨 → 중복 감지 실패
- conflict 항목이 [uncertain]으로 전환됨 → decay 정상 동작
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: B+C fusion 시뮬레이션 (Observed + conflict)</name>
  <files>.knowledge/raw/2026-04-15.md, .knowledge/knowledge/decisions.md</files>
  <action>
Phase 7 D-03에서 정의된 시뮬레이션 방법론을 실행한다.

**Step A: Before snapshot 기록**

decisions.md에서 시뮬레이션 대상 항목의 현재 상태를 기록한다:
1. "설계 결정 — index-first 접근 표준화" 항목: Observed 줄 값 확인 (Plan 01에서 `Observed: 1 times (2026-04-10)` 추가됨)
2. "설계 결정 — GSD 프로세스 knowledge 최소 부하 원칙 (Phase 6)" 항목: 현재 상태 `[active]` 확인
3. 전체 항목 수: `grep -c "^## 설계 결정" .knowledge/knowledge/decisions.md` 결과 기록

**Step B: 테스트 raw 항목 작성**

`.knowledge/raw/2026-04-15.md` 파일에 다음 두 항목을 추가한다 (파일이 이미 존재하면 append, 없으면 생성):

```markdown
### 14:00 — index-first 패턴 재확인
- knowledge 참조 시 index.md를 먼저 읽어 관련 파일만 선택적으로 로드하는 방식이 효율적임을 다시 확인
- 전체 파일을 순차적으로 읽으면 컨텍스트 비용이 높고 무관 정보가 포함됨
- Phase 8 planning에서 index-first 접근을 표준으로 유지하기로 재확인 (기존 결정 reinforcement)

### 14:01 — planner fallback compile 재검토
- Phase 8 기록 메타데이터 작업 중 compile 타이밍에 대해 재고
- planner가 fallback compile을 수행하는 것이 knowledge 최신성 유지에 도움이 될 수 있음
- /gsd-knowledge-compile 수동 전용보다 planner 자동 compile이 더 신뢰성 있을 수 있다는 관점
```

첫 번째 항목은 "index-first 접근 표준화" 결정과 동일 결론 → Observed 카운터 증가를 기대한다.
두 번째 항목은 "GSD 최소 부하 원칙" 결정과 모순 → [conflict:] 태그 + [uncertain] 상태 전환을 기대한다.

**Step C: /gsd-knowledge-compile 실행**

Agent 도구를 사용하여 `/gsd-knowledge-compile` 스킬을 호출한다. 이것은 SKILL.md의 Step 1~7을 실행하여 raw 항목을 knowledge에 병합하는 프로세스다.

**주의사항:**
- 컴파일 에이전트가 테스트 항목을 "일회성 확인"으로 분류하지 않도록 항목 형식을 자연스럽게 작성했다
- 컴파일 결과에서 decisions.md 외 다른 파일(guardrails.md, index.md 등)도 변경될 수 있음 — 정상

**Step D: After snapshot 비교**

컴파일 완료 후 다음을 확인한다:

1. **Observed 카운터 증가 확인:**
```bash
grep -A 10 "index-first 접근 표준화" .knowledge/knowledge/decisions.md | grep "Observed"
```
기대: `Observed: 2 times (2026-04-10, 2026-04-15)` (또는 유사 형태로 카운트 증가)

2. **conflict + uncertain 전환 확인:**
```bash
grep -A 5 "GSD 프로세스 knowledge 최소 부하 원칙" .knowledge/knowledge/decisions.md | head -3
```
기대: `[uncertain]` 상태 + `[conflict: 2026-04-15]` 태그

3. **중복 추가 미발생 확인:**
```bash
grep -c "^## 설계 결정" .knowledge/knowledge/decisions.md
```
기대: Before snapshot과 동일한 수 (15개) — 신규 항목이 추가되지 않아야 함. 만약 16개 이상이면 fusion이 중복 감지에 실패한 것이므로 수동 정리 + SKILL.md 지시 강화가 필요하다.

**판정 결과 처리:**

- **Observed 증가 + conflict/uncertain 전환 = PASS:** fusion이 정상 동작. 추가 조치 없음.
- **Observed 미증가 or conflict 미감지 = PARTIAL:** SKILL.md Step 5 지시가 불충분. 더 명시적인 매칭 힌트를 추가해야 한다 (예: "기존 항목 제목과 새 raw 항목의 주제를 비교하여..."). 이 경우 SKILL.md를 즉시 수정하고 시뮬레이션을 재실행한다.
- **신규 항목으로 중복 추가 = FAIL:** fusion 로직 자체가 미동작. SKILL.md에 "기존 항목 목록을 먼저 읽고, 동일 주제 항목이 있으면 신규 추가 대신 Observed 증가" 지시를 더 강력하게 추가한다.

**PARTIAL/FAIL 시 추가 조치:** SKILL.md 지시를 수정한 후 install.sh --force로 재배포하고, raw/2026-04-15.md의 테스트 항목을 유지한 채 다시 컴파일한다.
  </action>
  <verify>
    <automated>cd /home/ozt88/knowledge-compiler && echo "=== Observed count ===" && grep -c "Observed:" .knowledge/knowledge/decisions.md && echo "=== Observed 2+ times ===" && grep "Observed: [2-9]" .knowledge/knowledge/decisions.md && echo "=== uncertain status ===" && grep -c "\[uncertain\]" .knowledge/knowledge/decisions.md && echo "=== conflict tag ===" && grep -c "\[conflict:" .knowledge/knowledge/decisions.md && echo "=== total headings ===" && grep -c "^## 설계 결정" .knowledge/knowledge/decisions.md && echo "=== PATCH counts final ===" && echo "researcher=$(grep -c 'PATCH:knowledge-compiler' ~/.claude/agents/gsd-phase-researcher.md)" && echo "planner=$(grep -c 'PATCH:knowledge-compiler' ~/.claude/agents/gsd-planner.md)" && echo "discuss=$(grep -c 'PATCH:knowledge-compiler' ~/.claude/get-shit-done/workflows/discuss-phase.md)"</automated>
  </verify>
  <done>
- Observed가 2 times 이상인 항목이 최소 2개 존재 (기존 1개 + 시뮬레이션 1개)
- [uncertain] 상태 항목이 1개 이상 존재
- [conflict:] 태그가 1개 이상 존재
- 전체 heading 수가 15개 유지 (중복 추가 없음)
- PATCH count: researcher=1, planner=1, discuss=1
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 8 전체 구현 검증</name>
  <action>사용자가 Phase 8 전체 구현물(decisions.md 메타데이터, SKILL.md 수정, 패치 파일, 시뮬레이션 결과)을 검증한다.</action>
  <what-built>
Phase 8 전체 구현:
1. decisions.md 15개 항목에 context 태그 + Observed 메타데이터 소급 적용 (Plan 01)
2. SKILL.md Step 5 B+C fusion 정책 수정 — 충돌 기반 decay([uncertain] 전환) + context 태그 기록 지시 (Plan 02)
3. 패치 파일 3개에 조회 우선순위 + uncertain/superseded 처리 지침 추가 + install.sh 재배포 (Plan 02)
4. B+C fusion 시뮬레이션 — Observed 카운터 증가 + conflict/uncertain 전환 검증 (Plan 03 Task 1)
  </what-built>
  <how-to-verify>
1. decisions.md를 열어 항목 형식이 일관되는지 확인:
   - 모든 항목이 `[상태] [context: ...]` 줄을 제목 바로 다음에 갖는지
   - 모든 항목에 `**Observed:** N times (날짜)` 줄이 있는지
   - context 태그 카테고리가 항목 내용에 적합한지 (6개 카테고리: file-loading, agent-behavior, knowledge-format, compile-logic, install-deploy, scope-backlog)

2. B+C fusion 시뮬레이션 결과 확인:
   - "index-first 접근 표준화" 항목의 Observed가 2 이상인지
   - "GSD 최소 부하 원칙" 항목이 [uncertain]이고 [conflict:] 태그가 있는지

3. SKILL.md Step 5 확인:
   - 충돌 기반 decay 규칙이 있는지 ("직접적으로 모순되는" 기준 명시)
   - 신규 항목 기록 형식에 context 태그 + Observed 지시가 있는지

4. 패치 파일 확인:
   - researcher/planner/discuss 패치 모두 uncertain, superseded, Observed 우선순위 지침 포함
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 수정이 필요한 부분 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw 항목 → 컴파일러 에이전트 | 테스트 raw 항목이 의도한 대로 fusion 동작을 트리거하는지 |
| 컴파일러 에이전트 → decisions.md | LLM이 동일 결론/반대 결론을 올바르게 판단하는지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | 컴파일러의 uncertain 과도 적용 | mitigate | SKILL.md에 "직접적으로 모순되는" 기준 명시 (Plan 02에서 이미 구현) + 시뮬레이션으로 동작 확인 |
| T-08-03-02 | Tampering | 시뮬레이션 raw 항목이 실제 knowledge로 잔류 | accept | 테스트 항목도 유효한 관찰이므로 knowledge에 남아도 무방. conflict 항목은 [uncertain] 상태로 관리됨 |
</threat_model>

<verification>
1. B+C fusion Observed 카운터 증가 정상 동작
2. 충돌 기반 decay(conflict → uncertain 전환) 정상 동작
3. 신규 항목 중복 추가 없음 (heading 수 15개 유지)
4. 전체 PATCH count 정상 (최종 확인)
5. 사용자가 decisions.md 형식 + 시뮬레이션 결과를 승인
</verification>

<success_criteria>
- Observed 2+ times 항목이 최소 2개
- [uncertain] 항목이 1개 이상
- [conflict:] 태그가 1개 이상
- heading 수 = 15 (또는 컴파일러가 추가한 정상 신규 항목만큼 증가)
- PATCH count: researcher=1, planner=1, discuss=1
- checkpoint:human-verify 승인 완료
</success_criteria>

<output>
After completion, create `.planning/phases/08-knowledge-record-retrieve-design/08-03-SUMMARY.md`
</output>
