---
phase: 03-collection-instruction-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/claude-md-section.md
  - $HOME/.claude/CLAUDE.md
autonomous: true
requirements:
  - COLLECT-01

must_haves:
  truths:
    - "templates/claude-md-section.md의 수집 규칙에 '포함하지 않을 것' 항목이 없다"
    - "규칙 3이 허용 범위를 구체적으로 명시하여 코드 전문/개인정보/도구 호출 세부사항을 자연스럽게 배제한다"
    - "$HOME/.claude/CLAUDE.md의 Knowledge Compiler 섹션이 template과 동일한 긍정형 규칙을 포함한다"
    - "규칙 번호가 1-5로 순차 재조정되어 있다 (기존 6개 → 5개)"
  artifacts:
    - path: "templates/claude-md-section.md"
      provides: "긍정형 턴 수집 지시 템플릿 (source of truth)"
      contains: "행동과 결과"
    - path: "$HOME/.claude/CLAUDE.md"
      provides: "긍정형 턴 수집 지시가 반영된 글로벌 지시 파일"
      contains: "행동과 결과"
  key_links:
    - from: "templates/claude-md-section.md"
      to: "$HOME/.claude/CLAUDE.md"
      via: "install.sh가 template을 CLAUDE.md에 복사"
      pattern: "Knowledge Compiler"
---

<objective>
CLAUDE.md 턴 수집 지시의 부정형 규칙(규칙 4: "요약에 포함하지 않을 것")을 삭제하고, 규칙 3("요약에 포함할 것")을 세밀화하여 허용 범위를 명확히 한정하는 긍정형 전환을 수행한다.

Purpose: LLM은 부정형 지시보다 긍정형 지시의 준수율이 높다 (Pink Elephant Problem). 허용 항목만 구체적으로 명시하면 그 외 항목(코드 전문, 개인정보, 도구 호출 세부사항)은 자동으로 배제된다.
Output: templates/claude-md-section.md (source of truth) 및 $HOME/.claude/CLAUDE.md 두 파일 모두 긍정형 수집 규칙으로 전환 완료.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-collection-instruction-refactor/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: template 긍정형 전환 (COLLECT-01, D-01, D-02, D-03)</name>
  <files>templates/claude-md-section.md</files>
  <read_first>
    - templates/claude-md-section.md (수정 대상 - 현재 상태 확인 필수)
    - .planning/phases/03-collection-instruction-refactor/03-CONTEXT.md (D-01~D-04 결정사항 참조)
  </read_first>
  <action>
templates/claude-md-section.md의 **규칙:** 섹션을 다음과 같이 수정한다.

**현재 규칙 3-6:**
```
3. 요약에 포함할 것: 무엇을 했는지, 핵심 발견/결정, 변경된 파일
4. 요약에 포함하지 않을 것: 코드 전문, 사용자 개인정보, 도구 호출 세부사항
5. 사용자가 `/collect-off` 또는 "수집 중지"라고 하면 해당 세션 동안 수집 중단
6. 이 지시는 GSD 워크플로 안팎 모두에서 항상 적용
```

**변경 후 규칙 3-5:**
```
3. 요약에 포함할 것: 수행한 행동과 그 결과, 핵심 발견 또는 결정 사항, 변경된 파일 경로 (각 항목은 한 줄 이내로 요약)
4. 사용자가 `/collect-off` 또는 "수집 중지"라고 하면 해당 세션 동안 수집 중단
5. 이 지시는 GSD 워크플로 안팎 모두에서 항상 적용
```

핵심 변경:
- 규칙 4("포함하지 않을 것") 전체 삭제 (per D-01)
- 규칙 3의 "무엇을 했는지" → "수행한 행동과 그 결과"로 구체화
- 규칙 3의 "핵심 발견/결정" → "핵심 발견 또는 결정 사항"으로 명확화
- 규칙 3의 "변경된 파일" → "변경된 파일 경로"로 구체화
- 규칙 3에 "(각 항목은 한 줄 이내로 요약)" 범위 한정 추가 — 코드 전문이나 세부 로그가 들어갈 여지 차단 (per D-02)
- 기존 규칙 5→4, 규칙 6→5로 번호 재조정
- 규칙 1, 2, 예시 블록은 변경하지 않음 (per D-04: 규칙 5는 범위 밖)
  </action>
  <verify>
    <automated>grep -c "포함하지 않을 것" templates/claude-md-section.md | grep -q "^0$" && grep -q "수행한 행동과 그 결과" templates/claude-md-section.md && grep -q "각 항목은 한 줄 이내로 요약" templates/claude-md-section.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - templates/claude-md-section.md에 "포함하지 않을 것" 문자열이 0회 등장
    - templates/claude-md-section.md에 "코드 전문" 문자열이 0회 등장
    - templates/claude-md-section.md에 "수행한 행동과 그 결과" 문자열이 정확히 1회 등장
    - templates/claude-md-section.md에 "핵심 발견 또는 결정 사항" 문자열이 정확히 1회 등장
    - templates/claude-md-section.md에 "변경된 파일 경로" 문자열이 정확히 1회 등장
    - templates/claude-md-section.md에 "각 항목은 한 줄 이내로 요약" 문자열이 정확히 1회 등장
    - templates/claude-md-section.md의 규칙이 총 5개 (1~5번)
    - templates/claude-md-section.md에 "수집 중단" 문자열이 존재 (기존 규칙 5가 4번으로 유지)
    - templates/claude-md-section.md에 "GSD 워크플로 안팎" 문자열이 존재 (기존 규칙 6이 5번으로 유지)
    - 예시 블록(### 22:04)이 변경 없이 유지
  </acceptance_criteria>
  <done>templates/claude-md-section.md에서 규칙 4가 삭제되고, 규칙 3이 긍정형으로 세밀화되며, 번호가 1-5로 재조정된다</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md 글로벌 지시 동기화 (COLLECT-01, D-03)</name>
  <files>$HOME/.claude/CLAUDE.md</files>
  <read_first>
    - $HOME/.claude/CLAUDE.md (수정 대상 - 현재 상태 확인 필수)
    - templates/claude-md-section.md (Task 1 완료 후 source of truth - 동기화 원본)
  </read_first>
  <action>
$HOME/.claude/CLAUDE.md 파일의 "## Knowledge Compiler — 턴 수집" 섹션을 Task 1에서 수정한 templates/claude-md-section.md의 내용과 동일하게 교체한다 (per D-03: template이 source of truth).

구체적으로:
1. $HOME/.claude/CLAUDE.md에서 "## Knowledge Compiler — 턴 수집" 줄부터 파일 끝까지의 블록을 찾는다
2. 해당 블록 전체를 templates/claude-md-section.md의 전체 내용으로 교체한다
3. "## Knowledge Compiler" 위의 다른 섹션(Language, Context Engineering, Think Before Coding, Simplicity First, Code Quality, Goal-Driven Execution, Git, CLI Tools, GSD Workflow)은 일체 변경하지 않는다

교체 후 해당 섹션의 규칙이 다음과 같아야 한다:
```
3. 요약에 포함할 것: 수행한 행동과 그 결과, 핵심 발견 또는 결정 사항, 변경된 파일 경로 (각 항목은 한 줄 이내로 요약)
4. 사용자가 `/collect-off` 또는 "수집 중지"라고 하면 해당 세션 동안 수집 중단
5. 이 지시는 GSD 워크플로 안팎 모두에서 항상 적용
```

규칙 4("포함하지 않을 것")는 존재하지 않아야 한다.
  </action>
  <verify>
    <automated>grep -c "포함하지 않을 것" $HOME/.claude/CLAUDE.md | grep -q "^0$" && grep -q "수행한 행동과 그 결과" $HOME/.claude/CLAUDE.md && diff <(grep -A 20 "Knowledge Compiler" templates/claude-md-section.md) <(grep -A 20 "Knowledge Compiler" $HOME/.claude/CLAUDE.md) > /dev/null && echo "PASS: synced" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - $HOME/.claude/CLAUDE.md에 "포함하지 않을 것" 문자열이 0회 등장
    - $HOME/.claude/CLAUDE.md에 "코드 전문" 문자열이 0회 등장
    - $HOME/.claude/CLAUDE.md에 "수행한 행동과 그 결과" 문자열이 정확히 1회 등장
    - $HOME/.claude/CLAUDE.md에 "각 항목은 한 줄 이내로 요약" 문자열이 정확히 1회 등장
    - $HOME/.claude/CLAUDE.md의 Knowledge Compiler 섹션 규칙이 총 5개 (1~5번)
    - $HOME/.claude/CLAUDE.md의 Knowledge Compiler 섹션과 templates/claude-md-section.md의 내용이 diff 없이 일치
    - $HOME/.claude/CLAUDE.md의 다른 섹션(## Language ~ ## GSD Workflow)이 변경 없이 유지
  </acceptance_criteria>
  <done>$HOME/.claude/CLAUDE.md의 Knowledge Compiler 섹션이 template과 동일한 긍정형 규칙으로 동기화된다</done>
</task>

</tasks>

<verification>
Phase 3 전체 검증:

1. 부정형 패턴 부재 확인:
```bash
grep -n "포함하지 않을 것\|코드 전문\|사용자 개인정보\|도구 호출 세부사항" templates/claude-md-section.md $HOME/.claude/CLAUDE.md
# 기대: 매칭 없음 (exit code 1)
```

2. 긍정형 전환 확인:
```bash
grep -c "수행한 행동과 그 결과" templates/claude-md-section.md
# 기대: 1

grep -c "각 항목은 한 줄 이내로 요약" templates/claude-md-section.md
# 기대: 1
```

3. template-CLAUDE.md 동기화 확인:
```bash
diff <(cat templates/claude-md-section.md) <(sed -n '/## Knowledge Compiler/,$p' $HOME/.claude/CLAUDE.md)
# 기대: 차이 없음
```

4. 규칙 개수 확인:
```bash
grep -c "^[0-9]\." templates/claude-md-section.md
# 기대: 5
```
</verification>

<success_criteria>
- 두 파일 모두에서 "포함하지 않을 것", "코드 전문", "사용자 개인정보", "도구 호출 세부사항" 문자열이 0회 등장
- 두 파일 모두에서 "수행한 행동과 그 결과", "핵심 발견 또는 결정 사항", "변경된 파일 경로" 긍정형 항목이 존재
- 규칙 번호가 1-5 (총 5개)로 재조정됨
- templates/claude-md-section.md와 $HOME/.claude/CLAUDE.md의 Knowledge Compiler 섹션이 동일
</success_criteria>

<output>
After completion, create `.planning/phases/03-collection-instruction-refactor/03-01-SUMMARY.md`
</output>
