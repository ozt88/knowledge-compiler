---
phase: quick-260410-p9f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md, docs/DESIGN.md]
autonomous: true
---

<objective>
README.md와 docs/DESIGN.md를 v1.1 마일스톤 완료 상태에 맞게 최신화한다.

Purpose: v1.1에서 구현 완료된 incremental compile, guardrails.md, Stage 3 lint가 문서에 반영되지 않아 실제 구현과 문서 사이에 괴리가 있음.
Output: README.md, docs/DESIGN.md 두 파일 업데이트
</objective>

<context>
@README.md
@docs/DESIGN.md
@patches/gsd-verifier.patch.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md v1.1 반영</name>
  <files>README.md</files>
  <action>
README.md에 아래 4개 변경을 적용한다.

1. **파이프라인 다이어그램 (## 해법, 코드 블록 L13~17):**
   - 3번째 줄 `Phase 검증 완료 → verifier가 knowledge/ full reconcile` 을
   - `Phase 검증 완료 → verifier가 raw/ incremental compile + knowledge lint` 로 변경

2. **knowledge/ 파일 구조 테이블 (## knowledge/ 파일 구조, L67~73):**
   - `anti-patterns.md` 행의 "내용" 셀: `"이것은 하지 마라" 목록과 이유` → `맥락 의존형 접근법 (Observation-Reason-Instead 형식)`
   - 테이블 마지막 행(`troubleshooting.md`) 뒤에 새 행 추가:
     `| \`guardrails.md\` | 대안이 단일 선택으로 고정된 경우 — 긍정형 행동 지시 |`

3. **구조 트리 (## 구조, L49~63):**
   - `gsd-verifier.patch.md` 설명: `verifier Step 10b: full reconcile` → `verifier Step 10b/10c: incremental compile + knowledge lint`

4. **기타 검증:** 변경 후 마크다운 테이블 정렬이 깨지지 않았는지 확인.
  </action>
  <verify>
    <automated>grep -n "incremental compile + knowledge lint" README.md && grep -n "Observation-Reason-Instead" README.md && grep -n "guardrails.md" README.md</automated>
  </verify>
  <done>README.md에 4개 변경 모두 반영됨: 파이프라인 다이어그램, anti-patterns 설명, guardrails 행 추가, 구조 트리 verifier 설명</done>
</task>

<task type="auto">
  <name>Task 2: docs/DESIGN.md v1.1 반영</name>
  <files>docs/DESIGN.md</files>
  <action>
docs/DESIGN.md에 아래 5개 변경을 적용한다.

1. **Stage 2 컴파일 트리거 (#### Stage 2, L111 부근):**
   - `gsd-verifier Step 10b (full reconcile)` → `gsd-verifier Step 10b (incremental)`

2. **knowledge/ 파일 구조 테이블 (#### Stage 2, L96~103):**
   - `anti-patterns.md` 행의 "내용" 셀: `"이것은 하지 마라" 목록과 이유` → `맥락 의존형 접근법 (Observation-Reason-Instead 형식)`
   - 테이블 마지막 행(`troubleshooting.md`) 뒤에 새 행 추가:
     `| \`guardrails.md\` | 대안이 단일 선택으로 고정된 경우 — 긍정형 행동 지시 |`

3. **Stage 3 린트 (#### Stage 3: 린트, L114~119):**
   - 제목의 `(이후 확장)` → 삭제 (제목은 `#### Stage 3: 린트` 만 남김. 단, 원본에 "(이후 확장)"이 제목이 아닌 본문에만 있다면 본문에서 삭제)
   - 기존 3줄짜리 "컴파일 후 LLM이 knowledge를 스캔..." 내용을 전부 아래로 교체:

```
v1.1 구현 완료. Step 10b(컴파일) 직후 Step 10c로 실행.

**Skip condition:** `.knowledge/knowledge/` 디렉토리가 없으면 전체 skip.

**규칙:**

| Rule | 검사 대상 | 조건 | 조치 |
|------|-----------|------|------|
| 1 | `decisions.md` | 충돌하는 `[active]` decision 쌍 | `[conflict: YYYY-MM-DD]` 마킹 |
| 2 | `guardrails.md` | raw/ 출처 없는 guardrail | `[unverified]` 태그 |
| 3 | `decisions.md` | `[superseded]` 대체 참조 없음 | `[superseded-orphan]` 마킹 |
```

4. **구현 순서 (### 이후 확장, L143~147):**
   - `5. lint 단계` → `5. ~~lint 단계~~ ✓ (v1.1 Step 10c — MVP로 승격)`

5. **관찰 포인트 (## 5. 관찰 포인트, L149~152):**
   - 기존 2개 항목 뒤에 관찰 결과를 추가:
     ```
     3. (v1.1 결과) CLAUDE.md 행동 지시 방식 채택 확정 — 수집 누락률 허용 범위 내
     4. (v1.1 결과) knowledge/ 참조로 researcher가 과거 결정을 활용하는 것 확인
     ```
  </action>
  <verify>
    <automated>grep -n "incremental" docs/DESIGN.md && grep -n "Observation-Reason-Instead" docs/DESIGN.md && grep -n "guardrails.md" docs/DESIGN.md && grep -n "superseded-orphan" docs/DESIGN.md && grep -n "v1.1 결과" docs/DESIGN.md</automated>
  </verify>
  <done>docs/DESIGN.md에 5개 변경 모두 반영됨: Stage 2 트리거, anti-patterns/guardrails 테이블, Stage 3 실제 구현 내용, 구현 순서 lint 체크, 관찰 결과</done>
</task>

</tasks>

<verification>
두 파일 모두에서:
- `guardrails.md` 언급 존재
- `anti-patterns.md` 설명이 "Observation-Reason-Instead" 포함
- "full reconcile" 문구 잔존 없음 (grep -r "full reconcile" README.md docs/DESIGN.md 결과 0건)
</verification>

<success_criteria>
1. README.md: 파이프라인 다이어그램, 파일 구조 테이블(guardrails 행 포함), 구조 트리 모두 v1.1 상태 반영
2. docs/DESIGN.md: Stage 2 트리거, 파일 구조 테이블, Stage 3 실구현 내용, 구현 순서, 관찰 포인트 모두 v1.1 상태 반영
3. "full reconcile" 잔존 문구 없음
</success_criteria>

<output>
완료 후 `.planning/quick/260410-p9f-readme-md-docs-design-md-v1-1/260410-p9f-SUMMARY.md` 생성
</output>
