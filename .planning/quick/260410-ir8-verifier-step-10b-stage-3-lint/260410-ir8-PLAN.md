---
phase: quick-260410-ir8
plan: 01
type: execute
wave: 1
depends_on: [260410-ijc]
files_modified:
  - patches/gsd-verifier.patch.md
autonomous: true
must_haves:
  truths:
    - "verifier 패치의 decisions.md 설명이 researcher 패치와 동일한 형식(Attempt → Result → Decision + 상태 태그)을 사용한다"
    - "verifier 패치에 subsequent reinforcement 예시(3번째 이상)가 추가된다"
    - "troubleshooting.md 설명이 researcher 패치와 일관된 형식을 사용한다"
    - "나머지 내용(step 번호, skip 조건, conflict/reinforcement 감지 규칙 등)은 그대로 유지된다"
  artifacts:
    - path: "patches/gsd-verifier.patch.md"
      provides: "researcher 패치와 일관성이 맞춰진 verifier 패치"
  key_links:
    - from: "patches/gsd-verifier.patch.md"
      to: "patches/gsd-phase-researcher.patch.md"
      via: "D-08 패턴 — 두 패치 간 compile 로직 일관성"
      pattern: "decisions.md format, troubleshooting.md format, reinforcement examples"
---

<objective>
verifier Step 10b 패치의 남은 불일치를 researcher 패치와 맞춰 최종 린트한다.

Purpose: 260410-ijc에서 incremental compile 로직은 맞췄으나, knowledge 파일별 설명 형식과 reinforcement 예시에서 researcher/verifier 간 잔여 불일치가 존재한다. 이를 제거하여 D-08 패턴을 완전히 준수하도록 한다.
Output: 수정된 `patches/gsd-verifier.patch.md`
</objective>

<tasks>

<task type="auto">
  <name>Task 1: verifier 패치 decisions.md / troubleshooting.md 설명 및 reinforcement 예시 통일</name>
  <files>patches/gsd-verifier.patch.md</files>
  <action>
`patches/gsd-verifier.patch.md`에서 다음 3가지를 수정한다.

**변경 1: decisions.md 설명 형식**

현재:
```
   - `decisions.md` — Consolidate all attempts/decisions, verify status tag consistency
```

교체:
```
   - `decisions.md` — Attempt → Result → Decision. Status tags: `[active]`, `[rejected]`, `[superseded]`, `[uncertain]`
```

**변경 2: troubleshooting.md 설명 형식**

현재:
```
   - `troubleshooting.md` — Update error/solution mapping
```

교체:
```
   - `troubleshooting.md` — Error message ↔ solution mapping
```

**변경 3: subsequent reinforcement 예시 추가**

현재 reinforcement 섹션 끝 (첫 번째 reinforcement 예시 뒤):
```
   - Example (first reinforcement):
     ```
     ## Some Decision
     **Observed:** 2 times (2026-04-08, 2026-04-10)
     ```
```

아래 내용을 추가 (첫 번째 예시 블록 닫는 ``` 바로 뒤에):
```
   - Example (subsequent reinforcement):
     ```
     ## Some Decision
     **Observed:** 3 times (2026-04-08, 2026-04-10, 2026-04-11)
     ```
```
  </action>
  <verify>
    <automated>
# 1) decisions.md 형식 확인
grep -c "Attempt.*Result.*Decision.*Status tags" patches/gsd-verifier.patch.md | grep -q "[1-9]" && echo "PASS: decisions.md format matches researcher" || echo "FAIL: decisions.md format mismatch"
# 2) troubleshooting.md 형식 확인
grep -c "Error message.*solution mapping" patches/gsd-verifier.patch.md | grep -q "[1-9]" && echo "PASS: troubleshooting.md format matches researcher" || echo "FAIL: troubleshooting.md format mismatch"
# 3) subsequent reinforcement 예시 존재 확인
grep -c "subsequent reinforcement" patches/gsd-verifier.patch.md | grep -q "[1-9]" && echo "PASS: subsequent reinforcement example found" || echo "FAIL: subsequent reinforcement example missing"
# 4) step 8 (uncertain) 유지 확인
grep -c "conflicting.*active.*uncertain" patches/gsd-verifier.patch.md | grep -q "[1-9]" && echo "PASS: uncertain rule preserved" || echo "FAIL: uncertain rule missing"
# 5) step 9 (Last compiled update) 유지 확인
grep -c "Last compiled.*today" patches/gsd-verifier.patch.md | grep -q "[1-9]" && echo "PASS: Last compiled update preserved" || echo "FAIL: Last compiled update missing"
    </automated>
  </verify>
  <done>
- verifier 패치의 decisions.md 설명이 "Attempt → Result → Decision. Status tags: [active], [rejected], [superseded], [uncertain]" 형식을 사용한다
- verifier 패치의 troubleshooting.md 설명이 "Error message ↔ solution mapping" 형식을 사용한다
- subsequent reinforcement 예시(3 times)가 존재한다
- 기존 step 번호, conflict/reinforcement 감지 규칙, step 8/9가 그대로 유지된다
  </done>
</task>

</tasks>

<verification>
1. `grep "Attempt.*Result.*Decision" patches/gsd-verifier.patch.md` — 형식 일치 확인
2. `grep "Error message" patches/gsd-verifier.patch.md` — troubleshooting.md 형식 확인
3. `grep "subsequent reinforcement" patches/gsd-verifier.patch.md` — 예시 존재 확인
4. diff 결과상 의도한 3가지 변경만 이루어졌고 나머지는 동일
</verification>

<success_criteria>
- researcher 패치와 verifier 패치 간 잔여 불일치 3가지가 모두 해소되었다
- D-08 패턴: decisions.md / troubleshooting.md 설명 및 reinforcement 예시가 두 패치 간 일관적이다
- 기존 verifier 고유 내용(step 번호, skip 조건, usage instruction 등)은 보존된다
</success_criteria>
