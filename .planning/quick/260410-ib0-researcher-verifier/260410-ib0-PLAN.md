---
phase: quick
plan: 260410-ib0
type: execute
wave: 1
depends_on: []
files_modified:
  - patches/gsd-phase-researcher.patch.md
  - patches/gsd-verifier.patch.md
autonomous: true
must_haves:
  truths:
    - "researcher Step 0 reads index.md 'Last compiled' date and only processes raw files newer than that date"
    - "researcher Step 0 skips entirely when no new raw files exist since last compile"
    - "Both patches include conflict detection: new entry contradicting existing entry gets [conflict: YYYY-MM-DD] tag with both contents preserved"
    - "Both patches include reinforcement detection: repeated observation increments Observed counter with date list"
  artifacts:
    - path: "patches/gsd-phase-researcher.patch.md"
      provides: "Incremental compile logic in Step 0 + conflict/reinforcement rules"
      contains: "Last compiled"
    - path: "patches/gsd-verifier.patch.md"
      provides: "Conflict/reinforcement rules in Step 10b"
      contains: "[conflict:"
  key_links:
    - from: "patches/gsd-phase-researcher.patch.md"
      to: ".knowledge/knowledge/index.md"
      via: "Last compiled date parsing"
      pattern: "Last compiled"
    - from: "patches/gsd-verifier.patch.md"
      to: ".knowledge/knowledge/*.md"
      via: "Conflict/reinforcement detection during reconcile"
      pattern: "\\[conflict:"
---

<objective>
researcher 패치(Step 0)에 증분 컴파일 로직을 추가하고, researcher/verifier 양쪽 패치에 충돌/강화 감지 규칙을 추가한다.

Purpose: 매번 전체 raw 파일을 재처리하는 비효율 제거 + 기존 knowledge 와 새 raw 항목 간 모순/재확인을 명시적으로 추적
Output: 업데이트된 두 패치 파일
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@patches/gsd-phase-researcher.patch.md
@patches/gsd-verifier.patch.md
@.knowledge/knowledge/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: researcher Step 0 incremental compile + conflict/reinforcement rules</name>
  <files>patches/gsd-phase-researcher.patch.md</files>
  <action>
researcher 패치(Step 0)를 아래 내용으로 교체한다. 기존 구조를 유지하되 세 가지를 변경:

**변경 A — 증분 컴파일 로직 (Step 0 상단, "If raw/ has content:" 직후):**
기존 "1. Read all `.knowledge/raw/*.md` files" 단계를 아래로 교체:

1. Read `.knowledge/knowledge/index.md` and extract the `Last compiled` date (YYYY-MM-DD format).
   - If index.md does not exist or has no "Last compiled" line, treat as first compile (process ALL raw files).
2. List `.knowledge/raw/*.md` files. Filter to only files with filename date >= the "Last compiled" date.
   - Filename pattern: `YYYY-MM-DD.md`. Compare the date portion with "Last compiled" date.
   - **If no files are newer than "Last compiled" date: skip Step 0 entirely** (log "No new raw entries since {date}, skipping compile").
3. Read only the filtered (newer) raw files.

기존 "2. Check if `.knowledge/knowledge/` exists." 부터의 나머지 단계는 번호를 순서에 맞게 조정하여 유지한다 (4, 5, 6...).

기존 "5. After reading existing knowledge files, add new entries or update existing entries to merge them" 다음에 아래 내용 추가:
"6. Update `index.md`: set `Last compiled` to today's date (YYYY-MM-DD), update `Total entries` count."

**변경 B — 충돌 감지 규칙 (기존 step 5 "add new entries or update existing entries to merge them" 아래에 추가):**
다음 하위 규칙을 추가:

   **Conflict detection:** When a new raw entry contradicts an existing knowledge entry (opposite conclusion, reversed decision, etc.):
   - Do NOT overwrite the existing entry.
   - Append a `[conflict: YYYY-MM-DD]` tag to the entry heading (where YYYY-MM-DD is today's date).
   - Preserve both contents: show existing content first, then new content with a `> **New (YYYY-MM-DD):**` blockquote.
   - Example:
     ```
     ## Some Decision [conflict: 2026-04-10]
     [existing content]
     > **New (2026-04-10):** [contradicting content]
     ```

**변경 C — 강화 감지 규칙 (충돌 감지 바로 뒤):**

   **Reinforcement detection:** When a new raw entry reconfirms an existing knowledge entry (same conclusion observed again):
   - Add or increment `**Observed:** N times (date1, date2, ...)` line below the entry heading.
   - If the `**Observed:**` line already exists, append the new date and increment the count.
   - Example (first reinforcement):
     ```
     ## Some Decision
     **Observed:** 2 times (2026-04-08, 2026-04-10)
     ```
   - Example (subsequent reinforcement):
     ```
     ## Some Decision
     **Observed:** 3 times (2026-04-08, 2026-04-10, 2026-04-11)
     ```

전체 문서의 마크다운 구조와 영어 작성 스타일을 기존 패치와 일관되게 유지한다.
  </action>
  <verify>
    <automated>grep -q "Last compiled" patches/gsd-phase-researcher.patch.md && grep -q "\[conflict:" patches/gsd-phase-researcher.patch.md && grep -q "Reinforcement detection" patches/gsd-phase-researcher.patch.md && grep -q "skip Step 0 entirely" patches/gsd-phase-researcher.patch.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>researcher 패치에 증분 컴파일(Last compiled 날짜 기반 필터링 + 신규 없으면 스킵) + 충돌 감지([conflict: YYYY-MM-DD] 태그) + 강화 감지(Observed 카운터) 규칙이 모두 포함됨</done>
</task>

<task type="auto">
  <name>Task 2: verifier Step 10b conflict/reinforcement rules</name>
  <files>patches/gsd-verifier.patch.md</files>
  <action>
verifier 패치(Step 10b)에 충돌/강화 감지 규칙을 추가한다. 기존 구조를 유지하되 한 가지를 변경:

**변경 — 충돌/강화 감지 규칙 추가:**
기존 "4. If conflicting `[active]` decisions exist, mark them `[uncertain]`" 앞에 (즉 step 3 하위항목의 끝 부분, reconcile 규칙들 다음에) 아래 두 규칙을 추가:

   **Conflict detection:** When a raw entry contradicts an existing knowledge entry (opposite conclusion, reversed decision, etc.):
   - Do NOT overwrite the existing entry.
   - Append a `[conflict: YYYY-MM-DD]` tag to the entry heading (where YYYY-MM-DD is today's date).
   - Preserve both contents: show existing content first, then new content with a `> **New (YYYY-MM-DD):**` blockquote.
   - Example:
     ```
     ## Some Decision [conflict: 2026-04-10]
     [existing content]
     > **New (2026-04-10):** [contradicting content]
     ```

   **Reinforcement detection:** When a raw entry reconfirms an existing knowledge entry (same conclusion observed again):
   - Add or increment `**Observed:** N times (date1, date2, ...)` line below the entry heading.
   - If the `**Observed:**` line already exists, append the new date and increment the count.
   - Example (first reinforcement):
     ```
     ## Some Decision
     **Observed:** 2 times (2026-04-08, 2026-04-10)
     ```

주의: D-08 패턴에 따라 충돌/강화 감지 규칙의 핵심 블록(Conflict detection, Reinforcement detection)은 Task 1의 researcher 패치와 동일한 텍스트를 사용한다.

verifier는 full reconcile이므로 증분 컴파일 로직은 추가하지 않는다 (기존의 "Full reconcile — reprocess ALL raw entries" 특성 유지).

전체 문서의 마크다운 구조와 영어 작성 스타일을 기존 패치와 일관되게 유지한다.
  </action>
  <verify>
    <automated>grep -q "\[conflict:" patches/gsd-verifier.patch.md && grep -q "Reinforcement detection" patches/gsd-verifier.patch.md && grep -q "Full reconcile" patches/gsd-verifier.patch.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>verifier 패치에 충돌 감지([conflict: YYYY-MM-DD] 태그) + 강화 감지(Observed 카운터) 규칙이 추가됨. D-08 패턴에 따라 researcher 패치의 해당 블록과 동일 텍스트 사용. full reconcile 특성은 유지.</done>
</task>

</tasks>

<verification>
1. researcher 패치: "Last compiled" 날짜 기반 증분 필터링 로직 존재
2. researcher 패치: 신규 raw 없으면 Step 0 스킵 로직 존재
3. researcher 패치: 충돌 감지 + 강화 감지 규칙 존재
4. verifier 패치: 충돌 감지 + 강화 감지 규칙 존재
5. 양쪽 패치의 충돌/강화 규칙 텍스트가 D-08 패턴에 따라 동일
6. verifier 패치의 "Full reconcile" 특성 유지 확인
</verification>

<success_criteria>
- researcher Step 0이 index.md의 "Last compiled" 날짜 이후 raw 파일만 처리하도록 지시
- 신규 raw 파일 없으면 Step 0 전체 스킵
- 양쪽 패치에서 기존 entry와 모순되는 새 항목은 [conflict: YYYY-MM-DD] 태그로 병기
- 양쪽 패치에서 기존 entry를 재확인하는 새 항목은 Observed 카운터 증가
</success_criteria>

<output>
After completion, create `.planning/quick/260410-ib0-researcher-verifier/260410-ib0-SUMMARY.md`
</output>
