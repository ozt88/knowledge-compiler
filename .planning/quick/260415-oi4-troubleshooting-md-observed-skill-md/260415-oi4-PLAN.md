---
quick_id: 260415-oi4
type: quick
autonomous: true
files_modified:
  - .knowledge/knowledge/troubleshooting.md
  - skills/gsd-knowledge-compile/SKILL.md
---

<goal>
troubleshooting.md 모든 기존 항목에 `**Observed:** 1 times (2026-04-15)` 필드를 추가하고,
SKILL.md Step 5 컴파일러 지시에 troubleshooting.md Observed 업데이트 규칙을 명시한다.

목적: B+C fusion 정책이 decisions.md에만 적용되고 troubleshooting.md에는 Observed 카운터가
없어 재발 빈도를 추적할 수 없는 상태를 해소한다.
</goal>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.knowledge/knowledge/troubleshooting.md
@skills/gsd-knowledge-compile/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: troubleshooting.md 모든 항목에 Observed 필드 추가</name>
  <files>.knowledge/knowledge/troubleshooting.md</files>
  <action>
각 항목의 마지막 줄(`**파일:**` 또는 `**상태:**` 줄) 다음 줄에 `**Observed:** 1 times (2026-04-15)` 를 추가한다.

troubleshooting.md의 항목 목록 및 수정 위치:

1. `## install.sh — PATCH 마커 존재 시 패치 미반영` — `**파일:** install.sh, ...` 줄 다음에 추가
2. `## gsd-tools phase add — 백로그 항목 Phase 번호 오인` — `**파일:** .planning/ROADMAP.md, .planning/STATE.md` 줄 다음에 추가
3. `## Phase 검증 — 부정형 패턴 grep 음성인데 코드 미반영` — `**파일:** patches/gsd-phase-researcher.patch.md, ...` 줄 다음에 추가
4. `## install.sh --force 인수 파싱 순서 버그` — `**파일:** install.sh (커밋: 41c29b0)` 줄 다음에 추가
5. `## install.sh awk -v 멀티라인 변수 개행 소실` — `**상태:** WR-02 — 코드 리뷰에서 발견, 미수정` 줄 다음에 추가
6. `## install.sh --force가 CLAUDE.md를 stale 내용으로 덮어씀` — `**파일:** templates/claude-md-section.md, ...` 줄 다음에 추가
7. `## gsd-clear/gsd-knowledge-compile — last_raw_captured UTC 타임존 불일치` — `**파일:** skills/gsd-clear/skill.md, ...` 줄 다음에 추가
8. `## install.sh unpatch_agent — <!-- PATCH --> 주석 블록 제거 불가` — `**파일:** install.sh (unpatch_agent), ...` 줄 다음에 추가
9. `## install.sh patch_workflow — 앵커 미매칭 시 무음 실패` — `**파일:** install.sh (patch_workflow discuss-phase 앵커), ...` 줄 다음에 추가
10. `## install.sh patch_agent — planner 패치 마커 미삽입 (매번 재패치 시도)` — `**파일:** patches/gsd-planner.patch.md ...` 줄 다음에 추가
11. `## gsd-knowledge-compile 스킬 — "Unknown skill" 오류` — `**파일:** skills/gsd-knowledge-compile/SKILL.md, install.sh ...` 줄 다음에 추가
12. `## gap closure 실행 중 git reset --soft로 인한 파일 삭제` — `**파일:** patches/gsd-discuss-phase.patch.md, ...` 줄 다음에 추가

날짜: 2026-04-15 (항목 최초 기록일 미상이므로 오늘 날짜 사용)

**주의:** `**상태:**` 줄이 있는 항목(항목 5)은 `**상태:**` 줄 다음, `**파일:**` 줄 없는 경우도 마지막 필드 다음에 추가.
  </action>
  <verify>grep -c "Observed:" /home/ozt88/knowledge-compiler/.knowledge/knowledge/troubleshooting.md</verify>
  <done>파일 내 Observed 줄 수 = 12 (항목 수와 동일)</done>
</task>

<task type="auto">
  <name>Task 2: SKILL.md Step 5에 troubleshooting.md Observed 업데이트 규칙 추가</name>
  <files>skills/gsd-knowledge-compile/SKILL.md</files>
  <action>
SKILL.md Step 5 `**파일별 업데이트:**` 섹션에서 `troubleshooting.md` 항목을 찾아 다음 규칙을 추가한다.

현재 SKILL.md 79번째 줄:
```
- `troubleshooting.md` — 오류 메시지 ↔ 해법 매핑
```

이를 다음으로 교체:
```
- `troubleshooting.md` — 오류 메시지 ↔ 해법 매핑
  - 신규 항목 기록 시 마지막 필드 다음 줄에 `**Observed:** 1 times (YYYY-MM-DD)` 포함 (오늘 날짜)
  - 기존 항목과 **동일한 에러**가 재발하여 raw에서 다시 등장하면: `**Observed:**` 카운터를 1 증가시키고 날짜를 추가: `**Observed:** N times (date1, date2, ...)`
  - 동일 에러 판별 기준: 에러 제목(`## ...`) 또는 **에러:** 설명이 실질적으로 같은 경우
```

**B+C fusion 정책** 블록 바로 위 또는 아래에 troubleshooting.md Observed 관련 설명이 이미 없는지 확인 후 추가한다.
  </action>
  <verify>grep -A 4 "troubleshooting.md" /home/ozt88/knowledge-compiler/skills/gsd-knowledge-compile/SKILL.md</verify>
  <done>troubleshooting.md 항목 아래에 Observed 신규/재발 규칙 3줄이 포함된다</done>
</task>

<task type="auto">
  <name>Task 3: 변경사항 커밋</name>
  <files>.knowledge/knowledge/troubleshooting.md, skills/gsd-knowledge-compile/SKILL.md</files>
  <action>
두 파일을 스테이징하고 커밋한다:

```bash
cd /home/ozt88/knowledge-compiler
git add .knowledge/knowledge/troubleshooting.md skills/gsd-knowledge-compile/SKILL.md
git commit -m "feat(260415-oi4): troubleshooting.md Observed 필드 추가 + SKILL.md 업데이트 규칙"
```
  </action>
  <verify>git log --oneline -1</verify>
  <done>커밋이 생성되고 두 파일이 포함된다</done>
</task>

</tasks>

<verification>
1. `.knowledge/knowledge/troubleshooting.md`: 모든 12개 항목에 `**Observed:** 1 times (2026-04-15)` 줄이 마지막 필드 다음에 추가됨
2. `skills/gsd-knowledge-compile/SKILL.md`: `troubleshooting.md` 항목 아래 신규/재발 Observed 규칙이 명시됨
3. 커밋이 두 파일을 포함하여 생성됨
</verification>

<success_criteria>
- troubleshooting.md: `grep -c "Observed:" .knowledge/knowledge/troubleshooting.md` = 12
- SKILL.md: troubleshooting.md 섹션에 "재발" 또는 "카운터" 언급이 존재함
- git log에 260415-oi4 커밋이 존재함
</success_criteria>
