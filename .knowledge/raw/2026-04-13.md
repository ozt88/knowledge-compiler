### 00:00 — knowledge-compiler 재설치 (--force)
- `gsd-code-reviewer.md`, `gsd-code-fixer.md`가 `~/.claude/agents/`에 없어 install.sh가 실패함
- 원인: GSD 업데이트로 해당 에이전트들이 pr-review-toolkit 플러그인으로 이동된 것으로 추정
- `install.sh`의 `patch_agent` 함수에서 파일 누락 시 `error + return 1` → `warn + return 0`으로 수정
- 재설치 성공: phase-researcher, planner, executor, plan-checker, debugger, nyquist-auditor, discuss-phase, new-project, new-milestone, CLAUDE.md, gsd-knowledge-compile skill 모두 재적용
- 변경 파일: `/home/ozt88/knowledge-compiler/install.sh`

### 08:22 — gsd-knowledge-compile 스킬 "Unknown skill" 오류 수정
- 원인: `install.sh`가 `skill.md` (소문자)로 복사하는데 Claude Code는 `SKILL.md` (대문자)만 인식
- 수정: 소스 파일 git mv, install.sh 2줄 수정, 설치된 파일 rename
- 결과: 다음 대화에서 스킬 목록에 정상 표시됨

### 16:35 — install.sh new-project/new-milestone 패치 버그 수정
- install.sh에서 "patched" 메시지만 출력하고 실제 파일 수정을 하지 않는 버그 수정
- new-project.md 패치: git init 뒤에 `mkdir -p .knowledge/raw .knowledge/knowledge` 삽입하는 awk 코드 추가
- new-milestone.md 패치: `## 8. Research Decision` 앞에 동일 명령 삽입하는 awk 코드 추가
- gsd-planner.md: `<!-- PATCH:knowledge-compiler for gsd-planner.md -->` 마커 삽입 완료
- 실제 워크플로 파일(new-project.md, new-milestone.md)에도 직접 패치 적용
