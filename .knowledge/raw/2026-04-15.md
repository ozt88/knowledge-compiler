
### 14:00 — index-first 패턴 재확인
- knowledge 참조 시 index.md를 먼저 읽어 관련 파일만 선택적으로 로드하는 방식이 효율적임을 다시 확인
- 전체 파일을 순차적으로 읽으면 컨텍스트 비용이 높고 무관 정보가 포함됨
- Phase 8 planning에서 index-first 접근을 표준으로 유지하기로 재확인 (기존 결정 reinforcement)

### 14:01 — GSD knowledge 최소 부하 원칙 재검토
- GSD 프로세스에서 knowledge compile 부하를 최소화하는 현재 원칙을 재검토함
- planner가 fallback compile을 수행하는 것이 knowledge 최신성 유지에 도움이 될 수 있음
- /gsd-knowledge-compile 수동 전용보다 planner 자동 compile이 더 신뢰성 있을 수 있다는 관점
- 즉, GSD 최소 부하 원칙보다 자동 compile 빈도를 높이는 방향이 더 효과적일 수 있음

### 07:30 — Phase 08-02: B+C Fusion Decay + 조회 지침 + install.sh 버그 수정
- SKILL.md Step 5: [uncertain] 상태 전환 추가, "직접적으로 모순" 판단 기준 명시
- 패치 3개(researcher/planner/discuss): uncertain/superseded 처리 지침 + Observed 우선순위
- install.sh --force unpatch 로직 버그 3개 수정:
  - em dash awk regex → index() 함수로 교체
  - nested marker 재매칭 → skip == 0 조건 추가
  - skip 계산 오류(block_size+1→block_size-1): 마커가 patch_content 2번째 줄 기준
- gsd-planner.patch.md에 PATCH 마커 줄 추가 (없으면 grep -q 검사 실패)
- discuss-phase 앵커: check_existing → cross_reference_todos (실제 파일에 존재하는 앵커)

### 08:00 — Phase 8 실행 완료: B+C fusion 동작 검증
- Wave 2 executor dangling 커밋 복구 패턴: fast-forward merge로 `git merge <commit-hash>` 사용
- B+C fusion PASS: index-first Observed 1→2, GSD 최소 부하 원칙 [active]→[uncertain]+[conflict:]
- 워크트리 병합 "Already up to date" 발생 시 `git log --oneline --all`로 dangling 커밋 여부 확인 필수

### 17:00 — Quick 260415-oi4: troubleshooting.md Observed 필드 추가 계획
- troubleshooting.md 12개 항목 전부에 `**Observed:** 1 times (2026-04-15)` 필드 추가 계획 작성
- SKILL.md Step 5에 troubleshooting.md 신규/재발 Observed 업데이트 규칙 추가 예정
- decisions.md에만 있던 B+C fusion Observed 카운터를 troubleshooting.md로 확장하는 작업

### 21:00 — anti-patterns/guardrails/troubleshooting context 태그 추가 + SKILL.md 업데이트
- anti-patterns.md: 7개 항목 각각 [context: ...] 태그 + **Observed:** 1 times (2026-04-15) 추가
- guardrails.md: 13개 항목 각각 [context: ...] 태그 추가
- troubleshooting.md: 12개 항목 각각 [context: ...] 태그 추가 (Observed는 기존 유지)
- SKILL.md: guardrails/anti-patterns/troubleshooting 신규 항목 기록 시 [context: ...] 태그 포함 규칙 추가, anti-patterns Observed 초기값 및 재발 카운터 증가 규칙 추가

### 22:00 — Phase 8 UAT 완료: 7/7 pass
- decisions.md 형식, index.md 동기화, SKILL.md B+C fusion decay 규칙, 패치 파일 조회 우선순위, install.sh --force 멱등성, B+C fusion 증강/감쇄 모두 검증 통과
- context/Observed는 decisions.md 전용 메타데이터 — guardrails/anti-patterns/troubleshooting은 각각 별도 구조 사용 (확인됨)

### 22:30 — knowledge 파일 전체 확장 설계 결정
- context 태그와 Observed를 decisions.md 전용에서 전체 knowledge 파일로 확장하기로 결정
- guardrails.md: context 태그만 (Observed 불필요 — guardrail은 binary 제약, 신뢰도 카운터 의미 없음)
- anti-patterns.md: context 태그 + Observed 추가 (재발 빈도 추적 유의미)
- troubleshooting.md: context 태그 + Observed 추가 (동일 에러 재발 빈도 추적 유의미)

### 23:00 — 빈 환경 install 검토 → 3가지 이슈 발견 및 수정
- 이슈 1 (버그): install.sh new-project/new-milestone 패치 블록에서 `local tmp_file`을 함수 밖에서 사용 → set -e 환경에서 fresh install 시 크래시. `local` 제거로 수정
- 이슈 2 (스테일 주석): 헤더/Done 출력에 Phase 6에서 제거된 verifier 패치, planner fallback compile 언급 → 갱신
- 이슈 3 (시드 없음): .knowledge/knowledge/ 디렉토리는 생성되지만 파일 없음 → 첫 컴파일 시 형식 보장 안 됨
- 해결: templates/knowledge-seed/ 에 시드 파일 5개 생성 (decisions/guardrails/anti-patterns/troubleshooting/index.md), install.sh --project 시 자동 복사 (파일 없을 때만)

### 23:30 — knowledge seed 파일 설계 원칙
- 시드에 포함할 것: 어느 프로젝트에나 적용되는 메타 지식 (raw/ 읽기 경유 필수, 부정형 지시 금지, index.md 형식, per-turn 수집 방식 등)
- 시드에 포함하지 않을 것: 이 repo 고유 지식 (install.sh 버그, GSD Phase 번호 오인, PATCH count 확인 등)
- decisions.md/troubleshooting.md: 헤더+형식 주석만 (내용은 항상 프로젝트 고유)

### 23:59 — knowledge compile 완료
- decisions.md: context/Observed 전체 파일 확장 결정, knowledge seed universal 전략 신규 항목 추가 (→ 18개)
- troubleshooting.md: install.sh local 함수 밖 사용 set -e 크래시 신규 항목 추가 (→ 12개)
- guardrails.md: knowledge seed 파일 universal-only 원칙 신규 항목 추가 (→ 14개)
- index.md: Total entries 56, Phase 8 요약 업데이트, 키워드 인덱스 확장 (context 태그, Observed, seed, local, set -e)
- compile-manifest.json: Phase 8 planning 파일 10개 + quick task 4개 추가
- 커밋: 965801b (knowledge compile) + 5a99e74 (raw files)
